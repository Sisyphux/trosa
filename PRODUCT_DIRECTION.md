# Trade OS 产品口径

> Trade OS 是外贸客户关系工作台。它保存关系上下文、呈现已到期承诺，并帮助业务员把每次实际进展沉淀为可追溯记录。

## 产品核心

系统围绕四个长期对象组织工作：

| 对象 | 用户看到的名称 | 当前数据 |
|---|---|---|
| Company | 客户 | `customers` |
| People | 联系人 | `contacts` |
| Activity | 沟通记录、开发邮件、调研备注 | `follow_up_logs`、`outreach_emails`、`research_reports` |
| Task | 待办、下一步 | `reminders` |

日常闭环为：

```text
打开客户并恢复上下文
  → 执行已确认的动作
  → 记录实际发生的事实
  → 按需确认新的动作与日期
  → 到期时再次出现
```

Activity 可以没有下一步。Task 必须同时包含具体动作和日期。

## 页面职责

- **今天**：展示全部到期和逾期承诺、当前关注顺序与未来 14 天安排。
- **Inbox**：收集需要人判断的新回复、未联系复查和少量系统信号，处理后安静离开。
- **客户**：搜索、理解和维护全部公司关系，以最近发生、当前等待和下一步为重点。
- **本周工作**：从真实记录中选择需要团队共享的内容。
- **更多工具**：承载新客户、完整日历、全部记录和操作日志。
- **设置**：管理个人界面、可选模块、导入与数据维护。

## 信息压力规则

| 信息 | 放置位置 | 形成条件 |
|---|---|---|
| 已经发生的事实 | 客户时间线 | 用户记录或确认导入 |
| 需要判断的新信号 | Inbox | 尚未被现有记录或待办覆盖 |
| 明确的未来承诺 | 今天 / 日历 | 同时有动作和日期 |
| 可能值得关注 | 动态视图或可选建议 | 不自动生成逾期 |
| 日常无需再看 | 归档 | 保留并可恢复 |

等待回复、很久未联系、没有下一步等状态尽量从事实动态计算。等级和旧阶段字段仅承担历史兼容，不成为默认工作负担。

## 能力优先级

### 固定核心

- 客户、联系人与搜索
- 沟通记录和关系时间线
- 明确待办、今天与完整日历
- Inbox 与操作日志
- 归档和恢复
- 本地快照、完整性检查与备份恢复
- Excel 导入、来源审计和导出

### 可选辅助

- AI 问答、沟通整理和客户分析
- 官网监控
- 邮箱可发送性复核
- Apple 日历订阅
- 本周工作、开发邮件和 Excel 导入入口的个人显示开关

可选模块关闭后保留历史数据，固定核心继续工作。AI 仅在用户主动触发或明确启用的后台任务中运行。

## 数据口径

- `data/` 或 `CRM_DB_PATH` 指向的 SQLite 目录是唯一业务数据源。
- 每位业务员使用独立数据库，系统设置存于 `system.db`。
- Excel 是导入、导出和历史恢复格式；导入保留文件、工作表、单元格和内容指纹。
- Apple 日历通过个人 ICS 地址读取待办事件，日历客户端不能回写 CRM 数据。
- 当前运行链路不执行 iCloud 数据库复制，也不提供跨设备冲突合并。
- 数据修改后创建延迟安全快照；恢复前先备份当前版本，并校验清单、哈希和 SQLite 完整性。

## AI 边界

AI 是可关闭的辅助阅读层，可整理网站内容、总结沟通、生成分析或回答数据库范围内的问题。

- 输入范围和模型密钥需显式配置。
- 输出需区分事实、推断和未知。
- 模型输出不会直接改写客户、联系人、记录或待办。
- 建议形成待办前，用户确认客户、动作和日期。
- 消息发送、报价和商业承诺始终由用户完成。
- 未配置模型时，固定核心功能保持可用。

Pi Agent 作为 CRM 的自然语言操作层，通过 `/api/agent/*` 接口向上层 AI 模型提供原子工具：读取类直接执行，写入类生成提案待用户确认；明确的固定流程才使用对应 Skill。工具负责读取或执行单一业务动作，Skill 提供稳定流程经验，Agent 负责理解任务、组合步骤和说明依据。Skill 不是普通自然语言任务的前置条件。

前端 Pi 聊天入口已于 2026-08-05 移除，不再存在独立的 Pi SDK Runtime 执行引擎；模型调用由上层 AI 模型负责，不可用时读取与提案生成仍可工作，写入需用户确认。客户、联系人、沟通记录、待办、Inbox、日历和备份恢复等固定核心不依赖模型。

- 通用 Agent 可以直接搜索客户、查询沟通记录、读取客户时间线和整理下一步建议。
- 原子工具返回已记录事实和信息缺口，不替用户推断客户价值、意向或商业承诺。
- 新增、修改、完成待办和记录沟通仍需用户确认；发送消息、报价和商业承诺仍由用户执行。
- Agent 通过 Trosa 业务接口访问数据，不直接访问 SQLite、文件或 Shell。

## 私人定制

- 主导航保持稳定，个人可以调整顺序、默认页面、列表字段和 Inbox 范围。
- 可选模块统一在设置中隐藏或显示。
- 隐藏只影响入口和后台开关，不删除历史数据。
- Hamid、Amy、Kelley 的偏好和业务数据分别保存。

## 长期边界

当前阶段不引入数字评分式客户优先级、自动发送消息、复杂审批流、第三方模块市场和大规模权限体系。出现高并发、多地同时编辑或十几位以上用户时，再评估 PostgreSQL 与集中式托管。
