# 变更日志

## 2026-08-24 — 将服务器工作台源码移出 Trosa 项目

- 项目边界：Mac 原生服务器工作台已移到桌面上的独立项目 `/Users/luoxin/Desktop/Trosa AI OS`；Trosa 网站仓库不再包含 `tools/trosa-manager`。
- 运行影响：已安装的服务器工作台仍默认管理 `~/Desktop/Trosa`，不会改变 Trosa 的 SQLite、客户数据、附件或 ECS 运行服务；Trosa 的 `deploy/cloud` 脚本仍留在目标网站项目中供工作台调用。
- 后续方向：Ubuntu 轻量桌面、浏览器远程访问和 AI 控制层只在独立项目中继续建设，尚未对生产 ECS 安装图形桌面或远程桌面服务。
- 验证：控制器源码已移动，独立项目 README 和构建入口已补齐；已从新路径重新构建安装，真实窗口仍能连接 Trosa 并显示服务器状态与资源。

## 2026-08-24 — 让服务器资源后台静默刷新

- 用户可见变化：服务器工作台每 30 秒在后台更新 CPU、内存、磁盘、负载和运行时间；刷新期间保留上一次数值，不再显示“读取中”或“正在采集服务器数据”，收到新数据后直接替换，成功刷新不会改写顶部活动提示。
- 错误与性能边界：资源读取继续在后台命令线程执行；首次打开和用户主动点击“立即检查”仍显示必要的检查状态。后台连接失败时保留最后一次成功资源数据，并保留可重试的错误说明；切换项目时会清掉旧主机快照，避免跨服务器显示旧值。
- 数据/依赖影响：不修改 SQLite、客户数据、附件或数据库结构；不新增依赖。
- 验证：Swift 类型检查通过；原生工作台已重新构建安装；真实窗口完成首屏、30 秒周期和概览页验收，资源数值正常更新，自动刷新期间未出现“读取中”或“正在采集服务器数据”。

## 2026-08-24 — 缩短 SSM 状态与发布命令

- 运维变化：状态读取和代码发布的详细脚本现在随每个 GitHub release 一起保存；本地工作台通过 SSM 只发送短命令，让 ECS 下载并执行当前 commit 中的脚本，避免交互式终端承载长脚本时丢失字符。
- 数据/依赖影响：不修改 SQLite、客户数据、附件或数据库结构；仍使用公开 GitHub commit、ECS 现有 `curl`、`bash`、`tar`、Python 虚拟环境和 systemd。
- 验证：新增脚本通过 Shell 语法检查；已实际完成短命令发布，ECS release 为 `20260824055757`，状态脚本稳定返回 `TROSA_MANAGER_STATUS` 与 `TROSA_MANAGER_RESOURCE`，远端退出码为 0，公网 `/api/network/ping` 返回 `status=ok`。

## 2026-08-24 — 加固 SSM 长命令发布结果处理

- 运维变化：Workbench 的 SSM 交互式 Shell 现在逐字符、低速发送压缩命令，降低长发布命令在终端输入缓冲区中丢字或错字的概率；同时会把远端命令的真实退出码返回给本地脚本，不再把远端失败误报为发布成功。
- 数据/依赖影响：不修改 SQLite、客户数据、附件或数据库结构；继续使用现有 Workbench CLI、macOS `expect`、`gzip`、`base64`，不新增第三方依赖。
- 验证：已复现并捕获一次损坏命令；修复后已通过真实 SSM 发布，ECS release 为 `20260824054913`，服务状态正常，公网 `/api/network/ping` 返回 `status=ok`，远端命令退出码被正确返回为 0。

## 2026-08-24 — 修复服务器工作台的 GitHub 同步失败

- 用户可见变化：工作台“保存并同步上线”和“同步 GitHub”不再因为系统 Git 找不到 GitHub Desktop 的登录凭据而失败；会复用当前 Mac Keychain 中 GitHub Desktop 已登录的写入凭据，成功后再继续 ECS 发布。
- 安全与数据边界：凭据只由 macOS `security` 在推送进程内存中临时读取，不写入项目、Git 配置、日志、SQLite 或服务器；未找到凭据时仍会停止发布，避免 GitHub 未同步而网站先更新。
- 数据/依赖影响：不修改 SQLite、客户数据、附件或数据库结构；不新增第三方依赖，继续使用 macOS 自带 Git、Keychain 和 GitHub Desktop 登录状态。
- 验证：已用当前 GitHub Desktop Keychain 凭据完成实际 GitHub 推送；新版工作台已完成 Swift 类型检查和安装，随后成功执行提交、推送、ECS 发布与健康检查，服务器 release 为 `20260824054517`，公网 `/api/network/ping` 返回 `status=ok`。

## 2026-08-24 — 修复 SSM 模式下的代码发布并明确 GitHub 同步边界

- 用户可见变化：服务器工作台的“保存并同步上线”现在可以在 ECS 不支持 Workbench 非交互式 `exec` 和文件上传时继续发布；云端通过 SSM 交互式会话下载并发布刚推送的 GitHub commit，完成健康检查后再切换正式版本。
- 发布行为：发布对象固定为本地当前 `HEAD` 对应的公开 `Sisyphux/trosa` commit，不包含未提交文件、本地数据库、附件、密钥或 Python 虚拟环境；失败时保留原 release 并自动回退。单独 GitHub Push 仍只同步源码，不直接触发 ECS，避免把“同步成功”误报为“已上线”。
- 数据/依赖影响：不修改 SQLite、客户数据、附件或数据库结构；SSM 发布路径使用 ECS 现有的 `curl`、`tar`、Python 虚拟环境和 systemd，不新增应用运行时依赖。当前仓库必须保持公开，云端才能在无额外 GitHub Token 的情况下下载 commit 归档。
- 验证：`publish-workbench.sh`、`run-workbench-command.sh` Shell 语法检查和 Git diff 检查通过；2026-08-24 已通过真实 SSM 发布，ECS release 为 `20260824032443`，`app=active`、`tunnel=active`、`health=ok`，公网 `/api/network/ping` 返回 `status=ok`。

## 2026-08-24 — 将 GitHub 项目改名为 trosa

- 用户可见变化：正式代码仓库已从 `Sisyphux/crm-reminder` 改名为 `Sisyphux/trosa`；原有代码、main 分支和提交历史保留。账号下另一个名为 `-` 的空仓库未改动。
- 连接变化：本机项目的 `origin` fetch/push 地址已同步更新为 `https://github.com/Sisyphux/trosa.git`，GitHub Desktop 和服务器工作台后续会使用新仓库名。
- 数据/依赖影响：不修改 SQLite、客户数据、附件、服务器或业务代码；本次只调整 GitHub 仓库元数据和本机 Git remote 配置。
- 验证：GitHub 页面已显示 `Sisyphux/trosa`，本地 `git ls-remote origin` 成功返回 main 提交 `9e710fe`；原有 16 处本机未提交修改保持不变，未在本次改名操作中提交或推送。

## 2026-08-24 — 修复服务器工作台的 ECS Session Manager 连接兼容

- 用户可见变化：服务器工作台现在兼容 Workbench 在 SSH 不可用时自动降级的 ECS Session Manager（SSM）交互式会话；概览可以重新读取网站、应用、安全连接、release、CPU、内存、磁盘、负载和运行时间，远程重启、系统更新、目录读取、新建目录和回收站操作也共用同一连接方式。
- 运维/权限变化：已在阿里云 ECS 控制台开启 Session Manager（全地域）。工作台每次操作只建立一次短生命周期交互式会话，命令完成后立即关闭；不开放公网管理端口，也不把终端会话留在后台。
- 数据/依赖影响：不修改 SQLite、客户数据、附件或数据库结构；新增脚本只使用 Workbench CLI 与 macOS 自带的 `expect`、`gzip`、`base64`。SSM 模式本身不支持 Workbench 原生的非交互式 `exec`、上传、下载和 Agent 模式，文件传输入口仍需 SSH 恢复或单独适配。
- 验证：修复 CRLF 终端换行解析后，原生工作台实际显示“服务器运行正常”，并显示真实 ECS 资源数据；`app=active tunnel=active health=ok`，公网 `/api/network/ping` 返回 `status=ok`；Swift 类型检查、原生 App 构建安装、Shell 语法检查和 Git diff 检查通过。

## 2026-08-24 — 为服务器工作台增加主机资源概览与自动检查

- 用户可见变化：服务器工作台“概览”新增 CPU 使用率、内存使用率与容量、根分区磁盘使用率与容量、1/5/15 分钟负载、服务器运行时间；概览每 30 秒自动检查一次，也可手动立即检查。Workbench 连接失败时显示“无法读取服务器”，不会把连接失败误报为服务器停止或空数据。
- 运维变化：`status-workbench.sh` 新增稳定的 `TROSA_MANAGER_RESOURCE` 机器可读状态行，采集 `/proc`、`free` 和 `df` 的服务器本机数据；原有 systemd、健康检查、日志和技术记录保持不变。现有重启应用、重启 Tunnel、整机重启、系统更新、终端、发布、文件和备份入口保持不变。
- 数据/依赖影响：不修改 SQLite、客户数据、附件或数据库结构；不新增第三方依赖。仅增加远程状态读取和 Mac 工作台内存中的刷新定时器。
- 验证：已通过 Swift 类型检查、原生 App 构建、`plutil` 和 Shell 语法检查；全量 62 项 Python 回归测试通过；本地仿真能输出稳定资源状态行，真实窗口已验收“读取中”和 Workbench 失败两种状态。公网 `/api/network/ping` 返回 `status=ok`；真实 ECS 资源读数已在 Session Manager 恢复后完成验收。

## 2026-08-23 — 恢复公司固定地址的云端只读周报入口

- 用户可见变化：公司 Mac 在公司网络取得固定地址 `192.168.0.58` 后，会自动在 `http://192.168.0.58:8080` 提供“本周工作”；公司内其他设备无需成员账号即可查看。入口常年自动等待，不设置人工开关；Mac 离开公司、关机或失去该地址后自动停止提供服务，重新连接公司网络后自动恢复。
- 安全与数据边界：Trade OS 与 SQLite 仍只在 ECS 运行和写入。Mac 新增的局域网入口不打开本地数据库，只转发页面静态资源、周报汇总和白名单内的只读客户详情；所有写入方法、普通客户接口、设置和备份接口均在 Mac 端拒绝，ECS 端继续用现有只读权限二次限制。Mac 保存随机原始密钥，ECS 只保存 SHA-256 摘要；云端仅接受来自本机 Tunnel 对端且密钥正确的请求。
- 文件/依赖影响：新增 `deploy/macos/weekly-lan-gateway.py`、安装/启动脚本、LaunchAgent 和私密环境模板；使用 macOS 自带 Python 与 launchd，不新增第三方依赖。`app.py` 增加独立周报网关识别，不新增数据库表、不修改客户数据。
- 验证：全量 62 项 Python 回归测试、Python/JavaScript/Shell 语法检查和 LaunchAgent `plutil` 全部通过。正式 ECS release `20260823135346` 已发布且应用、Tunnel、健康检查均正常；公网无网关凭证仍回到登录页，正确凭证可读周报，客户写入与错误凭证均返回 401。Mac LaunchAgent 已安装并保持运行；离开公司网络时确认 8080 未监听。2026-08-24 Mac 回到公司后取得 `192.168.0.58`，自动监听已确认；通过固定地址完成端到端验证：页面外壳与周报读取返回 200，普通客户接口返回 403，写入返回 405，`/api/auth/me` 显示未登录但周报查看权限有效。

## 2026-08-23 — 明确服务器工作台与 Trosa 项目的边界

- 用户可见变化：Mac 应用正式更名为“服务器工作台”（v1.2.1），窗口、应用名与安装包均不再以 Trosa 命名。首页明确标注“当前项目：Trosa”，并把 Trosa 网站、Trosa 应用与服务器安全连接分开呈现；代码发布页明确说明 GitHub 保存的是 Trosa 源码，服务器工作台负责本机与服务器的管理操作。GitHub 同步按钮现在固定使用本机系统钥匙串和更稳定的 HTTP/1.1 连接，不再依赖未安装的 GitHub CLI。
- 文件/安装影响：安装新版时，已安装的旧 `trosa Server Manager.app` 会移动到 Mac 回收站，新的 `服务器工作台.app` 安装在“应用程序”目录；不会修改服务器、业务数据库、客户资料、附件或备份。
- 依赖影响：仍只使用 macOS Cocoa、Swift、系统钥匙串与已配置的 Workbench CLI，不新增应用、凭据文件或运行时依赖。
- 验证：`swiftc -parse-as-library -typecheck`、`plutil -lint`、`bash -n` 与 Git diff 检查通过；新版已安装为 `/Users/luoxin/Applications/服务器工作台.app`，实际窗口验收确认标题为“服务器工作台”、副标题明确“当前项目：Trosa · 管理这台服务器”，并正确显示 Trosa 网站、Trosa 应用、安全连接和当前版本。真实 ECS 状态查询返回 `app=active tunnel=active health=ok`，公网健康接口正常。

## 2026-08-23 — 将 Mac 管理器升级为面向日常维护的 trosa 工作台

- 用户可见变化：原 `trosa Server Manager` 重构为 `trosa 工作台`（v1.1）。首页先展示“是否需要处理”的结论，并分别显示网站、trosa 应用、安全连接和当前网站版本；新增四个日常入口：上传客户资料、管理服务器文件、更新网站、立即备份。技术记录默认收起，异常时才展开。文件页增加客户资料/应用数据/回收站/服务器根目录快捷入口、上一级导航和更清楚的回收站说明；更新网站页将流程收敛为“保存并同步上线”，严格执行本机 Git 提交 → GitHub 同步 → ECS 发布，GitHub 同步失败时不更新网站。
- 数据/权限影响：不修改业务数据库、客户资料或服务器文件。首次使用文件页时 macOS 可能请求桌面文件夹访问权限，用于读取本机 Trosa 项目和用户主动选择上传的文件；`Info.plist` 提供明确系统权限说明。服务器删除仍先移入 `/var/lib/trade-os/.trosa-trash`，保留 7 天。
- 运维变化：`status-workbench.sh` 额外输出稳定的 `TROSA_MANAGER_STATUS` 状态行，工作台据此可靠显示应用、Tunnel、公网健康与当前 release，原有详细诊断输出仍保留在技术记录中。
- 依赖影响：继续只使用 macOS Cocoa、Swift 与已配置的 Workbench CLI，不新增应用、Python 或 Node 运行时依赖。
- 验证：Swift 类型检查和原生 App 构建通过；在 Mac 实际打开工作台，验证概览显示 ECS 网站/应用/Tunnel 均正常、当前 release 为 `20260821083700`；验证文件页布局与系统权限提示；`bash -n deploy/cloud/status-workbench.sh` 和真实 ECS 状态查询通过，返回 `app=active tunnel=active health=ok`。

## 2026-08-21 — 增加 trosa Server Manager 与本地滚动备份

- 用户可见变化：新增可双击运行的 Mac 原生 `trosa Server Manager.app`，集中提供 ECS 状态、服务重启、系统更新、服务器终端、远程文件浏览/上传/下载/回收站、代码发布/回滚和 GitHub 同步入口。
- 数据变化：新增 ECS 一致性快照下载脚本；备份包含数据库、附件、manifest 和校验信息，下载到 Mac 后核对 SHA-256，并自动清理超过 14 天的本地归档。新增每日 03:30 的 Mac LaunchAgent 安装脚本。
- 依赖影响：管理器使用 macOS Cocoa 和现有 Workbench CLI，不新增 Python/Node 运行时依赖；新增 `tools/trosa-manager/` 和 `deploy/macos/install-trosa-backup.sh`。
- 验证：Swift 原生 App 编译并启动成功；备份归档实际下载 31 MB，SHA-256 与 ECS 输出一致，内容包含四个数据库和附件目录；现有 58 项 Python 测试、浏览器扩展 smoke test、Shell 语法检查通过。

## 2026-08-21 — 完成 ECS 正式切换与公网验收

- 部署变化：Trade OS 已从 Mac 正式运行目录切换到阿里云 ECS；`trade-os` 与 Cloudflare Tunnel 均已配置为 systemd 自启，域名 `https://app.trosa.space` 已由 ECS 提供服务。
- 数据变化：从 Mac 正式数据目录生成最终一致性快照，恢复 `system.db`、Hamid/Amy/Kelley 三个业务库和 4 个附件；ECS 上四个 SQLite 数据库 `integrity_check` 均为 `ok`。旧 Mac LaunchAgent 已卸载并禁用，保留本地文件用于回退。
- 安全与运维：应用继续只监听 `127.0.0.1:8080`；ECS 主机防火墙初始化脚本只放行 SSH，发布包不含数据、密钥和虚拟环境。新增/完善状态、日志、发布和回滚说明。
- 验证：公网健康接口 HTTP 200；HTTPS 安全响应头和生产 CORS 白名单检查通过；Cloudflare Tunnel 4 条连接已注册；服务 `trade-os`、`cloudflared` 均为 `active/enabled`。

## 2026-08-21 — 增加 ECS + Workbench 云部署与生产安全基线

- 用户可见变化：生产服务的跨域访问改为显式来源白名单；浏览器响应增加基础安全头；浏览器扩展默认连接正式 `app.trosa.space` 服务地址，并保留本地开发地址切换能力。新增 Workbench 状态查看、日志读取、发布和回滚命令，后续可从本地项目目录完成云端维护。
- 部署变化：新增干净发布包构建、ECS 基础环境初始化、systemd Cloudflare Tunnel 服务和原子 release 切换流程。发布包排除 SQLite 数据、附件、备份、环境文件、密钥、虚拟环境和本地开发目录；云端继续保持单实例、单一 SQLite 写入源。
- 数据/依赖影响：不修改业务数据库、不迁移客户数据；固定 `requests`、`openpyxl`、`xlrd`、`email-validator` 和 `dnspython` 的运行版本；新增 `deploy/cloud/` 运维脚本和 `cloudflared.service`，未新增应用运行时依赖。
- 验证：全量回归测试 58 项通过；`py_compile`、`node --check`、`bash -n` 和 `zsh -n` 通过；发布归档成功生成并确认包含应用代码、依赖清单、部署脚本和浏览器扩展，不包含本地数据与生产密钥。ECS 只读体检确认 Ubuntu 22.04、2 vCPU、40GB 磁盘、时区同步正常，尚未启用云端服务。

## 2026-08-20 — 修复操作日志页永不加载与登录页周报入口误导

- 用户可见变化：侧边栏“操作日志”页面此前一直停留在“Loading...”，现在进入页面即加载最近 100 条操作日志，并可按全部/创建/更新/删除/完成/同步筛选。登录页的“本周工作”入口只在公司局域网（内部只读查看者）可见；从本机或公网打开时不再显示该按钮，避免点击后出现误导性的“登录已过期，请重新登录”提示并跳回登录页。
- 原因：`switchPage()` 的页面分发缺少 `case 'logs'`，`loadLogs()` 从未被调用；登录页“本周工作”按钮无条件渲染，但周报接口仅允许内部查看者（`CRM_INTERNAL_VIEWER_CIDRS`）访问，非局域网点击必然 401。
- 数据/依赖影响：无数据库结构、业务数据或第三方依赖变更；仅前端 `app/static/app.js` 两处修改（补充分发分支、登录页按 `/api/auth/me` 的 `internal_viewer` 隐藏周报入口）。
- 验证：`node --check app/static/app.js`、`python -m py_compile app.py db.py scheduler.py` 通过；全量 57 项回归测试通过；真实浏览器验收：登录 Hamid 后操作日志页正常加载并显示筛选按钮与日志条目，客户/Inbox/日历/搜索等页面无 console error、无 API 失败；未登录本机访问时登录页不再显示“本周工作”按钮。

## 2026-08-20 — 统一客户国家名：全部英文国家名归一为中文

- 用户可见变化：客户国家字段中的英文国家名全部统一为中文。扫描三个用户库（amy / hamid / kelley）的 `customers.country` 与 `import_unmatched_customers.country`，共 94 条变更：hamid 库 92 条（Malaysia→马来西亚、Brazil→巴西、Peru→秘鲁、Spain→西班牙、Bahrain→巴林、Poland→波兰、Singapore→新加坡、Chile→智利、Thailand→泰国、Panama→巴拿马、Netherlands→荷兰、Ecuador→厄瓜多尔、Belgium→比利时、Vietnam→越南、Qatar→卡塔尔、Portugal→葡萄牙、Honduras→洪都拉斯、Guatemala→危地马拉、DE→德国、Costa Rica→哥斯达黎加、Argentina→阿根廷、United Arab Emirates→阿联酋），kelley 库 2 条（Dominican Republic多米尼加共和国→多米尼加共和国），amy 库无英文国家名。国家筛选、列表与详情显示随之统一。
- 文本字段：仅统一中文语境与结构化标注中的国家名（系统备注「UAE+沙特」→「阿联酋+沙特」、跟进日志同、客户备注「城市: Dubai, UAE」→「城市: Dubai, 阿联酋」等 9 处）。英文邮件草稿（outreach_emails）、AI 研究记录（external_analysis_notes）、公司名称（如 Qatar Neon、Acrilmax Ecuador）、网址域名（如 naveconperu.com）和英文搜索关键词中的国家词保持不变，避免破坏对外内容与检索。
- 数据/依赖影响：hamid 库 `customers` 表 92 行、kelley 库 `customers` 表 2 行更新（`country` 与 `updated_at`），hamid 库文本字段 9 处更新，均写入 `operation_logs` 审计记录；不新增表、不迁移。修复前已创建一致性快照 `amy.db.pre-country-cn-20260820`、`hamid.db.pre-country-cn-20260820`、`kelley.db.pre-country-cn-20260820`（此前另有 `*.pre-uae-normalize-20260820` 快照）。`app.py` 的 `_COUNTRY_MAP` 与 `COUNTRY_TEXT_ALIASES` 补充 `u.a.e.` / `u.a.e` 别名，防止未来导入再次产生该变体。
- 验证：三个库 `PRAGMA integrity_check` 均返回 ok；重扫确认 `customers` 与 `import_unmatched_customers` 的 country 字段无残留英文国家名；`python3 -m py_compile app.py` 通过；映射单测覆盖 `UAE`、`United Arab Emirates`、`U.A.E.`、`u.a.e`、`U.A.E` 均归一为「阿联酋」，`阿联酋` 幂等，`Dubai` 不受影响。代码修改尚未发布到正式运行目录，需运行 `deploy/macos/publish-production.sh` 生效（数据修复已直接落在正式数据库，立即生效）。

## 2026-08-20 — 删除已冻结的 AI 背调 / 工作区问答 / 旧备份恢复等死代码

- 用户可见变化：Inbox 会话详情中移除“查看分析”按钮（原入口依赖已冻结的客户辅助分析页签）；不再提供 `/api/customers/<id>/analysis`、`/api/customers/<id>/external-analysis-notes`、`/api/customers/<id>/research` 及 generate/batch/web-logs、`/api/ai/ask`、`/api/excel/info`、`/api/calendar/subscription/rotate` 等接口，前端与对应按钮、页签、快捷键和 CSS 一并删除。客户详情仍可正常读取历史理解摘要与辅助分析数据（只读），历史数据不迁移、不删除。
- 数据/依赖影响：不新增数据库表、不执行数据迁移；`research_reports`、`external_analysis_notes`、`customer_understandings`、`ai_recommendations` 表保留，客户删除等既有级联清理不受影响。`app.py` 移除 `app.engine.generate_research_report` 导入；`db.py` 移除无调用者的 `init_db`、`seed_demo_data`、`_list_backups_legacy`、`_restore_from_backup_legacy`；`app/engine.py` 移除 `analyze_customer`、`analyze_single_intelligence`、`generate_research_report` 及仅其使用的 `SYSTEM_CONTEXT`、`PRONAMAX_CONTEXT`；`scheduler.py` 移除未调度的 `_run_web_monitor`、`_run_research_expiry_check_legacy`、`_run_research_expiry_check`。全部删除项先经脚本与人工确认无任何前端、后端或测试引用。
- 验证：`node --check app/static/app.js` 通过；`python3 -m py_compile app.py db.py app/engine.py scheduler.py` 通过；全量回归测试通过；`/api/agent/*`、`/api/backup/*`、`/api/sync*`、`/api/integrations/prospecting-lab/token`、`/share/weekly`、`/api/calendar/ical*` 等仍被测试或外部入口引用的接口均保留。

## 2026-08-20 — 客户文件预览扩展到 Word / PPT / 邮件 / 压缩包

- 用户可见变化：客户资料“文件”页新增可预览类型。Word（.docx）按段落和表格展示文本；PPT（.pptx）按页展示文字；邮件（.eml）展示发件人、收件人、日期、主题和正文；压缩包（.zip / .tar / .gz）列出内部文件名称与大小；RTF 不再显示原始标记，改为剥离控制字后的纯文本。旧版 .doc / .ppt、.rar / .7z、.msg 仍显示“暂不支持预览”并提供下载入口。文件列表点击行为同步更新：可预览类型点击打开预览页，其余类型点击直接下载。预览页视觉对齐设计系统：纸张/墨色/陶土色变量、文件类型徽标、主次按钮、斑马纹表格、窄屏换行，不支持类型页底部说明同步区分。
- 数据/依赖影响：不新增数据库表、不执行数据迁移、不新增第三方依赖；全部预览解析使用 Python 标准库（zipfile / tarfile / email / xml.etree），只提取文本与目录，不执行宏、脚本或外部程序。预览仍只展示文件开头部分（Word 前 60 个段落/表格、PPT 前 30 页、文本类前 20000 字符、压缩包前 200 项），完整内容需下载原文件。
- 验证：`python -m py_compile app.py tests/test_risk_regressions.py`、`node --check app/static/app.js` 通过；新增 docx / pptx / eml / zip / rtf 预览与不支持类型回归测试，全量 57 项测试通过；Flask 测试客户端冒烟验证各类型预览页输出、转义与新视觉样式正确。已通过 `deploy/macos/publish-production.sh` 发布正式服务，公网 `/api/network/ping` 与 `/api/auth/users` 返回 200，正式运行目录与公网静态资源均包含新预览实现。本机没有 Chrome/Chromium/Edge 可执行文件，未执行真实浏览器截图验收。

## 2026-08-19 — 客户简报支持轻量快速改级并修复窄屏元信息截断

- 用户可见变化：客户详情简报中的“等级”改为与其他事实文字一致的无边框轻量输入框；基础等级为 A / B / C / D，用户可直接输入可选的 + / - 后缀（如 A+、B-），输入后立即保存并更新当前简报。移除下拉三角，改用弱化的编辑图标提示可修改，保存中和保存后在字段旁显示状态反馈。
- 交互修复：客户详情顶部的国家、行业、归属和网站信息在窄屏下按可用宽度换行，长文本和网站入口不再被标题栏裁切。
- 数据/依赖影响：沿用现有客户 `PUT` 接口和操作日志；为旧 SQLite 客户表增加可重复执行的等级约束迁移，不新增数据库表或第三方依赖。
- 验证：`node --check app/static/app.js`、`python -m py_compile app.py db.py scheduler.py`、全量 51 项回归测试通过；正式 Hamid 数据库副本迁移后 867 个客户保持可读，`A-` 写入和重复启动通过，原有 1 条历史外键引用问题未新增；隔离浏览器验收覆盖可编辑等级输入、D+ 即时保存、无边框样式、492px 窄屏元信息换行与无横向溢出；重新发布后公网 `/api/network/ping` 返回 200，首页和带新版本号的 JS/CSS 均已返回新实现。

## 2026-08-17 — 修复客户详情首次点击无法关闭

- 用户可见变化：客户详情弹窗的关闭按钮明确作为普通按钮处理；只读周报客户详情关闭时会立即释放遮罩状态，避免手机端需要重复点击。
- 交互边界：客户资料仍有未保存修改时继续显示确认提示；仅在加载中或错误状态关闭时跳过无意义的未保存检查。
- 验证：`node --check app/static/app.js`、全量 51 项回归测试通过，正式服务已发布并返回健康状态 200。

## 2026-08-17 — 增加公司局域网只读会议入口并收紧公网共享

- 用户可见变化：公司 Wi‑Fi 内可通过生产 Mac 的局域网地址打开 `/share/weekly`，无需账号即可查看本周工作和周报客户详情；内网模式不提供新增、修改、删除等写入能力。
- 公网边界：`https://app.trosa.space` 不再接受旧的公司公网 IP 免密共享会话，公网入口继续要求成员账号和访问码；旧共享 cookie 会被清除。
- 配置/安全：生产服务设置 `CRM_BIND_HOST=0.0.0.0`，并仅以 `CRM_INTERNAL_VIEWER_CIDRS=192.168.0.0/23` 识别当前办公室局域网。请求根据真实对端地址判断，不信任 Host 或转发头；路由器不应配置端口转发。
- 数据/依赖影响：不新增数据库表、不执行数据迁移、不新增依赖；内网查看沿用现有周报只读接口与缓存禁止响应头。

## 2026-08-17 — Today 恢复客户跟进工作区

- 用户可见变化：移除 Today 顶部统计和主工作区中的“自动开发节点”分区，页面恢复为左侧今日明确跟进、右侧当前选中客户信息与记录入口；不再为该页面请求独立的自动开发节点接口。
- 数据/依赖影响：不删除或修改现有 15 / 30 / 60 天提醒数据，客户详情中的历史兼容能力保持不变；不新增数据库表或第三方依赖。发布脚本默认源目录同步更新为重命名后的 `/Users/luoxin/Desktop/Trosa`；正式服务的静态文件改为始终从运行副本绝对路径读取，LaunchAgent 的工作目录与日志目录不再依赖桌面项目文件夹名称。
- 验证：`node --check app/static/app.js`、`python -m py_compile app.py`、`zsh -n deploy/macos/publish-production.sh` 通过；新增 Today 页面结构回归测试，全量 51 项测试通过。正式服务重载后 `/api/network/ping` 返回 HTTP 200，运行目录固定为 `TradeOS/runtime`，正式首页 DOM 不再包含自动开发分区。真实浏览器截图验收因本机没有 Chrome/Chromium 可执行文件而未执行。

## 2026-08-17 — 增加公司网络只读周报入口

- 用户可见变化：新增 `/share/weekly` 会议入口。当前默认只允许公司真实公网出口 `122.233.82.41` 访问；进入后自动打开“本周工作”跨成员只读视图，不需要三位成员的个人访问码。
- 权限边界：共享会话只能读取周报汇总、周报客户索引和周报只读客户详情；写入、设置、备份、联系人导出和其他 CRM 接口仍然需要成员登录。共享会话在离开公司网络后失效，页面与 JSON 禁止缓存和搜索引擎收录。
- 配置/数据影响：不新增数据库表、不修改业务数据；可通过 `CRM_WEEKLY_VIEWER_IPS` 使用逗号分隔的 IPv4/IPv6 或 CIDR 覆盖默认公司出口地址。没有加入 VPN 出口 IP。正式服务仍需在发布时将该环境变量写入生产环境文件。
- 验证：新增未登录共享入口的 IP 白名单、只读接口和写入拒绝回归测试；`node --check app/static/app.js`、`python3 -m py_compile app.py` 通过。当前尚未发布到正式 Cloudflare Tunnel，发布前需在真实公司网络和非公司网络各验收一次。

## 2026-08-16 — 收敛 Customer Memory 核心路径并冻结非核心入口

- 用户可见变化：Today 将人工跟进与保留的 15 / 30 / 60 天自动开发节点分开显示；自动节点单独统计、标注逾期，并可直接打开客户记录联系。客户详情简报补充客户等级、当前上下文、关键需求和关联文件数量；Today 各区块独立加载，失败时显示明确错误和重试入口，不再把加载失败误显示为“没有待办”。
- 产品边界：移除客户详情中的 AI 客户研究/推荐入口、Inbox 的 AI 建议入口和官网监控入口；冻结官网监控、研究过期检查及客户理解自动写入。保留官网事实识别、邮箱可发送性验证、15 / 30 / 60 天自动开发机制、本周工作周报，以及用户主动触发的沟通记录 AI 整理按钮。历史研究、监控和理解数据未删除，继续作为兼容性只读数据保留。
- 数据/依赖影响：不新增数据库表、不执行数据迁移、不新增第三方依赖；客户详情的下一次跟进日期改为从未完成提醒汇总，文件数量从现有客户文件表读取。自动开发节点仍沿用现有 `outreach_*` 提醒记录，完成任一节点时继续关闭同客户后续节点。
- 验证：`.venv-mac/bin/python -m unittest discover -s tests -p 'test_*.py' -v`（49 项通过）；`.venv-mac/bin/python -m py_compile app.py scheduler.py`、`node --check app/static/app.js` 通过；使用隔离数据库启动 `127.0.0.1:8091` 完成 Today 自动节点、客户详情、AI 入口冻结和官网识别入口的真实浏览器验收，未修改正式运行目录数据。

## 2026-08-16 — 增加每日自动本机历史快照

- 用户可见变化：正式服务每天 02:15（Asia/Shanghai）自动创建本机数据库恢复点，即使当天没有客户修改也会保留一份历史版本；系统仍保持单一主数据库，不自动切换或双写。
- 数据/依赖影响：快照包含 `system.db`、Hamid/Amy/Kelley 三个用户数据库、客户附件、来源清单和 SHA-256 校验；本机快照继续按既有 90 天策略清理，新增定时快照会参与常规保留，不新增第三方依赖或数据库表。
- 恢复与安全：定时快照与恢复操作串行，恢复前仍会先保存当前版本；任务错过后应用重启可在 7 天内补执行，失败只报警并保留主库运行，不自动切库。
- 验证：新增定时快照和调度器注册回归测试；`python3 -m py_compile db.py scheduler.py app.py`、`.venv-mac/bin/python -m unittest discover -s tests -p 'test_*.py' -v`（47 项通过）；正式服务已重新发布并健康检查返回 HTTP 200，手动恢复点校验通过。

## 2026-08-14 — 对齐下一步字段样式并优化展开过渡

- 用户可见变化：下一步动作与日期的标签、字号、颜色、输入高度和圆角统一到 Today 事实区与现行表单规范；日期输入保留清晰的系统日期格式。
- 交互修复：加号展开改为 220ms 的轻量透明度、位移和高度过渡；仅在右侧客户焦点区需要时做小幅跟随，保持客户名称和上下文可见，不推动整页跳动。
- 数据/依赖影响：仅调整 Today 记录区的字段样式、展开状态和局部滚动逻辑，不修改 SQLite、业务接口或第三方依赖。
- 验证：在 1280×800 与 1440×900 隔离页面中确认字段可见、输入高度为 42px、展开状态不横向溢出；`node --check app/static/app.js`、`python3 -m py_compile app.py app/engine.py` 通过。

## 2026-08-14 — 将 Today 记录入口收成沟通记录

- 用户可见变化：Today 记录区改名为“沟通记录”，默认只保留一个输入框；下一步动作与日期收进标题旁的加号按钮，点击后按需展开，减少重复说明和操作层级。
- 交互修复：移除沟通记录入口和下一步展开时的平滑滚动与延迟聚焦，改为稳定的就地聚焦，避免页面跳动和展开动画割裂。
- 数据/依赖影响：仅调整现有 Today 记录区的文案、HTML 结构与 CSS；沿用原有沟通记录、待办接口，不修改 SQLite、业务数据或第三方依赖。
- 验证：`node --check app/static/app.js`、`python3 -m py_compile app.py app/engine.py`；使用隔离数据库和无头 Chromium 检查收起/展开、键盘聚焦和窄视口布局。

## 2026-08-14 — 精简 Today 跟进记录区的视觉层级

- 用户可见变化：记录区移除重复说明文字，客户反馈成为唯一主输入；“安排下一步”改为轻量次操作，展开后的动作与日期使用简洁分隔布局，减少卡片、边框和视觉噪音。
- 数据/依赖影响：仅调整 Today 宽屏记录区的文案、HTML 结构与 CSS，不修改 SQLite、业务接口或第三方依赖。
- 验证：在 1440×1000 视口的真实无头 Chromium 中核对收起/展开两种状态，确认输入框、动作日期与提交按钮布局完整；`node --check app/static/app.js` 通过。

## 2026-08-14 — 修复 Today 下一步区域被面板裁切

- 用户可见变化：Today 宽屏客户焦点区展开“安排下一步”后恢复纵向滚动，动作和日期输入框、提交按钮不会再被右侧圆角容器截断；聚焦下一步输入框时会自动滚动到可视区域。
- 数据/依赖影响：仅调整现有 Today 工作区 CSS，不修改 SQLite、业务接口或第三方依赖。
- 验证：在 1230×600 视口的隔离数据库无头 Chromium 中确认展开后输入框完整可见；`node --check app/static/app.js`、`python3 -m py_compile app.py app/engine.py` 通过。

## 2026-08-14 — 分开记录客户反馈与下一步，并同步 Today 待办

- 用户可见变化：Today 客户档案的“记录跟进”改为单独填写“客户反馈”；只有点击“安排下一步”后才显示动作和日期字段，避免把客户事实与销售动作混在同一个输入框。提交后继续完成当前到期待办，并可按填写的日期创建新的明确待办。
- 交互修复：从客户详情中完成待办或保存沟通记录后，底层 Today 工作台会立即刷新，关闭详情时不再看到已经完成的今日事项。
- 数据/依赖影响：沿用现有 `follow_up_logs`、`reminders` 和 `/api/reminders/<id>`、`/api/customers/<id>/follow_history` 接口；不新增数据库表、迁移或第三方依赖。
- 验证：`node --check app/static/app.js`、`python3 -m py_compile app.py app/engine.py`、`.venv-mac/bin/python -m unittest discover -s tests -p 'test_*.py' -v`（45 项通过）；使用隔离数据库副本和已缓存的无头 Chromium 完成真实 DOM 验收：客户反馈字段与旧输入框状态正确、下一步展开后动作/日期可保存、Today 待办在宽屏记录区和客户详情记录后均从列表移除。系统没有可执行的 Google Chrome，因此未使用 Chrome 可视化启动器。

## 2026-08-12 — 修复客户详情打开后的内容穿透

- 用户可见变化：客户详情工作区改为稳定的不透明纸张层，客户列表、背景纹理和页面内容不再从客户资料、沟通时间线和状态区域透出；保留弹层外部的遮罩层级，移动端和窄窗口仍可滚动查看详情。
- 数据/依赖影响：不修改 SQLite、业务数据、接口或第三方依赖；仅调整客户详情弹层的局部 CSS，并更新静态样式版本参数。
- 验证：`node --check app/static/app.js`、`python3 -m py_compile app.py db.py`；通过真实页面计算样式确认修复范围只作用于 `#customerEditModal` 工作区，完整浏览器截图验收受本地账号访问码限制，未宣称已完成截图验收。

## 2026-08-11 — 收紧客户简报的文字密度

- 用户可见变化：客户简报移除每个区块下方的解释句、顶部来源图例、状态字段重复的“来源：”说明和信息缺口的展开描述；保留标题、事实值、日期以及沟通记录中必要的紧凑来源标记。右侧执行动作移除说明段落，信息缺口详情改为悬停/辅助功能标签可读，页面默认更适合快速扫描。
- 数据/依赖影响：不修改 SQLite 表、业务数据、接口或第三方依赖；来源、缺口详情和状态来源仍由接口返回并保留，仅减少默认可见文字。
- 验证：`node --check app/static/app.js`、`.venv-mac/bin/python -m unittest tests.test_risk_regressions -v`、`python3 -m py_compile app.py`；浏览器实测仍受当前环境无 Chrome 可执行文件和本地端口监听限制，未宣称截图验收通过。

## 2026-08-11 — 客户详情改为事实优先的客户简报

- 用户可见变化：客户详情页在页签前新增“客户简报”，按客户身份、沟通对象、最近 3 条已发生事实、当前状态和信息缺口分层展示；每条事实保留来源与日期，当前等待、下一步和关系状态分别标注来源。右侧区域只保留记录沟通、安排下一步等执行动作，避免与简报重复。时间线首屏只读取 3 条，更早记录继续按页加载；联系人、文件、全部待办和辅助分析仍按需打开。
- 数据/依赖影响：不修改 SQLite 表、业务数据或第三方依赖；客户摘要接口新增最近事实、当前状态、下一步来源、联系人数量和基础资料缺口字段。客户列表的“资料质量”检查复用同一套本地检查，补充缺联系人、缺行业/领域、缺网站、网站格式待确认、缺基本简介、缺国家/地区和疑似重复资料。缺口只代表系统可核对的信息缺失，不自动生成待办或商业判断。
- 验证：`.venv-mac/bin/python -m unittest tests.test_risk_regressions -v` 36 项通过；`node --check app/static/app.js`、`python3 -m py_compile app.py` 通过；已尝试真实浏览器验收，但当前环境没有 Chrome 可执行文件，隔离测试服务无法保持本地端口监听，因此未宣称浏览器截图验收通过。

## 2026-08-10 — 修复账号页重复渲染与周报入口失效

- 用户可见变化：账号选择页在登录态过期、刷新或快速切换账号时，只保留一张“本周工作”卡和每个成员各一张账号卡；点击本周工作或账号后，过期请求不会再把登录页重新盖回或追加重复卡片。
- 数据/依赖影响：不修改 SQLite 数据、账号信息、接口或第三方依赖；仅让前端账号列表请求可取消，并限制为最新一次打开账号页才可渲染。
- 验证：`node --check app/static/app.js`、登录页并发响应回归和 `.venv-mac/bin/python -m unittest tests.test_risk_regressions -v`（35 项）通过。受隔离环境权限限制，无法启动本机页面完成浏览器实测。

## 2026-08-11 — 周报成员头部不再显示客户数量

- 用户可见变化：周报页面每位成员名字旁原先显示“N 个客户”或“N / M 个客户”的统计，已从成员头部移除；名字右侧仅保留“更新中 / 更新失败”等状态提示。周报正文内容不变。
- 数据/依赖影响：不修改 SQLite 表或业务数据，不调整接口；仅移除前端 `renderWeeklyMember` 中的统计文案渲染。
- 验证：`node --check app/static/app.js` 通过，`totalCustomers`、`countLabel` 不再被引用。

## 2026-08-10 — 恢复“今天”客户卡片拖动排序

- 用户可见变化：今天页面的客户卡片再次可以直接拖动以调整当天关注顺序；点击客户、打开客户资料、勾选批量操作和触屏长按多选保持原有行为。
- 数据/依赖影响：复用现有 `/api/reminders/today/order` 与 `reminders.manual_order`，仅保存当前用户的今日/逾期待办显示顺序；不修改客户资料、待办内容、日期、数据库结构或第三方依赖。
- 验证：JavaScript 语法检查通过；今日待办排序接口的完整列表校验与撤销回归测试通过。

## 2026-08-10 — 客户时间线星标提供持续点亮反馈

- 用户可见变化：客户沟通记录加入本周工作后，右侧星标会变成陶土色实底、暖白色星形，并带有克制的暖色描边与阴影；鼠标移开后该状态仍持续可见，旁边的“已纳入本周”文字继续提供明确说明。
- 交互修复：星标写入成功后立即根据服务端返回的状态点亮或熄灭，再在后台重新读取时间线校准；网络较慢时不会再等到第二次读取结束才显示结果。
- 数据/依赖影响：不修改 SQLite 表、业务数据或接口；仅调整既有星标控件的视觉状态与过渡，并更新静态样式版本参数以确保刷新后加载新版外观。
- 验证：`node --check app/static/app.js` 通过；`.venv-mac/bin/python -m unittest tests.test_risk_regressions -v` 35 项通过，其中包括客户时间线对沟通记录与开发信返回 `is_reported` 状态的回归覆盖。

# Trade OS 功能修改日志

## 2026-08-13 — Prospecting Lab 自动集成

- 用户可见变化：Prospecting Lab 在 Run 开始、研究/联系人完成、Gmail 发送和回信入账后自动串联 Trosa，避免重复开发。
- 数据/依赖影响：`system.db/app_settings` 保存集成令牌 SHA-256 摘要；不新增第三方依赖。
- 安全边界：令牌仅允许 Hamid 客户索引、新建客户、联系人/分析备注/已确认开发信写入；禁止删除、备份恢复、待办和跨用户访问。
- 验证：Trosa 风险回归 37 项通过；Prospecting Lab 回归 54 项通过；正式 Bearer API 返回 453 个客户，排除快照刷新成 453 条。

本文件记录用户可感知的功能变更、数据迁移和重要安全修复。日期按 Asia/Shanghai 记录。

## 2026-08-10 — 屏幕熄灭后保持服务连接并在唤醒后自动恢复

- 用户可见变化：作为正式主机的 Mac 现在允许显示器照常熄灭，但会阻止空闲系统睡眠，Trade OS 与 Cloudflare Tunnel 不再因闲置休眠而离线。浏览器从后台或系统唤醒后会主动检查连接；恢复成功会刷新当前非编辑页面，打开中的编辑弹窗不会被刷新覆盖。
- 部署影响：正式启动脚本改由 macOS 自带 `caffeinate -i` 守护，不新增第三方依赖，也不读取或修改 SQLite 数据。健康检查频率从 5 分钟缩短至 1 分钟，检测到服务或 Tunnel 异常时仍只重启对应进程。
- 验证：`zsh -n`（正式启动、发布、健康安装与自检脚本）、`plutil -lint`（健康 LaunchAgent）和 `node --check app/static/app.js` 通过；已发布到正式运行目录。本机 `/api/network/ping` 与公网 Tunnel 健康接口均返回 HTTP 200；已安装的健康 LaunchAgent 显示 60 秒执行间隔，正式应用服务处于运行状态。

## 2026-08-10 — 发布脚本保留正式运行环境

- 用户可见变化：无直接界面变化；发布当前修复到正式运行副本时，不再把开发用 `.venv` 同步到生产 `runtime/`，避免无关环境文件覆盖或膨胀正式运行目录。
- 数据/依赖影响：发布仍排除唯一业务数据目录、私密设置、日志和正式 `.venv-mac`；不修改 SQLite 数据或生产依赖。
- 验证：核对正式进程正在使用 `~/Library/Application Support/TradeOS/runtime/serve.py`，其运行副本此前落后工作区；发布脚本现明确排除两套开发/正式虚拟环境。已按既有流程发布，正式运行副本与工作区 `app.py` 一致，`/api/network/ping`、`/api/auth/users` 正常，受保护的 `/api/extension/match` 返回 401（路由已上线）。

## 2026-08-10 — 自动归属改为可解释的确认建议

- 用户可见变化：扩展不再因“唯一匹配”直接选中客户和联系人进入保存页。邮箱/手机号精确命中、公司域名候选和显示名称线索都会以高/中/低可信建议呈现，必须由用户点击确认或搜索客户后继续；因此历史 CRM 中的错误邮箱归属不能再静默带入新沟通。确认后仍可随时修改客户或联系人。
- 数据/依赖影响：扩展版本升级为 0.5.0；匹配接口为精确联系人附加高可信标记，不新增数据库表、依赖或自动写入行为。
- 验证：真实 DOM 回归确认“错误的精确命中只作为建议 → 搜索正确客户 → 选择正确联系人 → 保存正确编号”；既有读取、去重、诊断与后端归属保护测试继续通过。

## 2026-08-10 — 等待动态邮箱与 WhatsApp 消息区域挂载

- 用户可见变化：侧栏首次读取成功但消息数为 0 时，会间隔约半秒自动再读取最多两次；动态加载的邮件线程和 WhatsApp 单聊不再因侧栏打开时机过早被立即显示为“没有新增内容”。通信失败、未授权页面等明确错误仍会直接显示，不会被无意义重试掩盖。
- 数据/依赖影响：扩展版本升级为 0.4.4；仅增加当前页面的短时只读重试，不修改数据库、业务数据或依赖。
- 验证：新增“首次空读取 → 页面挂载消息 → 自动重试成功”真实 DOM 回归；既有消息提取、frame 恢复、归属修改、写入去重、匿名诊断和后端 API 测试继续通过。

## 2026-08-10 — 扩展读取器兼容深层页面结构

- 用户可见变化：网易邮箱正文处于可访问的嵌套 iframe 或开放 Shadow DOM 时，扩展会继续向内读取；WhatsApp 即使不再提供旧的 `msg-container` 标记，也可从真实消息 `data-id` 识别单聊消息与手机号。自动提取版本分别更新为网易 0.5、WhatsApp 0.3。
- 数据/依赖影响：扩展版本升级为 0.4.3；仅扩展内容脚本的只读 DOM 遍历范围扩大到当前页面可访问区域，不读取私有数据库、网络请求或其他标签页，不新增数据库表与依赖。
- 验证：新增网易开放 Shadow DOM 邮件、WhatsApp 仅有 `data-id` 消息容器的真实 DOM 回归；既有 iframe、消息去重、中文自动恢复、人工归属与后端写入保护继续通过。

## 2026-08-10 — 修复 WhatsApp 重复 DOM 与中文连接错误恢复

- 用户可见变化：WhatsApp 同一条消息即使同时带有多个页面标记，也只会显示和写入一次，不再因重复指纹导致保存失败。扩展也能识别中文 Chromium/Edge 的“无法建立连接、接收端不存在”错误，自动加载内容脚本后重试。
- 数据/依赖影响：扩展版本升级为 0.4.2；后端在写入前再按消息指纹去重，保留原始采集载荷但只创建一条业务沟通及来源指纹记录，不新增数据表或依赖。
- 验证：新增同一采集载荷内重复消息去重、中文接收端错误自动恢复回归；现有邮箱 iframe、WhatsApp 手机号、归属改选、匿名诊断、写入归属校验与接口去重测试继续通过。

## 2026-08-10 — 沟通采集加入当前页验收与归属写入保护

- 用户可见变化：扩展侧栏右上角新增“✓ 当前页验收”。它检查当前页面是否在授权范围、每个可访问页面区域能否与内容脚本通信、是否触发自动注入恢复、是否已取得消息，以及 Trade OS 登录状态；可复制不含正文、邮箱或手机号的匿名诊断报告。归属修改继续可用，保存时若联系人不属于当前客户会明确阻止写入。
- 数据/依赖影响：扩展版本升级为 0.4.1；浏览器端只在内存中生成匿名诊断，不新增网络上传、数据库表或第三方依赖。后端写入接口新增客户—联系人归属一致性校验，保护既有客户数据。
- 验证：新增 frame 诊断、自动恢复计数、匿名报告与“跨客户联系人拒绝写入”回归；真实 DOM 归属改选→保存、网易 iframe、WhatsApp JID 手机号、内容脚本恢复、匹配/去重 API、Manifest 与语法检查均通过。

## 2026-08-10 — 上线自检与 Cloudflare Tunnel 自动恢复

- 用户可见变化：新增可双击运行的 `Trade OS 上线自检与修复.command`；它会检查本机服务和公网健康接口，在出现 Cloudflare 1033、Tunnel 失联或本机服务未监听时自动重启对应服务，并在恢复后给出结果。生产 Mac 登录后会立即执行一次相同检查，之后每 5 分钟运行一次。
- 数据/依赖影响：新增用户级 `com.tradeos.health` LaunchAgent、健康检查与安装脚本；只读取生产环境中的公网地址，不读取或修改 SQLite 业务数据、Tunnel 凭据和私密设置，不新增第三方依赖。异常与恢复信息写入 `~/Library/Application Support/TradeOS/logs/health-monitor.log`。
- 验证：`zsh -n` 与 `plutil -lint` 通过；`com.tradeos.health` 已加载并显示 300 秒执行间隔、首次运行退出码为 0；已安装的一键脚本实测输出“Trade OS 与公网 Tunnel 均正常”，公网 `/api/network/ping` 返回 HTTP 200，未触发额外重启。

## 2026-08-10 — 本周工作完整展示当周沟通内容

- 用户可见变化：本周工作客户卡片中的“实际工作、结果、下一步”不再按行数截断，已纳入本周的沟通内容完整显示；页面仍只读取当前所选周内已纳入本周工作的记录，较早沟通继续通过“查看详情”进入客户阅读区查看。
- 数据/依赖影响：不修改 SQLite 表、业务数据、接口筛选条件或第三方依赖；仅调整前端文字溢出样式并更新静态样式版本参数。
- 验证：`node --check app/static/app.js`、`python3 -m py_compile app.py` 和 `.venv-mac/bin/python -m unittest tests.test_risk_regressions -v`（35 项）通过；本地页面已加载新版样式，核对周报正文计算样式为 `display: block`、`overflow: visible`、`-webkit-line-clamp: unset`。

## 2026-08-09 — 客户时间线明确显示“纳入本周工作”状态

- 用户可见变化：在客户详情的沟通记录和开发信时间线中，已纳入本周工作的条目会持续显示陶土色星标与“已纳入本周”文字；星标按钮增加清楚的加入/移除说明和键盘焦点。用户从记录中加入本周工作后，关闭并在短时间内重新打开客户页，状态仍会保持最新。
- 审查修复：登录页在局域网地址接口返回不完整信息时，改为显示当前访问地址，不再渲染 `http://undefined:undefined`。
- 数据/依赖影响：不修改 SQLite 表或业务数据；客户时间线读取接口补充返回既有的 `is_reported` 状态，前端缓存仅保存已读取的最新时间线。
- 验证：新增客户时间线回归测试，覆盖已纳入的沟通记录和未纳入的开发信；将执行 Python/JavaScript 语法检查与完整回归测试。

## 2026-08-09 — 周报改为同步沟通记录中的既有高光

- 用户可见变化：周报不再提供独立高光工具，也不再创建浏览器本地周报标记；“实际工作、结果、下一步”和只读客户历史直接呈现原客户沟通记录中已经保存的黄色、绿色和粉色高光。周报页面只读，修改高光仍回到客户沟通记录中完成。
- 数据/依赖影响：不修改数据库结构或业务数据；此前 `tradeos.weekly-highlight.v1:*` 浏览器本地值不再读取，不会覆盖数据库事实，也不会自动删除。
- 验证：新增周报接口回归断言，确认三种 `<mark>` 高光从已上报沟通记录原样进入周报摘要；JavaScript/Python 语法检查及周报回归测试通过。本地浏览器使用真实周报数据确认既有粉色高光直接显示、周报没有独立可编辑目标，选中文字后标色菜单保持隐藏。

## 2026-08-09 — 沟通采集支持人工改选客户与联系人

- 用户可见变化：扩展即使得到唯一自动匹配，也会在归属卡片提供“修改客户或联系人”；可搜索全部客户，选中客户后重新读取该客户联系人并选择正确联系人，也可暂不关联或新建联系人。域名匹配只推荐公司，不再顺带锁定某个联系人；每条智能候选显示邮箱、手机号、域名或名称判断依据，页面显示名称仅作为需要人工核对的弱线索。
- 数据/依赖影响：扩展版本升级为 0.4.0；匹配接口新增 `exact_reason`、`name_candidates` 和候选置信等级，不修改 SQLite 表或既有客户/联系人数据，不新增第三方运行依赖。最终写入继续以用户在保存页确认的 `customer_id` 与 `contact_id` 为准。
- 验证：新增真实 DOM 交互回归，覆盖“错误自动客户/联系人 → 搜索正确客户 → 读取并选择正确联系人 → 保存正确编号”全流程；名称弱匹配、精确匹配依据、域名候选、身份冲突、读取恢复和扩展写入去重测试全部通过。

## 2026-08-09 — 自动恢复邮箱与 WhatsApp 内容脚本连接

- 用户可见变化：侧栏遇到 `Could not establish connection. Receiving end does not exist.` 时，不再直接显示“不支持当前页面”；扩展会先校验当前标签页属于网易邮箱或 WhatsApp，再向缺少接收端的当前 frame 主动加载采集脚本并重试。扩展重载后仍开着的旧标签页，以及稍后创建的邮件 iframe，均可通过“重新读取”自动恢复。
- 数据/依赖影响：扩展版本升级为 0.3.1，复用既有 `scripting` 权限；注入范围严格限制在 Manifest 已授权的网易邮箱和 WhatsApp 页面，不修改数据库、业务数据或第三方依赖。内容脚本增加幂等安装保护，重复重试不会注册多个消息监听器。
- 验证：新增“接收端不存在 → 主动注入 → 重试成功”、重复注入监听器去重和非支持站点拒绝测试；网易 iframe、WhatsApp 手机号、内容脚本消息入口、frame 合并、夹具和扩展 API 回归继续通过。

## 2026-08-09 — 性能优先模式与首屏数据收敛

- 用户可见变化：设置新增“界面性能”三档选择：自动、性能优先、完整视觉。自动模式会在首次登录后执行一次不足一秒的本地帧率测试，结合设备状态决定是否降低玻璃、重阴影、列表动画、页面过渡与平滑滚动；测试结果与选择按账号保存，且不会上传页面内容或客户数据。客户工作区可先显示当前会话缓存的客户身份；周报每位成员首屏显示最多 10 位客户，并可继续显示更多客户。
- 性能与接口变化：`/api/customers/<id>/summary` 只返回最近一项下一步；联系人、附件上传与附件恢复接口返回可直接渲染的新记录，前端保存后局部更新。成员级周报接口支持 `limit`/`offset`，缓存仍按成员与周分离。滚动工具栏、回复弹窗和文字选择的高频监听统一为每帧最多一次更新；页面隐藏时停止 Inbox 轮询，周报只读详情会取消被快速切换替代的旧请求。
- 数据/依赖影响：个人偏好 JSON 新增 `interface_performance` 与 `performance_probe`；用户数据库启动时新增联系人邮箱/归属与活跃客户更新时间索引。无需迁移既有业务记录，不新增第三方依赖。
- 验证：`node --check app/static/app.js`、`python3 -m py_compile app.py db.py` 通过；回归测试新增轻量客户摘要、联系人局部写入和性能偏好覆盖，`tests.test_risk_regressions` 共 34 项通过。

## 2026-08-09 — 周报高亮改为选区浮动工具（同日撤回）

- 用户可见变化：移除周报页常驻高亮工具条；只有选中“实际工作、结果、已确认下一步”中的文字时，才在选区旁显示黄、绿、粉和清除工具，完成标记或取消选区后自动隐藏。客户详情改为仅由“查看详情”按钮打开，正文区域专门用于阅读和选字；高亮继续保存在当前浏览器对应周次、成员、客户和字段的阅读状态中。
- 数据/依赖影响：仅调整前端交互和页面结构，不修改数据库、业务数据或第三方依赖；浏览器本地高亮存储格式保持不变。
- 验证：JavaScript/Python 语法检查通过；本地浏览器确认工具默认隐藏、周报正文点击事件已与打开客户详情隔离，并刷新前端缓存版本。
- 后续调整：该方案把周报阅读状态与客户沟通事实分成了两套来源，已由“周报改为同步沟通记录中的既有高光”取代。

## 2026-08-09 — 客户详情改为分层加载并支持局部更新

- 用户可见变化：客户详情首屏只加载身份摘要和最近 20 条沟通；时间线可继续读取更早记录，长正文按需展开。联系人、文件、全部待办和辅助分析改为打开对应页签后加载；右侧行动区保留当前等待、明确下一步和其他未完成待办，自动 15/30/60 天节点单独折叠。
- 性能与接口变化：新增 `GET /api/customers/<id>/tasks` 与 `GET /api/customers/<id>/analysis`；summary 返回最近真实沟通、主要联系人摘要、联系人数量、明确待办和自动节点。保存沟通、联系人、文件、待办和资料后，前端只更新对应区域，不再重新下载完整客户对象；沟通写入响应直接返回新记录、最近联系日期、当前等待、完成待办和下一步。
- 数据/依赖影响：不修改 SQLite 表、业务数据或第三方依赖；继续使用本地 SQLite 作为唯一业务数据源，文件上传/删除仍沿用既有来源、软删除和恢复链路。
- 验证：`py_compile`、`node --check`、客户分拆接口冒烟检查通过；现有 Python 回归测试 36 项全部通过。

## 2026-08-09 — 周报三级加载与摘要查询优化

- 用户可见变化：本周工作页先显示日期、三位成员栏和独立加载状态；Hamid、Amy、Kelley 并行加载，单个成员失败时保留其他成员并显示错误与重试。成员数据加载完成后立即呈现，点击客户才加载完整沟通历史；页面改为连续列表、细分隔线和明确的周切换控件，正文统一到 14–16px 左右。
- 性能与接口变化：成员级周报只查询周范围内已上报记录，按客户聚合活动次数、实际工作、结果和已确认下一步，不再传输首页用不到的逐条历史；旧聚合接口保留并行兼容。服务端缓存延长为 15 分钟，过期后先返回最近缓存并后台刷新；前端使用最近一次本地缓存静默刷新。新增周报查询索引，写入成功后按成员失效缓存。
- 数据/依赖影响：新增 `follow_up_logs(is_reported, follow_date, is_deleted, customer_id)` 与 `outreach_emails(is_reported, sent_date, customer_id)` SQLite 索引；不修改业务数据，不新增第三方依赖。
- 验证：`.venv-mac/bin/python -m unittest tests.test_risk_regressions -v` 通过（33 项）；`node --check app/static/app.js`、`python3 -m py_compile app.py db.py` 通过；新增回归测试确认未上报记录不会进入周报摘要且首页不返回 `week_entries`。

## 2026-08-09 — 收敛本周工作页标题、字体与周切换控件

- 用户可见变化：移除顶部重复说明“从三位成员真实记录中选择的客户进展”，移除成员标题下重复的“本周工作”；上一周/下一周改为带明确文字和图标的控件，修复空圆按钮问题；重新调整标题、成员名、客户名和正文的字号层级。
- 数据/依赖影响：仅调整本周工作页 HTML/CSS/展示文案，不修改数据库、业务数据或第三方依赖。
- 验证：`node --check app/static/app.js` 与 Python 语法检查通过。

## 2026-08-09 — 提升只读客户工作区可读性并补齐高光入口

- 用户可见变化：只读客户工作区改为更实的纸张层，降低背景纹理、玻璃叠加和内容透视，长文本更容易阅读；标题区域增加“选中文字后可标记”提示，周报客户详情时间线支持打开现有高亮并在当前查看中临时添加高亮。
- 数据/依赖影响：不修改数据库、业务数据或第三方依赖；只读工作区中的临时标记不会写回数据，正式记录高亮仍在可编辑客户界面保存。
- 验证：`node --check app/static/app.js` 通过；高亮入口使用既有黄色、绿色、粉色工具菜单与视觉系统样式。

## 2026-08-09 — 修复成员级周报接口参数错误

- 用户可见变化：修复打开成员周报时出现 `weekly_summary() got an unexpected keyword argument 'user'` 的服务器错误；Hamid、Amy、Kelley 的独立周报接口现在可以正常接收成员参数。
- 数据/依赖影响：仅修正 Flask 路由参数绑定，不修改数据库、业务数据或依赖。
- 验证：Python/JavaScript 语法检查及周报权限、客户详情回归测试通过；已重新发布并完成公网健康检查。

## 2026-08-09 — 周报分成员加载与客户详情渐进接口第一阶段

- 用户可见变化：打开“本周工作”时先显示日期、三位成员栏和各自加载状态；Hamid、Amy、Kelley 分别加载，某位成员失败时显示错误与重试，不再把失败误显示为空内容。周报首页只保留客户、本周记录数量、实际工作、结果和已记录下一步，历史沟通在点击客户后加载。
- 性能与接口变化：新增 `/api/weekly-summary/<user>`、`/api/customers/<id>/summary` 和 `/api/customers/<id>/timeline`；周报不再在首页逐客户查询最近历史，客户基础资料与时间线支持轻量分页读取。原聚合周报接口继续保留兼容。
- 数据/依赖影响：不修改 SQLite 表、业务数据或第三方依赖；仅新增读取接口和前端加载状态。
- 验证：`py_compile`、`node --check` 与现有回归测试通过；未登录接口仍返回 401，成员级接口拒绝未知用户。

## 2026-08-09 — 修复客户附件备份、恢复删除与批量上传限额

- 用户可见变化：数据库备份现在同时保存 `uploads/customer_files/` 附件本体及 SHA-256 清单；恢复会校验并还原附件目录。删除附件改为移入可恢复区，客户文件页提供“撤销”入口，并新增恢复接口；批量上传改为每个文件不超过 25MB、每次最多 10 个且总量不超过 250MB，界面与服务端口径一致；长文件名可通过悬停查看完整名称。
- 数据/依赖影响：备份目录新增 `uploads/customer_files/` 与 `manifest.json` 的 `attachments` 清单；删除文件暂存于 `uploads/customer_files/.trash/<客户ID>/`，不新增 SQLite 表、不新增第三方依赖。旧版不含附件清单的备份仍可恢复数据库，保留当前附件避免误删。
- 验证：附件删除→撤销、备份后移除本体→恢复下载、两份各 13MB 文件批量上传均通过；全套回归测试 33 项通过，Python/JavaScript 语法检查通过。

## 2026-08-09 — 新增网易邮箱与 WhatsApp Web 沟通采集扩展

- 用户可见变化：新增 `browser-extension/` Manifest V3 扩展，可在网易个人/企业邮箱和 WhatsApp Web 当前会话中按用户点击提取已加载消息，识别邮箱或手机号，匹配客户/联系人，编辑预览后确认写入；支持继续加载提示、未匹配搜索、待归属 Inbox 暂存、重复消息过滤、保存成功撤销和打开客户。
- 数据/依赖影响：Trade OS 新增 `communication_sources` 与 `communication_source_items` 表，分别保存原始提取载荷、清理载荷、来源 URL、适配器版本、告警和消息指纹；新增扩展专用匹配/写入 API，不新增第三方运行依赖，不访问 WhatsApp 私有数据库或网络请求。
- 验证：扩展通过 JSON/JavaScript 语法检查，适配器夹具覆盖网易三种结构、WhatsApp 单聊/无号码/引用/媒体/失效结构；后端通过 Python 编译与扩展 API 回归测试。

## 2026-08-09 — 优化扩展邮箱归属与沟通整理

- 用户可见变化：网易邮箱现在优先使用邮件节点中的真实发件邮箱，避免把显示名称误当成公司或联系人；当精确联系人不存在时，会用邮箱域名匹配已有客户网站、联系人域名或客户名称线索并标记为“域名候选”，仍需用户确认。采集预览新增“AI 帮我整理这段沟通”，复用 Trade OS 现有沟通整理接口，将摘要和关键需求填入可编辑字段；原文、原始载荷和清理正文继续分开保存。
- 数据/依赖影响：新增扩展匹配返回的域名候选字段；联系人创建接口补充返回新联系人编号；不新增依赖和数据库表。
- 验证：补充域名候选与扩展 API 去重测试，Python/JavaScript/Manifest 检查通过。

## 2026-08-09 — 修复网易企业邮箱实际子域名识别

- 用户可见变化：扩展现在支持网易企业邮箱实际使用的 `mailh.qiye.163.com` 等 `*.qiye.163.com` 子域名；企业邮箱页面不再被误报为“不支持当前页面”。
- 数据/依赖影响：仅调整浏览器适配器的页面识别规则，不修改客户数据、数据库结构或依赖。
- 验证：企业邮箱子域名匹配回归检查、JavaScript 语法检查通过。

## 2026-08-09 — 修复企业邮箱导航卡片误当正文和错误客户自动归属

- 用户可见变化：网易适配器不再使用过宽的通用 `message`/`body` 类选择器，并明确排除“添加邮箱、高效管理、工作邮箱”等页面导航卡片；邮件正文必须具备消息标识，或同时具备地址/邮件头和时间证据。若精确联系人邮箱归属与域名识别出的另一客户冲突，扩展不再自动选中，将同时展示现有归属和域名候选供用户确认。
- 数据/依赖影响：不修改业务数据、数据库结构或依赖；已有错误联系人归属保持原样，避免扩展在未经确认时改写联系人。
- 验证：新增导航噪声排除检查和“2M Graphics 精确联系人 / Gold Coast Plastics 域名候选”冲突回归测试。

## 2026-08-09 — 跨 iframe 采集网易邮件并从 WhatsApp 会话标识提取手机号

- 用户可见变化：扩展侧栏现在检查并聚合当前标签页所有可访问 frame，网易企业邮箱正文位于独立 iframe 时也能返回；WhatsApp 除可见标题外，还会从当前消息节点的 `@c.us` / `@s.whatsapp.net` 会话标识和 wa.me 链接中提取带国家码手机号。保存的联系人名称仍来自可见名称，手机号来源单独标记。
- 数据/依赖影响：扩展新增 `webNavigation` 权限，仅用于枚举当前激活标签页的 frame；不扫描其他标签页或后台会话，不读取 WhatsApp 内部数据库和网络请求，不修改数据库结构。
- 验证：Manifest、JavaScript、适配器夹具和扩展 API 回归测试通过。

## 2026-08-09 — 修复内联邮件 frame 注入并建立扩展运行时验收

- 用户可见变化：网易企业邮箱使用 `about:blank`、`srcdoc` 或其他继承父页面来源的内联 frame 时，内容脚本现在会随父页面注入，并通过父页面 referrer 识别网易平台；侧栏把顶层识别信息与正文 frame 消息合并。WhatsApp JID 手机号提取保持独立运行，不再受网易 frame 聚合影响。
- 数据/依赖影响：Manifest 新增 `match_about_blank` 与 `match_origin_as_fallback`；不新增项目运行依赖，测试复用本机 browser-tools 已安装的 jsdom。
- 验证：新增真实 DOM 运行测试，覆盖网易企业邮件、about:blank 继承、导航噪声拒绝、WhatsApp 隐藏手机号 JID、跨 frame 聚合五条路径。

## 2026-08-09 — 恢复 WhatsApp 顶层直读并兼容 Chromium frame API

- 用户可见变化：WhatsApp 恢复为扩展早期稳定的顶层页面直读，成功后不再进入网易专用的多 frame 流程；网易仅在顶层无正文时读取子 frame。frame 枚举改为 Chromium 全版本兼容的回调形式，失败时保留顶层结果并显示实际通信错误。
- 数据/依赖影响：扩展版本升级为 0.3.0；不修改业务数据、数据库结构或后端依赖。
- 验证：真实 DOM、内容脚本消息入口、frame 合并、Manifest/语法、扩展适配器夹具和扩展后端 API 回归均通过；全仓 36 项测试中扩展相关测试全部通过，另有 1 项既有客户附件响应字段契约测试失败，与本次扩展改动无关并单独保留记录。

## 2026-08-09 — 修复启动维护误删客户历史附件

- 用户可见变化：启动维护不再清理 `uploads/customer_files/` 下超过 45 天的客户附件；历史报价单、合同、样品资料等在服务重启后仍可下载。
- 原因：原维护逻辑遍历整个 `uploads/`，会把客户附件误判为可再生成的临时上传文件，造成数据库仍有元数据但文件本体缺失。
- 数据/依赖影响：不恢复已经被此前维护删除的文件；现有数据库、附件元数据和文件路径不变，无新增依赖。`CRM_UPLOAD_RETAIN_DAYS` 仍仅作用于其他临时上传文件。
- 验证：新增回归测试，上传后将客户 PDF 的修改时间设为 46 天前，执行启动维护并通过下载接口读取成功；同时修复该下载测试中未关闭的临时 PDF 字节流。相关回归测试通过。

## 2026-08-08 — 对齐 Pi Agent 规则与文档：删除单独成条、写入确认分级、修正已移除的 Pi Web 描述

- 用户可见变化：`AGENTS.md` 的 Pi Agent 操作助手边界原把所有写入一刀切都需确认；现拆分为两条——创建/修改/完成/记录沟通仍先展示字段再写入，但确认强度低于删除；删除联系人、沟通记录、待办和归档客户强制二次确认目标并保留可恢复撤销快照，删除确认优先级最高。`PRODUCT_DIRECTION.md` 和 `Trade OS 系统设计说明.md` 中描述的“Pi 工作台聊天入口 / Pi SDK Runtime / Pi Worker”已于 2026-08-05 随 Pi Web 集成移除而不复存在，现对齐为：Pi Agent 通过 `/api/agent/*` 接口向上层 AI 模型提供原子工具，模型调用由上层负责，无独立执行引擎。
- 原因：项目规则“事实源优先”要求产品说明与现行实现一致；Pi Web 前端聊天入口和 Pi SDK Runtime 已在 2026-08-05 的变更中移除，但两份设计文档仍在描述已不存在的执行引擎；同时用户明确要求把确认重心从写入移到删除（写入写错影响小，删除不能删错）。
- 数据/依赖影响：不修改数据库结构、业务数据或第三方依赖；仅修订项目协作规则与产品/系统设计文档措辞，不改代码逻辑（命中与提案机制保持现状）。
- 验证：逐项核对 `app.py` 的 `/api/agent/*` 路由实现与 `CHANGELOG.md` 2026-08-05 的移除记录；确认 `app/static/app.js` 与 `index.html` 已无 Pi 聊天入口调用（仅余一处孤立图标映射 `'打开 Pi Agent': 'sparkle'` 待后续清理）。

## 2026-08-08 — 客户文件支持点击即预览：Excel/CSV 服务端渲染表格预览

- 用户可见变化：客户详情的文件列表现在整行可点击——点击文件条目（图标或文件名）即可打开：PDF 与图片在浏览器内直接预览；Excel（.xlsx/.xls）与 CSV 由系统读取前 50 行渲染成表格预览页；文本（.txt/.md）渲染为文本预览页；Word、压缩包等不支持预览的类型点击直接下载。列表行显示“点击预览/点击下载”提示，悬停时文件名高亮，操作区保留“预览/下载/删除”按钮；预览页内置“下载原文件”与“关闭”。
- 原因：上传成功后用户需要更快地确认文件内容，此前只能靠右侧小按钮下载，Excel 也没有任何预览途径。
- 数据/依赖影响：新增预览接口 `GET /api/customers/<id>/files/<fileId>/preview`（图片/PDF 重定向到内联打开，其余渲染独立 HTML 预览页，所有单元格/文本内容均 HTML 转义防注入，读取前 50 行/15 列与 2 万字符上限）；新增依赖 `xlrd`（仅用于 .xls 老格式读取，BSD 许可，需在运行环境安装）。不改数据库结构。
- 验证：新增预览回归测试（xlsx 表格渲染、`<script>` 内容被转义、下载入口存在），全套测试通过；本机端到端验证 xlsx/csv/pdf/docx 四种路径均符合预期；已发布到线上并验证预览路由生效（未登录返回 401）、静态资源与日志正常。另外修复了生产 runtime venv 的 pip 包装脚本硬编码指向桌面 venv 的问题（改用 `python -m pip` 正确安装 xlrd）。

## 2026-08-08 — 客户资料新增“文件”页签：上传、预览、下载与删除附件

- 用户可见变化：客户详情工作区新增“文件”页签。可在客户名下上传 Excel、Word、PDF、图片、文本、压缩包等基本文件（单个不超过 25MB，可多选或拖拽），并可选填报价单/合同/样品/展会资料等分类。文件列表展示类型、大小、上传时间与上传人；PDF 与图片可直接在浏览器查看，其余类型点击下载；删除前需确认，文件本体移除后删除记录保留在操作日志中。文件会随客户详情一起加载，查阅客户时无需再翻找本地 Excel。
- 原因：团队之前只能在外部用 Excel 保存客户资料，无法在查阅客户时直接引用原始文件；客户资料页此前没有附件能力。
- 数据/依赖影响：新增 SQLite 表 `customer_files`（客户归属、原始名、存储名、相对路径、大小、MIME、分类、SHA256、上传人、软删除与时间戳），通过现有 `USER_TABLE_SQL` 启动迁移自动创建，无需手工迁移。文件二进制存放在 `uploads/customer_files/<客户ID>/`（随机令牌命名、按客户隔离）；SQLite 快照不含文件本体，从早期备份恢复后缺失的文件会在列表中标注“文件本体缺失”，不影响客户、联系人或沟通记录。新增接口：`GET/POST /api/customers/<id>/files`、`GET /api/customers/<id>/files/<fileId>/download`（`?inline=1` 预览）、`DELETE /api/customers/<id>/files/<fileId>`；下载与删除均校验文件归属客户。
- 验证：新增 3 项回归测试（上传→列表→预览/下载→删除全链路、拒绝不支持类型/空文件/未知客户、跨客户访问隔离），`unittest discover` 26 项全部通过；`py_compile` 与 `node --check` 通过。已通过 `deploy/macos/publish-production.sh` 发布到线上（发布前对 4 个生产库做了手动快照），重启后验证：`/api/network/ping` 与 `/api/auth/users` 正常、静态资源与新路由均生效（未登录请求新路由返回 401 而非 404）、三位用户生产库均自动建好 `customer_files` 表与索引、服务日志无错误。

## 2026-08-07 — 导出沟通记录时补齐备注、结果与下一步

- 用户可见变化：“导出全部沟通”（客户详情 → timeline 模式）此前只导出每条沟通记录的内容正文，结果/判断（结果字段）在正文非空时被丢弃、下一步计划完全不导出，且未包含客户资料中的“备注与需求”；“导出联系人”CSV 也未包含联系人的备注字段。现在两种导出都会完整包含对应备注内容。
- 原因：`_customer_context_markdown` 的时间线事件仅取 `content or result` 单一字段（content 非空时 result 丢失），且未读取 `next_plan`；“已记录需求与备注”块仅在全量/需求模式下输出；`export_contacts_csv` 的 SELECT 未包含 `ct.notes`。
- 数据/依赖影响：不修改数据库结构或业务数据，仅调整导出渲染与 CSV 字段；CSV 新增一列 `contact_notes`。
- 验证：构造样例数据验证 timeline/compact/full 三种模式均输出“结果：…”、“下一步：…”与“已记录需求与备注”；SQL 实测 `contact_notes` 正常返回；23 项 Python 回归测试全部通过。

## 2026-08-07 — 工程结构清理：移除开发残留文件

- 变更：删除未使用的旧虚拟环境 `venv/`、`app/venv/`（当前使用 `.venv-mac/`，`.venv/` 保留作离线依赖回退）；清理所有 `__pycache__/`（含无源码的 pi_bridge/pi_web_host 缓存）；清空 `logs/` 旧日志；删除一次性诊断脚本 `diag.py`（硬编码已废弃的 Windows 路径）、`test_inbox.py`、`preview_server.py`、空文件 `check_inbox_state.py` 与根目录空 `crm.db`；删除已完成使命的设计 QA 截图（`design-qa-implementation-*.png`、`design-audit/`）与已全部落地的 `MOTION_OPTIMIZATION_PLAN.md`；清理 `.DS_Store` 与空目录 `.uploads/`；`tmp/`（客户开发邮件清单，真实业务数据）加入 .gitignore 防止误提交。
- 保留依据：`serve.py` 为生产入口（`deploy/macos/run-production.sh` 引用）、`maintenance.py` 被 app.py 引用、`email_verifier.py` 被 scheduler.py 引用；`design/` 视觉参考库被 `TRADE_OS_UI_SYSTEM.md` 与 `CATALOG.md` 引用，`design-qa.md` 被 `inspiration/CATALOG.md` 引用为实现证据；`archive/` 为历史归档。
- 数据/依赖影响：不修改数据库结构、业务数据或第三方依赖；不影响生产运行（生产服务位于 `~/Library/Application Support/TradeOS/runtime/`，与本目录独立；`data` 符号链接未改动）。
- 验证：`py_compile` 全部核心模块（app/db/config/desktop/scheduler/ical_gen/email_verifier/maintenance/serve/app.engine）通过；`Mac启动器.command` 与 deploy 脚本引用的 `.venv-mac/`、`serve.py`、`maintenance.py` 均完好；Git 跟踪文件零删除；磁盘占用约 339M → 287M。

## 2026-08-06 — 修复搜索客户时刷屏报错

- 用户可见变化：在全局搜索栏输入客户关键词时不再频繁弹出“请求失败”错误提示；快速连续输入时，系统只保留最后一次搜索请求，之前被替换的请求按正常取消处理，不视为失败。快速切换客户工作区时也不再出现同类取消报错。
- 原因：搜索预览对每个字符都发起一次请求并中止上一个在途请求；被中止的请求抛出 `AbortError`，前端请求层把它当作普通失败弹出了错误提示，而实际搜索仍正常返回结果。
- 数据/依赖影响：不修改数据库结构或 CRM 数据，仅调整前端请求层对主动取消请求的处理；同时为客户分析流程里的客户详情请求补充静默模式，与其自身错误展示保持一致。
- 验证：`node --check app/static/app.js` 通过；已发布到生产运行环境并重启，`/api/network/ping` 与 `/api/version` 均返回 200，服务端已能提供修复后的 app.js。

## 2026-08-06 — 本周工作页面加载提速

- 用户可见变化：周报/本周工作接口响应从约 134KB 降到 35KB（删除前端不使用的整周跟进列表与明细聚合副本，只保留周报卡片所需数据）；接口增加 60 秒聚合缓存，写入操作后自动失效；文本静态资源（CSS/JS/HTML/API）支持 gzip，app.js 从 331KB 降到 80KB、visual-v2.css 从 171KB 降到 35KB。
- 数据/依赖影响：不修改数据库结构或 CRM 数据；响应字段删除 `follow_ups` 与 `reported_items`，前端仅使用 `reported_customers`（对应加载逻辑同步简化）。
- 验证：23 项 Python 回归测试全部通过；本机首次聚合 17ms、缓存命中 1ms；公网 CDN 缓存命中后静态资源约 1s；页面无功能回归。公网隧道吞吐仍受本机代理线路限制（约 8–33KB/s），与代码无关。

## 2026-08-05 — 客户网站识别接入 Brave Search 与 browser-tools

- 用户可见变化：在新增客户时输入官网并点击“从网站识别/自动识别”，系统会读取官网标题、结构化数据、国家线索、邮箱、电话、LinkedIn 和行业线索；可配置 Brave Search 补充公开摘要，可用 Chrome DevTools 时由 browser-tools 读取动态页面。识别结果先展示来源和待确认字段，点击“确认并应用”后才填入表单，保存客户仍需用户再次确认。
- AI 使用边界：官网事实与规则推断分开标注；AI 整理改为表单中的可选勾选项，模型不可用时网站识别仍可工作，模型结果不会自动写入客户或联系人。
- 数据/依赖影响：不新增 SQLite 表、不自动创建客户、不修改既有业务数据。安装用户级 Skill `brave-search` 与 `browser-tools` 到 `/Users/luoxin/.codex/skills/` 并安装 npm 依赖；Brave 需要用户自行配置 `BRAVE_API_KEY`，动态页面读取需要 Chrome/Chromium 和 CDP 9222。npm 审计提示上游依赖存在高危/严重项，未执行自动升级。
- 验证：`.venv-mac/bin/python -m unittest discover -s tests -p 'test*.py' -v` 通过，23 项测试全部通过；`py_compile`、`node --check app/static/app.js` 通过。当前机器未发现 PATH 中的 Chrome，因此 browser-tools 已安装但尚未进行真实动态页面读取。

## 2026-08-05 — 提高客户资料加载稳定性

- 用户可见变化：客户详情主资料请求遇到临时网络、服务异常或数据传输不完整时会自动重试；失败提示会区分超时、服务繁忙、资料不存在和数据不完整，并确保“重新加载”按钮可以真正再次发起请求。
- 稳定性优化：SQLite 每个进程只尝试一次 WAL，避免每次详情请求重复触发文件锁；WAL 或连接参数暂时不可用时，读取请求继续使用当前日志模式，详情连接始终释放；客户详情和客户分析请求均使用静默重试，避免重复弹出通用错误提示。
- 数据/依赖影响：不修改 CRM 数据、表结构或第三方依赖；仅调整 SQLite 连接初始化与前端 GET 请求重试策略。
- 验证：Python/JavaScript 语法检查通过；临时数据库详情接口并发 40 次全部返回 200（平均约 49ms、最大约 66ms）；前端请求层已验证首次 500 后自动重试成功。页面资源加载正常，当前浏览器安全策略限制了本地页面自动点击，重试入口已通过代码路径检查确认。

## 2026-08-05 — 优化客户详情加载反馈与重复打开行为

- 用户可见变化：点击客户后会立即打开“正在加载客户资料…”状态，并锁定详情窗口中的内容区域；基础资料完成后立即显示，客户分析在后台继续加载。加载失败或超过 15 秒时显示原因和“重新加载”按钮，关闭窗口不会被迟到的响应重新打开或覆盖。
- 性能与交互优化：取消过期的客户详情请求，连续点击只保留最后一次；移除客户分析流程中的重复客户详情请求，不再等待 AI 分析完成后才显示基础资料。
- 数据/依赖影响：不修改 CRM 数据、SQLite 结构或第三方依赖；仅增加前端加载状态、请求取消和失败重试逻辑。
- 验证：已在正常本地环境、约 1.2 秒延迟的慢网络、离线失败和连续点击场景中完成浏览器回归；正常本地客户详情请求约 16ms，分析查询约 7ms；JavaScript 语法检查通过，未发现浏览器错误日志。

## 2026-08-05 — 优化旧设备前端帧率与首屏负载

- 用户可见变化：检测到低功耗设备（包括 4 核旧 Mac、低内存或开启省流量模式）时，工作台会自动关闭页面转场、列表重排动画、主要玻璃模糊和背景纹理；客户列表在低功耗模式下分批出现，降低首屏卡顿，页面信息架构和业务操作保持不变。
- 性能优化：静态 JavaScript/CSS/HTML 与 JSON 响应支持 gzip；仪表盘独立接口并行请求；总览页不再默认下载当前界面未挂载的全量客户索引；全局客户搜索会取消过期请求；客户/今日列表刷新会清理旧的行级排序监听器，避免重复进入页面后的监听器累积。
- 数据/依赖影响：不修改 CRM 数据、SQLite 结构或第三方依赖；gzip 使用 Python 标准库，低功耗模式仅改变材质与动效呈现。
- 验证：`node --check app/static/app.js`、`.venv-mac/bin/python -m py_compile app.py` 通过；22 项 Python 回归测试通过；本机浏览器 1× CPU 客户页采样 2.86 秒得到 169 帧、2 个慢帧、0 个长任务；4× CPU 压力测试首屏 3 个慢帧，进入空闲后连续约 13 秒无新增慢帧；资源响应已确认返回 `Content-Encoding: gzip`。

## 2026-08-13 — 关闭背调报告过期自动提醒（research_stale）

- 用户可见变化：不再自动生成“背调报告已超过30天未更新，建议刷新”提醒（该提醒由调度器每日检查 `research_reports.expires_at` 生成，关闭后不会反复出现）。已清除 13 条未完成的 research_stale 待办。
- 数据/依赖影响：通过系统用户设置关闭 `ai_research` 可选模块（仅 Hamid）；不修改代码与调度逻辑，需要时可将模块开关改回开启。AI 背调生成功能不受影响（生成接口不依赖该模块开关）。已过期背调报告的 `expires_at` 统一顺延 30 天，避免调度器在开关生效前继续生成提醒。
- 验证：未完成 research_stale 待办清零；`user_preferences:hamid` 中 `ai_research=false`；写入前后快照 `system.db.pre-disable-research-20260813`、`hamid.db.pre-disable-research-20260813`。

## 2026-08-05 — 修复 AI 整理写入客户 notes 的乱码记录

- 用户可见变化：客户详情“关键需求”处曾出现 ASCII 乱码（中文/符号被替换为 `?`，如“7月31日”显示为“? 7 ? 31”）。已修复受影响的两条客户记录（Gold Coast Plastics、Plastics Direct）的 notes 字段，按跟进记录原文还原为正常 UTF-8 文本。
- 数据/依赖影响：仅修正 `customers.notes` 两条记录的文本内容；写入前保留一致性快照 `hamid.db.pre-fix-notes-20260805`，操作日志记录 `FIX_GARBLED_NOTES`。未修改数据库结构。
- 根因（待开发修复）：AI 整理接口把模型输出写入 notes 时的编码处理存在缺陷，非 ASCII 字符被替换为 `?`；本次为数据层修复，代码级修复另行安排。
- 验证：全库扫描确认除这两条外无其他含 `??` 或 U+FFFD 的乱码文本；修复后 hex 抽查为合法 UTF-8。

## 2026-08-05 — 移除 Pi Web 集成并优化首屏渲染性能

- 用户可见变化：工作台不再提供内嵌 Pi Web 聊天按钮、面板和原生 Pi 页面入口；相关 `/api/pi/*` 接口、`pi_bridge.py`、`pi_web_host.py`、`启动 Pi Agent.command`、`.pi/` 与 `.pi-runtime/` 全部移除。前端删除约 1100 行 Pi Agent 代码与 3 个悬浮/面板组件，页面首次加载更轻；`/api/agent/command` 轻量技能路由保留。
- 性能优化：移除全部 `backdrop-filter: url("#tradeOsLiquidGlass")` 无效引用及 SVG liquid-glass 滤镜定义；主要毛玻璃降档（侧栏 66→30px、命令栏 36→24px、客户工作区头部 34→28px），浮层（全局搜索、菜单）保持原有效值，视觉基本不变。两张在用的 2MB 背景图转为 WebP（1.99MB→91KB、1.65MB→103KB），并删除两张未引用图片。静态资源总目录从约 19MB 降到 4.6MB。
- 数据/依赖影响：不修改数据库结构或 CRM 数据；删除未使用的中文字体包 11MB、未跟踪的旧服务器备份目录及压缩包 47MB。Pi 会话历史（`pi-sessions`）随运行时一并移除，不涉及业务数据。
- 验证：Python 语法检查、JavaScript 语法检查通过；22 项 Python 回归测试全部通过（移除 5 项 pi 契约测试）；本机临时实例验证首页、CSS、JS、WebP 背景图均返回 200，页面无 ai-ask/nativePi/liquid-glass 残留引用。

## 2026-08-05 — Pi Agent 支持经确认的今日待办重排

- 用户可见变化：Pi Agent 新增“重排今日待办”工具。它会先读取当前完整待办列表，展示原顺序与新顺序，用户确认后才调整今日/逾期待办的显示顺序；不会修改标题、日期、完成状态或客户资料。
- 数据/依赖影响：复用既有 `reminders.manual_order` 字段；排序写入增加操作日志、更新时间和冲突感知撤销快照，不新增数据库表或第三方依赖。提交时可携带当前列表快照，待办在确认期间发生变化会拒绝覆盖。
- 验证：新增 Pi 工具契约、完整列表校验、排序写入和撤销回归测试；26 项 Python 回归测试全部通过，并完成 Python 语法检查。

## 2026-08-05 — Pi Agent 支持经确认的联系人删除

- 用户可见变化：Pi Agent 新增“删除联系人”工具。它会重新读取客户联系人，确认目标字段后才删除；主联系人和仍被沟通、开发信或邮件投递记录引用的联系人会被拒绝删除。
- 数据/依赖影响：复用既有 `contacts` 表和联系人删除接口；删除增加操作日志、冲突感知撤销快照，不新增数据库表或第三方依赖。无历史引用的非主联系人删除后可恢复。
- 验证：新增联系人历史依赖拦截、删除、撤销和 Pi 工具契约回归测试；27 项 Python 回归测试全部通过，并完成 Python、JavaScript 和 zsh 语法检查。

## 2026-08-05 — 修复周报接口未登录数据暴露

- 用户可见变化：周报接口现在要求登录；已登录总览和周报功能保持可用，未登录访问返回 401。
- 数据/依赖影响：不修改客户数据、数据库结构或第三方依赖；收紧已有接口的访问边界。
- 验证：路由扫描确认业务数据接口均有登录保护；新增回归测试覆盖未登录周报请求，正式公网验证将确认 401。

## 2026-08-05 — 补齐浏览器默认站点图标

- 用户可见变化：浏览器请求 `/favicon.ico` 时现在返回 Trade OS 的现有界面图标，不再出现无意义的静态资源 404。
- 数据/依赖影响：复用现有 SVG 图标，不新增依赖，不修改 CRM 数据或数据库结构。
- 验证：已确认 CSS 引用的图标和图片资源全部返回 HTTP 200；新增 favicon 路由后将通过正式公网入口复测。

## 2026-08-04 — 阻止空客户记录进入正式数据

- 用户可见变化：创建客户时至少需要填写客户名称或公司名称；空表单现在返回明确提示，不会生成空客户。
- 数据/依赖影响：仅增加后端输入校验，不修改既有客户、联系人或数据库结构。
- 验证：回归测试覆盖“空请求返回 400 且客户数量不变”；已清理本次验收产生的唯一空客户记录。

## 2026-08-04 — 完善正式发布健康检查与生产访问码说明

- 用户可见变化：发布脚本现在同时检查本机网络探针和用户列表接口；生产访问码说明与当前实现一致，首次由用户设置并只保存哈希，不再要求把明文访问码放入环境文件。
- 数据/依赖影响：不修改 CRM 数据、数据库结构或现有访问码；发布脚本固定使用 `/usr/bin/curl`，避免 LaunchAgent 或不同 shell 的 PATH 差异造成误报。
- 验证：公网首页、CSS、视觉样式和主 JavaScript 均返回 HTTP 200；本机主页和网络探针返回 HTTP 200；24 项 Python 回归测试全部通过，包含生产 PIN 设置/登录、客户创建、联系人添加、删除、恢复和永久删除回归。

## 2026-08-04 — Tunnel 增加本机代理出站兼容

- 用户可见变化：当当前网络直连 Cloudflare Tunnel 的 TCP/UDP 7844 均被阻断时，macOS Tunnel 会通过本机 HTTP CONNECT 代理建立 7844 出站连接，恢复 `app.trosa.space` 的公网访问。
- 数据/依赖影响：新增仅使用 Python 标准库的本机 Tunnel 转发辅助进程；不读取或写入 CRM 数据，不修改数据库、业务端口或 Cloudflare 凭据。代理地址默认使用现有 `127.0.0.1:7892`，可通过环境变量调整。
- 验证：本机代理对 `region1.v2.argotunnel.com:7844` 返回 CONNECT 200；正式 LaunchAgent 重载和再次重启自恢复后均出现 4 条 `Registered tunnel connection`，连续公网探测和 `https://app.trosa.space/` 请求均返回 HTTP/2 200。

## 2026-08-04 — Tunnel 自启动文件迁移到正式运行目录

- 用户可见变化：电脑重启后，Tunnel 自启动不再依赖桌面项目目录；启动脚本、Tunnel 配置和代理辅助程序统一放在 `~/Library/Application Support/TradeOS/tunnel/`，降低 LaunchAgent 在登录早期无法打开桌面路径导致 1033 的风险。代理尚未就绪时，LaunchAgent 每 10 秒重试一次。
- 数据/依赖影响：不修改 CRM 数据、数据库结构或第三方依赖；发布脚本新增三份 Tunnel 运行文件的同步，Tunnel 配置继续保留现有凭据文件路径。
- 验证：已确认重启后旧 LaunchAgent 的 exit code 127 与桌面脚本 `can't open input file` 日志；迁移后的正式 LaunchAgent 状态为 `running`，并已通过公网 HTTP/2 200 验证。

## 2026-08-04 — Tunnel 传输协议改为自动协商

- 用户可见变化：macOS 正式 Tunnel 不再强制使用 HTTP/2/TCP，启动时允许 `cloudflared` 在 QUIC/UDP 与 HTTP/2/TCP 之间自动选择，降低单一出站协议受限时出现 Cloudflare 1033 的概率。
- 数据/依赖影响：不修改 CRM 数据、数据库结构或第三方依赖；仍使用现有 Cloudflare Tunnel 与本地 `127.0.0.1:8080` 上游。
- 验证：已核对当前 Tunnel 日志中的 TCP 7844 超时、实测 `cloudflared` 版本 2026.7.3，并根据 Cloudflare 官方连通性要求更新启动模板；最终恢复仍取决于当前网络至少放行 TCP 或 UDP 7844。

## 2026-08-04 — 恢复原生 Pi 页面完整窗口与默认项目会话

- 用户可见变化：移除 QSA 内的半屏 iframe 承载，Pi 入口现在直接打开原生 Pi Web UI 的完整独立窗口；QSA 页面保留在原窗口，原生页面本身不再被重新排版。
- 数据/依赖影响：Pi Web UI 继续以当前 TradeOS 项目根目录作为 `cwd`，移除本次嵌入新增的 `PI_CODING_AGENT_DIR` 会话目录，恢复 Pi 默认会话位置；CRM 业务读写仍通过 Trosa API 和确认流程完成。
- 验证：前端不再创建 `nativePiFrame`；后端启动管理器不再设置独立 Pi 会话目录；21 项 Python 回归测试、Python/JavaScript/zsh 语法检查通过。

## 2026-08-04 — 修复原生 Pi Web UI 启动后端口拒绝连接

- 用户可见变化：修复 Pi Web UI 启动前的 `trosa-pi --help` 检查在后台服务中超时，以及后端过早返回未监听端口的问题；帮助检查改为启动器的快速静态响应，真正的 Pi RPC 仍通过 Trosa 扩展启动，页面只会在原生 UI 确认监听后加载。
- 数据/依赖影响：仅调整项目内 Pi 启动器的帮助检查分支，不修改 CRM 数据、数据库结构或第三方依赖。
- 验证：源代码与正式运行副本的 `trosa-pi --help` 检查通过；Pi Web UI 独立临时端口启动并正常监听；启动管理器等待真实监听标记；21 项 Python 回归测试、Python/JavaScript/zsh 语法检查通过。

## 2026-08-04 — 发布原生 Pi Web 入口并修复正式服务 405

- 用户可见变化：将原生 Pi Web 启动路由和前端资源同步到 Mac 正式运行副本；原生 Pi 入口不再因正式服务仍使用旧代码而返回 405。
- 数据/依赖影响：仅同步代码与静态资源并重启本机 Waitress 服务；唯一业务数据目录、私密环境变量、日志和 Python 运行环境均未修改。
- 验证：发布脚本健康检查通过；正式服务对 `POST /api/pi/native/start` 返回预期的未登录 401 而非 405；21 项 Python 回归测试和 Python、JavaScript、zsh 语法检查通过。

## 2026-08-04 — 工作台接入原生 Pi Web Agent 半屏页面

- 用户可见变化：工作台现有 Pi 入口改为打开原生 `pi-web-ui` 页面；原生聊天、工具过程、会话管理、确认和停止能力保持原样，页面以右侧半屏工作区承载，QSA/Trosa 主界面继续可见；页面启动失败或加载超时会提供重试和新窗口入口。
- 数据/依赖影响：新增原生 Pi Web UI 进程管理和当前账号会话 Cookie 传递；每个账号使用独立 Pi 会话目录，CRM SQLite 数据源、Trosa 业务 API 和写入确认边界不变；未配置 `CRM_DB_PATH` 时，Pi 会话默认保存在项目 `.pi-runtime/agent-data`，不写入业务数据库。
- 验证：修复原生 Pi 启动器的项目路径和变量名；原生 Pi Web UI 进程已通过本机回环端口启动检查；20 项既有 Python 回归测试与新增原生入口契约检查通过；Python、JavaScript 和 zsh 语法检查通过。

## 2026-08-04 — Pi 聊天改为强制使用 Pi SDK Runtime

- 用户可见变化：工作台 Pi 聊天只通过 Pi SDK Runtime 处理请求；移除自动转交旧 `/api/agent/command` 和 `/api/ai/ask` 的降级链路。Pi Runtime 不可用时，界面显示明确错误并停止本次任务，不把其他 Agent 或模型的结果伪装成 Pi 回答。
- 数据/依赖影响：不新增数据库表、不修改 CRM 数据、不新增第三方依赖；核心 CRM 继续独立运行，但 Pi 聊天必须配置并运行 Pi Worker、模型和 Trosa 业务接口。
- 验证：新增 Pi 前端单执行引擎回归检查；20 项 Python 回归测试、Python/JavaScript/Node Worker 语法检查通过；真实 Pi SDK Worker 启动成功并注册 15 个 Trosa 工具。

## 2026-08-04 — Pi 通用 Agent 增加原子化沟通检索

- 用户可见变化：普通自然语言任务不再要求命中固定 Skill；Pi 可自由组合客户搜索、客户工作区、沟通搜索和客户时间线工具，支持按关键词、国家、沟通方向和日期范围追溯已记录事实，再生成下一步建议。
- 数据/依赖影响：新增只读 `/api/agent/messages/search` 和 `/api/agent/customers/<id>/timeline` 接口；不新增数据库表、不修改 CRM 数据、不新增第三方依赖。Pi 仍不能直接访问 SQLite、文件或 Shell，新增、修改、完成待办和记录沟通仍需确认。
- 验证：19 项 Python 回归测试通过；Python、前端 JavaScript 和 Pi Worker 语法检查通过；Pi SDK Worker 启动成功并注册 15 个 Trosa 工具。

## 2026-08-04 — 增加 Pi 多会话和运行中控制

- 用户可见变化：聊天框支持新建、切换和命名多个 Pi 会话；运行中的 Pi Agent 支持停止、立即追加指令和排队继续；Pi 原生的选择、单行输入和编辑器请求会显示为对应的确认控件。
- 数据/依赖影响：继续使用每个用户的 `pi-sessions/<用户>` 会话文件保存历史，不修改 CRM 数据表、不改变 CRM 单一数据源，也不新增第三方依赖；会话控制通过 Pi SDK Runtime 原生 `newSession`、`switchSession`、`steer`、`followUp` 和 `abort` 完成。
- 验证：18 项 Python 回归测试通过；Python、前端 JavaScript 和 Pi Worker 语法检查通过；Pi SDK Worker 已实测启动、注册 13 个 Trosa 工具，并实测新建和命名会话成功。

## 2026-08-04 — 修复 Pi 新建会话无响应

- 用户可见变化：新建 Pi 会话遇到上一条任务占用 Runtime 时会立即提示当前忙碌状态，不再长时间无反馈；按钮会显示“创建中”并在成功或失败后恢复。
- 数据/依赖影响：仅调整 Pi Runtime 会话控制锁和前端错误反馈，不修改 CRM 数据、会话历史或数据库结构。
- 验证：Python 和 JavaScript 语法检查通过；正式服务将以非阻塞会话控制重新发布。

## 2026-08-04 — 增加 Pi 待办修改、撤销和持久化会话

- 用户可见变化：Pi Agent 现在可以在确认后修改现有待办、调整日期、取消待办和创建待办；聊天框显示持久化的历史消息，并为已完成写入提供“撤销这次操作”入口。撤销会在发现数据已被后续修改时停止覆盖并提示冲突。
- 数据/依赖影响：每个用户数据库新增 `undo_actions` 快照表和索引，用于保存写入前/写入后的业务数据快照；Pi 会话改为保存到 `pi-sessions/<用户>`，不改变客户、联系人、沟通和待办的既有数据源，也不新增第三方依赖。
- 验证：17 项 Python 回归测试通过，包含待办修改、日期调整、取消、撤销恢复和撤销冲突测试；Python/JavaScript/Node Worker 语法检查通过；Pi SDK Worker 启动成功并注册 13 个 Trosa 业务工具。

## 2026-08-04 — 将 Pi SDK Worker 接入工作台聊天框

- 用户可见变化：聊天框默认通过 Pi SDK Worker 运行真实 Pi Agent，回答过程中会显示分析、工具执行、完成和等待确认等状态；请求改为后台任务，长时间模型调用不再占住网页请求，降低 Cloudflare 524 的概率。
- 数据/依赖影响：新增项目级 `.pi/pi-sdk-worker.mjs`，复用现有 Trosa Extension 和业务 API；关闭 Pi 内置文件、Shell、编辑和数据库能力；新增异步 Pi 任务接口，不修改 CRM 数据结构或业务数据。
- 验证：Node Worker 启动并注册 8 个 Trosa 工具；本地 Pi 异步任务接口返回 HTTP 202 且轮询接口返回终态；Python/JavaScript 语法检查通过；15 项 Python 回归测试通过；正式服务已发布重启，Worker 文件与源代码一致、provider catalog 保留 38 个文件，未登录 Pi 接口返回 401，公开用户接口返回 200。

## 2026-08-04 — 修复正式服务无法启动 Pi Runtime

- 用户可见变化：修复 macOS LaunchAgent 环境下 Pi Agent 找不到 Node.js、聊天框错误回退到只读问答的问题。
- 数据/依赖影响：仅为 Pi 子进程补充固定的 macOS Node/Homebrew PATH；不修改 CRM 数据、数据库结构或模型配置。
- 验证：已确认正式 LaunchAgent 环境缺少交互式 PATH；修复后在无 Node PATH 的模拟后台环境中真实 Pi RPC 启动成功；Python/JavaScript 检查和 15 项回归测试通过；正式服务已重新发布并运行。

## 2026-08-04 — 修复发布脚本误排除 Pi provider catalog

- 用户可见变化：修复正式服务中 Pi 进程导入阶段缺少 `amazon-bedrock.json` 等 provider 数据、聊天框持续回退到只读问答的问题。
- 数据/依赖影响：发布脚本现在只排除项目根目录的业务 `data/`，保留 `node_modules/**/data/` 等依赖目录；不修改 CRM 数据或数据库结构。
- 验证：正式副本已补齐 38 个 Pi provider 数据文件；`/api/pi/chat` 固定短句测试返回 HTTP 200 和 `runtime: pi`；正式服务已重新发布并重启。

## 2026-08-04 — 将工作台聊天框接入真实 Pi Runtime

- 用户可见变化：Pi Agent 浮动聊天框现在通过 Pi RPC 运行真实 Agent，会调用项目级 Trosa 工具读取客户资料、待办和沟通；写入操作会把 Pi 的确认请求显示在工作台确认卡中。
- 数据/依赖影响：新增后端 Pi RPC 子进程桥接和会话级确认响应；Pi 只能通过 Trosa 业务 API 访问 CRM，继续关闭文件、Shell 和 SQLite 工具；不新增数据库表或第三方依赖，使用现有 `.pi-runtime` 与 DeepSeek 配置。
- 验证：Python/JavaScript 语法检查通过；真实 Pi RPC 连通通过；使用本机 Demo API 验证 Pi 调用 Trosa 只读工具并返回结果；15 项 Python 回归测试通过；正式服务已发布并重启，Pi 路由未登录请求返回 401，登录接口和公网健康检查返回 200。

## 2026-08-04 — 将 Pi Agent 嵌入 Trosa 工作台

- 用户可见变化：新增可拖动的 Pi Agent 浮动入口；支持读取今日/本周待办、查看当前客户上下文，并用自然语言准备新增待办或记录沟通。
- 数据/依赖影响：新增 `/api/agent/command` 自然语言路由和待确认操作卡；读取直接使用 SQLite 事实源，写入先保存到既有 `agent_proposals`，只有用户确认后才写入 `reminders` 或 `follow_up_logs`；新增取消提议接口，无数据库迁移、无新增第三方依赖，未配置模型时核心 CRM 仍可独立运行。
- 验证：新增 Agent 命令回归测试；15 项 Python 回归测试全部通过；`py_compile` 与 `node --check app/static/app.js` 通过。

## 2026-08-04 — 增加 Pi Agent 旧服务兼容降级

- 用户可见变化：当正在运行的 Trosa 服务尚未加载 Pi Agent 命令路由时，读取类问题自动回退到既有只读问答，并明确提示写入能力需要重启服务；不再直接显示 405 阻断提示。
- 数据/依赖影响：仅增加前端错误状态识别和兼容降级，无数据库、业务数据或依赖变化。
- 验证：JavaScript 语法检查通过；新 Agent 服务仍优先使用受控命令与确认提议路径。

## 2026-08-04 — 将 Kelly 账号更名为 Kelley

- 用户可见变化：账号、界面标签和操作说明统一使用 `kelley` / `Kelley`。
- 数据/依赖影响：当前 `data/kelly.db` 迁移为 `data/kelley.db`，访问码环境变量改为 `CRM_PIN_KELLEY`；历史备份保留原文件名，无新增依赖。
- 验证：已完成代码与文档引用检查，并通过 Python、JavaScript 语法检查。

- 补充修复：同步备用运行副本的登录用户列表、显示标签与访问码变量，避免登录页继续显示 Kelly。

## 2026-08-04 — 增加 Trosa Pi Agent 基础操作层

- 用户可见变化：Pi 浏览器助手现在可读取今日工作、搜索客户、查看客户工作区，并通过自然语言新增客户、联系人、修改基础资料、创建跟进和记录沟通。
- 数据/依赖影响：新增项目级 Pi 扩展与受控启动包装器；Pi 关闭文件、编辑、Shell 和直接数据库工具，写入统一调用现有 Trosa API，并在执行前弹出确认；新增 Trosa 账号登录环境变量，不写入项目文件。
- 验证：已核对现有客户、客户工作区、Agent 提议和客户更新接口；Pi 包装器与扩展文件设置为可执行/可加载，待本机 Trosa 服务运行后进行端到端验证。

## 2026-08-03 — 完成已更新客户的逾期待办

- 用户可见变化：将 Amy 的 19 条、Kelly 的 7 条已在今天产生跟进更新的逾期待办标记为完成；Hamid、Amy、Kelly 的 15/30/60 天周期开发待办保留。
- 数据/依赖影响：更新 Amy 与 Kelly 的 26 条 `reminders`，并补齐可对应的跟进记录关联；分别保留 `amy.db.pre-complete-overdue-followups-20260803` 与 `kelly.db.pre-complete-overdue-followups-20260803` 备份；无新增依赖。
- 验证：三人数据库中，今天已有跟进但仍未完成的非周期逾期待办为 0；Amy 与 Kelly SQLite `integrity_check` 均返回 `ok`。

## 2026-08-03 — 补全 YXE Acrylics 待办状态

- 用户可见变化：将 YXE Acrylics 的今日价格反馈/DDP 跟进待办标记为已完成，并关联跟进记录。
- 数据/依赖影响：仅更新 Hamid 数据库中的 1 条 `reminders` 与 1 条 `follow_up_logs`；操作前备份为 `hamid.db.pre-complete-yxe-20260803`；无新增依赖。
- 验证：待办 2224 已完成并关联跟进记录 34374；SQLite `integrity_check` 返回 `ok`。

## 2026-08-03 — 补全三条阿联酋客户待办状态

- 用户可见变化：将 Bluecon Manufacturing、Creative Roots、Synergy Arabia 的今日跟进待办标记为已完成，并关联各自的跟进记录。
- 数据/依赖影响：仅更新 Hamid 数据库中的 3 条 `reminders` 与 3 条 `follow_up_logs`；操作前备份为 `hamid.db.pre-complete-uae-20260803`；无新增依赖。
- 验证：3 条待办均为 `is_done=1`，关联记录分别为 34377、34378、34379；SQLite `integrity_check` 返回 `ok`。

## 2026-08-03 — 增加 Pi Agent 安全启动入口

- 用户可见变化：新增「启动 Pi Agent.command」，双击后隐藏输入 DeepSeek API Key，启动本机浏览器界面并打开 `http://127.0.0.1:3210`。
- 数据/依赖影响：新增本地 `@watercol/pi-web-ui` 浏览器界面依赖；密钥仅存在于当前启动进程环境，不写入项目文件。
- 验证：Pi Agent `0.83.0` 与 Pi Web UI `0.5.4` 可执行文件验证通过；启动脚本已设置为可执行，并优先复用 ChatGPT 已有的 ripgrep。

## 2026-08-03 — 合并恢复最新客户与跟进数据

- 用户可见变化：从微信归档项目恢复并合并 Hamid、Amy、Kelly 三位成员的客户、联系人和跟进记录；不导入旧项目的 Inbox、提醒、AI 或邮件数据。
- 数据/依赖影响：Hamid 合并新增/更新 9 个客户、31 个联系人、8 条跟进；Amy 更新 19 个客户并新增 18 个联系人、19 条跟进；Kelly 更新 14 个客户并新增 19 条跟进。三人的合并前数据库均保留 `*.pre-merge-20260803` 备份；无新增依赖。
- 验证：三人数据库 SQLite `integrity_check` 均返回 `ok`；合并后分别为 Hamid 401/563/903、Amy 294/233/1542、Kelly 153/0/1065（客户/联系人/跟进）。

## 2026-08-03 — 修复新客户完成后重复显示待办

- 用户可见变化：完成或记录新客户开发跟进后，剩余的 15/30/60 天自动开发节点会自动结束，不再把同一客户重复显示为待办。
- 数据/依赖影响：仅更新现有 `reminders` 的完成状态；无数据库迁移或新增依赖。
- 验证：已核对 Gulfcrafts 的重复节点来源，并执行 Python 语法编译检查。

## 2026-08-03 — 恢复沟通方式选择与客户详情高透玻璃

- 用户可见变化：记录沟通的“沟通方式”恢复为明确的下拉选择；客户详情恢复高透液态玻璃主层与内容层，保留正文可读性。
- 数据/依赖影响：无数据库迁移或业务数据变更；AI 整理仍保持用户主动触发。
- 验证：前端 JavaScript 与 Python 语法检查通过；正式服务已发布重启，模型服务无客户数据的连通性请求返回 HTTP 200。

## 2026-08-03 — 手机端今日页恢复顶部快捷操作与导航抽屉

- 用户可见变化：手机端「今天」页不再显示会遮挡待办列表的底部悬浮快捷栏；搜索与「记录沟通」快捷入口固定在页面顶部区域，并使用与宽屏一致的半透明玻璃图标按钮。左上角菜单按钮和侧边导航抽屉恢复可用，页面切换无需依赖底部操作条。
- 数据/依赖影响：无数据库结构、业务数据或第三方依赖变更。
- 验证：执行前端 JavaScript 语法检查；以 319px 手机视口检查快捷栏定位和导航抽屉规则。

## 2026-08-02

### 优化本周工作与跨成员客户档案查看

- 本周工作沿用主工作台的抽象木纹背景，恢复按成员分栏的玻璃卡片层次；客户档案同样使用木纹纸张与轻玻璃叠层。
- 移除客户“点亮/标记”入口与所有页面视觉状态；历史字段仅为兼容既有数据库保留。
- 沟通记录原有的黄、绿、粉文本高亮继续以富文本方式保留，并在本周工作与只读客户档案中正常呈现。
- 客户档案内的资料、时间线与侧栏从直角分割调整为圆角玻璃分区，减少生硬的格线和折角感。
- 点击客户后打开完整的只读客户档案，包含资料、联系人、待办、下一步和完整沟通时间线；周会查看不会切换账号或写入对方数据。
- 移除本周工作右上角不相关的全局操作栏，仅保留全局搜索，避免误导为可在跨成员周会页直接写入。
- 数据与依赖影响：周会客户详情接口按需增加只读详情字段；无数据库迁移、无业务数据写入、无新增依赖。
- 验证：`python3 -m py_compile app.py`、`node --check app/static/app.js` 通过；使用项目虚拟环境运行周会访问控制与客户视图回归测试通过。桌面与窄屏视觉布局已补充对应响应式规则，待发布后在实际设备复核。

### 修复 Mac 自动启动读取私密配置

- 生产服务不再依赖桌面目录中的原有私密配置文件，改为只读取独立的 `.env.production`。
- 正式运行副本和唯一业务数据迁至 `~/Library/Application Support/TradeOS/runtime/`，原项目根目录 `data/` 改为指向该目录的链接，避免 macOS 对桌面目录的后台访问限制。
- 数据与依赖影响：完成一次同机位置迁移，无业务数据复制或合并；新增独立会话密钥配置。
- 验证：LaunchAgent 正在运行；生产登录接口已返回三人账号与访问码模式；本机 `127.0.0.1:8080` 和 `https://app.trosa.space` 均返回 HTTP 200。

### 优化线上首次访问后的页面加载

- 将本地脚本、样式、字体和视觉素材设置为长期缓存，避免手机每次重新打开都重复下载较大的字体与背景素材。
- 数据与依赖影响：无；不修改业务数据或接口结果。
- 验证：正式服务已重载；背景图片响应头为 `public, max-age=31536000, immutable`；本机登录接口与 `https://app.trosa.space` 返回成功。

### 增加正式环境一键发布脚本

- 新增 `deploy/macos/publish-production.sh`：将桌面项目的代码与静态资源同步到正式运行目录，自动重启服务并检查健康状态。
- 数据与依赖影响：明确排除唯一业务数据、私密设置、日志与运行环境，不创建第二份数据库。
- 验证：待首次发布时运行脚本并检查线上地址。

### 增加线上请求状态提示

- 线上请求超过 240ms 时，在页面顶部显示“正在刷新最新内容”或“正在保存更改”的轻量状态提示；完成后自动消失，避免远程等待被误认为页面卡住。
- 数据与依赖影响：无；仅增加前端状态反馈。
- 验证：待前端语法检查与正式发布后，在手机端验证加载和保存操作。

### 修复 iPad 与手机端客户页布局

- iPad 横屏不再套用宽桌面的多列客户表，改为紧凑列数；高级筛选改为两列，搜索与视图筛选分为更易触控的两行。
- 手机继续使用单列客户卡片和单列筛选，避免横向挤压。
- 数据与依赖影响：无，仅修改响应式样式。
- 验证：待使用 iPad 与手机尺寸预览并发布后确认。

### 收紧线上加载提示与页面操作菜单

- 移除全局“正在刷新/保存”提示；普通刷新仅在对应的客户或 Inbox 列表区域显示小型加载符号，避免干扰连续操作。
- 修复右上角快捷操作菜单：收起时不再露出叠在一起的玻璃卡片；再次点击、点击空白处、按 Escape 或切换页面都会收起。
- 数据与依赖影响：无，仅修改前端状态反馈与菜单交互。
- 验证：待前端语法检查、正式发布和手机端交互验证。

### 修复触屏客户列表误触与滚动冲突

- iPad 和手机上的客户卡片恢复自然上下滑动；手指滑动不再触发客户排序或拦截页面滚动。
- 客户排序保留给桌面鼠标和键盘操作；移动端仅通过卡片内明确的复选框、客户名称和操作按钮执行动作。
- 数据与依赖影响：无，仅调整触屏手势处理。
- 验证：待在 iPad 和手机上验证卡片区域滑动、选择与打开客户。

### 强制 iPad 采用触摸优先布局

- 针对 iPad Safari 可能提供桌面宽度的情况，按触摸能力而非屏幕宽度切换到触摸优先布局：抽屉导航、紧凑工具栏、受限内容宽度和可滚动筛选。
- 触摸设备上的客户卡片强制允许纵向滑动，禁用桌面拖拽逻辑；手机卡片限制为容器宽度，消除异常横向拉伸。
- 数据与依赖影响：无，仅修改响应式布局与手势判定。
- 验证：待正式发布后在 iPad 与手机的 Safari 中复核。

### 调整 iPad 导航与移除无效快捷菜单

- iPad 恢复常驻左侧工作栏；仅手机使用可收起的抽屉导航。
- 移除右上角没有实际价值的快捷操作菜单及其悬浮卡片，释放工具栏空间。
- 数据与依赖影响：无，仅移除前端低价值入口并调整响应式导航。
- 验证：待正式发布后在 iPad 和桌面端检查导航与顶部工具栏。

### 修复触控样式被旧桌面规则覆盖

- 将 iPad/手机的触控优先规则置于样式文件末尾并提高优先级，确保客户卡片的纵向滑动、iPad 常驻工作栏与手机宽度限制不会再被旧桌面样式覆盖。
- 数据与依赖影响：无，仅修复样式层叠顺序。
- 验证：待正式发布后在 iPad 和手机 Safari 重新加载页面验证。

### 增加 Mac 正式运行与自动启动准备文件

- 新增 macOS 正式运行脚本、LaunchAgent 模板和私有生产环境变量模板，为当前 Mac 作为主机的上线切换做准备。
- 生产脚本从项目内未提交的 `.env.production` 读取会话密钥、三位用户访问码和唯一 SQLite 数据目录，不在代码、LaunchAgent 或日志中保存这些值。
- 数据影响：无；当前本地启动器和正在运行的数据目录保持不变，正式服务只在最终切换时启用。

### 创建 Cloudflare Tunnel 与正式访问地址

- 已在 Cloudflare 创建当前 Mac 专用的 Trade OS Tunnel，并将 `app.trosa.space` 预留给该 Tunnel。
- 新增 macOS Tunnel 运行脚本与自动启动模板；Tunnel 只转发到本机回环地址 `127.0.0.1:8080`，不开放家庭网络端口。
- 数据影响：无；Tunnel 未启动前不会对外提供 CRM 数据。

### 上线前补齐生产访问码与周会数据保护

- 本地使用维持原有“选择账号即进入”，不影响当前三人的日常工作；设置 `CRM_ENV=production` 后，选择账号还需输入该用户独有的 6 位个人访问码。
- 生产启动时会校验三位访问码均已设置、为 6 位数字且互不重复；错误访问码连续失败 5 次后，该账号会冷却 15 分钟。访问码仅保存在服务器环境文件，不会发送给前端或写入 SQLite。
- 本周工作总览及其客户详情接口现在要求已登录，阻止未登录访问获取三位成员的汇总、客户索引和客户详情。
- 数据影响：无数据库迁移或业务数据改动；新增仅为会话鉴权与内存中的失败计数。
- 验证：待本次修改完成后执行 Python 回归测试、Python 编译和前端脚本语法检查。

### 明确 Mac 主机与备用机的容灾边界

- 将当前 Mac 定义为唯一日常写入主机；备用 Mac/NAS 作为冷备机，平时不运行应用、不同步活动数据库。
- 上线指南补充每日校验快照、外接 SSD 与异地副本、季度恢复演练和故障切换顺序。
- 数据影响：无；不创建第二个业务数据源。

### 修复 Mac 启动器在离线和解压后环境损坏时无法启动

- 启动器检测到虚拟环境损坏并重建后，会优先复用项目内保留的 Python 依赖目录；无需联网下载即可恢复启动。
- macOS 未安装 Chrome/Edge 时改用系统 `open` 命令启动默认浏览器，避免部分终端环境下的 AppleScript 语法错误。
- 保留在线安装路径，项目内没有可用依赖时仍会按 `requirements.txt` 安装，并在启动前验证全部运行依赖。
- 数据影响：无；仅重建可随时删除的 `.venv-mac` 运行环境，不改动 SQLite 业务数据。
- 验证：使用损坏虚拟环境复现场景，重建后完成依赖导入与桌面服务启动检查。

## 2026-07-31

### 全屏今日页改为客户档案工作面
- 1181px 以上将今日页重组为左侧跟进队列与右侧客户档案工作区，减少单纯放大列表造成的空白感。
- 客户档案集中展示当前等待、下一步、关键需求和最近发生，并提供记录、查看客户与访问官网的图标操作。
- 在档案底部加入完整的跟进输入区，记录工作可直接围绕当前选中客户展开。
- 906px 半屏维持原有的紧凑双栏布局，避免牺牲日常浏览密度。
- 数据影响：无，仅修改前端响应式布局、信息编排和交互入口。

### iPhone 单列布局与客户表头对齐
- iPhone 版将搜索栏、页面标题与待跟进列表分层排布，侧栏默认收起并以独立玻璃抽屉展开。
- 移动端任务行收紧为单列信息层级，减少重复文字，保留客户、下一步和日期三项重点。
- 客户页表头与数据行共用同一列栅格，并将表头整理为轻玻璃条，统一列宽与操作区比例。
- 数据影响：无，仅修改前端响应式样式和客户列表列宽计算。

### Inbox 与客户页补齐抽象树纹背景
- Inbox 和客户页加入与今日页同源的暖纸张与抽象树纹画布，统一三个主入口的空间氛围。
- 为高密度列表增加更厚的纸张遮罩，树纹保留在背景层，避免干扰表格和筛选阅读。
- 数据影响：无，仅修改前端背景层级与配色。

### 全屏采用紧凑木纹导航与堆叠操作卡片
- 桌面全屏和半屏统一使用 84px 图标导航轨道，让主要工作区保持紧凑比例。
- 侧栏加入中浅烟熏胡桃木纹理，保留清晰木纹并使用深墨色图标保证可读性。
- 页面操作按钮改为同尺寸、同颜色、同层级的液态玻璃卡片叠放；悬停、聚焦或点击时逐张向下落开。
- 数据影响：无，仅修改前端静态素材、布局、样式和菜单交互。

### 侧栏纹理由木材质调整为抽象树纹
- 使用暖象牙底色、年轮弧线、树枝分叉与低饱和配色线条替换写实木板纹理。
- 进一步降低侧栏对比度和阴影重量，使纹样作为轻背景存在，同时保持导航图标清晰。
- 数据影响：无，仅替换前端静态纹样和侧栏配色。

### 今日主页面改为暖色编辑型工作台
- 主背景加入低对比度纸张色洗素材，以暖米色、鼠尾草绿和陶土粉形成轻微层次。
- 重新分配左右栏宽度，扩大任务列表并稳定右侧下一步卡片；全屏与半屏均保持紧凑工作台比例。
- 将任务列表整合到统一的轻玻璃容器中，选中客户使用低饱和绿色光晕和纸张高光突出。
- 收紧页面标题、列表和详情卡片的垂直节奏，继续沿用复古衬线标题与商务化低饱和配色。
- 数据影响：无，仅修改前端背景素材、布局和视觉样式。

### 全屏工作台密度与背景字体联动
- 为 1181px 以上桌面端设置独立的大工作台比例，扩大内容宽度、任务列表、详情卡与文字尺寸。
- 加深背景纸张色洗的可见度，同时保持卡片和文字的可读性。
- 标题与客户名统一使用偏鼠尾草灰的暖墨色；辅助日期采用同系灰绿，任务状态延续陶土色。
- 数据影响：无，仅修改前端响应式布局、颜色和排版。

### 主画布增加抽象树纹对比版
- 以大尺度年轮弧线、枝桠分叉和低饱和线条替换纸张色洗，呼应紧凑侧栏的抽象树纹。
- 纹样集中在画布边缘，中心保留大面积安静区域供文字和液态玻璃卡片阅读。
- 保留原纸张色洗素材，便于后续继续比较或切换。

### 合并滚动工作栏与全局工具栏
- 停用旧的滚动 `compact-workbar`，移除其中重复的搜索、记录沟通和新增客户按钮。
- 顶部全局搜索与页面操作成为唯一的持久工具入口，页面滚动时不再出现第二套按钮。
- 桌面和半屏布局为固定工具栏预留 72px 顶部空间，页面标题及内容不会与搜索玻璃栏重叠。
- 数据影响：无，仅修改前端布局和滚动状态控制。

### 强化顶部液态玻璃与操作光标轨道
- 搜索栏、搜索预览和页面操作组件接入既有 SVG 折射滤镜，降低不透明底色并加强双层边缘高光、背景模糊和暖色系统内的轻微折射色散。
- 页面操作展开区改为一个连续玻璃容器，三个功能按钮统一图标颜色和透明底色。
- 展开区增加随鼠标与键盘焦点移动的玻璃光标滑块，光标在三个按钮之间平滑移动。
- 客户工作区标题、客户现状玻璃卡及常用分段选项同步接入同一折射材质。
- 数据影响：无，仅修改前端样式与顶部操作交互。

### 修复顶部搜索结果预览布局
- 将搜索图标按钮的尺寸规则限制到搜索栏直属按钮，避免错误覆盖结果卡片的宽度、内边距和高度。
- 搜索结果改为完整宽度的两行客户预览卡：客户名与国家标签同排，进入客户工作区的提示独占第二行。
- 结果面板与搜索栏保持一致的玻璃圆角、边缘高光和水平宽度。
- 数据影响：无，仅修改前端样式。

### 顶部搜索玻璃栏与向下展开操作按钮
- 全局搜索改为页面顶部水平居中的常驻长条玻璃栏，在窄屏与桌面端均保留输入区域和自动结果预览。
- 当前页面操作入口固定在右上方，子操作改为与入口同尺寸、同圆角、同玻璃材质的独立按钮，并沿入口正下方纵向展开。
- 数据影响：无，仅修改前端布局与样式。

### 全站图标升级为 Phosphor
- 引入 Phosphor Icons 2.1.1 Regular，并将所需 SVG 与许可证保存在项目静态资源中，8081 离线或内网环境也能稳定显示。
- 统一搜索、页面操作、导航、状态、编辑、导出、删除、刷新等高频图标的造型、视框和视觉线宽。
- 侧边导航与顶部全局工具优先完成替换；页面操作入口会随当前页面切换为对应图标，同时保留悬停提示与无障碍名称。
- 数据影响：无，仅修改前端静态资源、结构与样式。

### 全站加入液态玻璃搜索与页面功能切换框

- 在 8081 正式预览界面加入常驻全局搜索框，可从任意页面输入客户、联系人或沟通内容并进入客户搜索结果。
- 搜索输入时自动显示最多 5 条客户预览，支持方向键切换；回车或点击结果会直接打开对应客户工作区，没有匹配预览时回车进入完整搜索结果。
- 新增随当前页面变化的功能切换框；今天、Inbox、客户、本周工作、日历、新客户、记录和设置分别提供对应操作，桌面端悬停或点击后向下展开。
- 展开后的页面操作恢复为统一线性图标按钮，操作名称通过悬停提示与无障碍标签提供。
- 顶部功能切换入口移除“今天的操作”等可由图标表达的文字；搜索入口默认收起为图标，聚焦后展开输入区域，并移除输入框占位说明。
- Inbox 筛选、客户视图及其他分段选择控件统一采用厚边缘液态玻璃材质；配色使用系统既有的暖纸色、透明象牙白和少量陶土色边缘强调，选中项通过内高光区分。
- 桌面端原页面快捷架由全局工具替代，移动端保留底部安全区布局。
- 数据影响：无，仅修改前端结构、样式与交互。
- 验证：浏览器检查全局搜索、页面切换菜单内容和向下展开状态；静态脚本语法检查通过。

## 2026-07-30

### 修复客户详情延后跟进后仍显示在今日待办

- 问题：在客户详情右侧点击「3天后」延后下一步后，提醒日期已经更新，但背景中的「今日待办」列表没有同步刷新，客户仍显示在今日待办中。
- 修复：延后操作完成后同时刷新客户详情和工作台待办数据；日期改用本地日期格式生成，避免时区转换造成日期偏移。
- 数据影响：仅更新既有 `reminders.remind_date` 与 `customers.next_follow_up`，无表结构或历史沟通记录变更。
- 验证：`app.py` Python 编译通过；完整回归测试依赖当前环境缺少 `apscheduler`，未执行。

### 修复快速编辑设置跟进日期后客户仍留在今日待办

- 问题：在「今天」栏多选客户后打开「快速编辑选中客户」弹窗，设置「下次跟进日期」为未来日期后，客户仍保留在今日待办中。
- 根因：`batch_update_next_follow_up` 只为每个客户创建/合并新日期的 `follow_up` 提醒，但没有关闭此前把客户留在今日待办里的旧未完成 `follow_up` 提醒，导致 `/api/reminders/today` 仍把它们查出来。
- 修复：在创建新提醒前，先关闭这些客户所有未完成的 `follow_up` 提醒（`UPDATE reminders SET is_done=1 ... WHERE reminder_type='follow_up'`），与单客户编辑 `update_customer` 的行为一致。设置未来日期后客户离开今日待办，设置今天/过去日期则客户进入今日待办。
- 数据影响：仅状态字段更新，不改变表结构。

### 快速编辑支持批量填写跟进记录

- 在「快速编辑选中客户」弹窗新增「批量填写跟进记录（可选）」分区，包含沟通方式、信息方向、跟进内容、结果或判断四个字段。
- 填写跟进内容后提交，会为每个选中客户创建同一条 `follow_up_logs` 记录，并完成该客户最近一条 `remind_date <= 今天` 的未完成 `follow_up` 提醒，使客户离开今日待办。
- 新增后端接口 `POST /api/customers/batch/follow_history`，行为与单客户 `POST /api/customers/<id>/follow_history` 对齐：创建记录、完成对应提醒、按剩余未完成提醒刷新 `customers.next_follow_up`、把 `last_contact` 推进到沟通日期、`未建联` 状态自动转为 `跟进中`。
- 与既有字段（等级/状态/下次跟进日期）可组合使用；只填跟进内容、不设下次跟进日期也可单独提交。
- 数据影响：写入 `follow_up_logs`、更新 `reminders.is_done` 与 `customers` 表，无表结构变更。

## 2026-07-29

### 设置中加入界面字号调整（按档位调优显示）

- 在「设置 → 个人使用设置」卡片中新增「界面字号」分组，提供小 / 标准 / 大 / 超大 四档，切换即时预览，保存后跨会话生效。
- 实现方式不是简单改根字号等比缩放，而是通过 `html.font-size-{small|standard|large|xl}` class 对每档做专门优化：
  - **小（13.5px）**：侧栏收窄到 220px、卡片/页面 padding 收紧、today-workspace 右栏减到 320px，提升信息密度；hero 字号压缩比例大于正文避免标题过大。
  - **标准（15px）**：默认值，无额外覆盖。
  - **大（16.5px）**：侧栏加宽到 264px、today-workspace 右栏加到 420px 防溢出；模块/客户字段网格从 3 列降为 2 列。
  - **超大（18px）**：侧栏 280px；today-workspace、表单 form-row、设置网格全部改为单列；stats-grid 降为 2 列；客户工作台摘要从 4 列降为 2 列。
  - 每档独立重塑 `--fs-display-xl` 至 `--fs-micro` 整套 type scale token，hero 与正文按不同比例缩放，保持视觉层次。
- 后端 `_default_user_preferences`、`_load_user_preferences`、`_sanitize_user_preferences` 同步增加 `font_size` 字段并做白名单校验（仅接受 `small/standard/large/xl`，其他值回退为 `standard`）。
- 数据影响：`user_preferences` JSON 新增一个字符串字段，无需数据库迁移；旧数据缺该字段时按 `standard` 处理。
- 兼容：半屏及以下（≤1100px）字号档位只改 type scale token，不改侧栏宽度与栅格列数，沿用 821-1100px 折叠侧栏断点，避免大字号侧栏盖过响应式布局导致错乱。
- 验证：`app.py` 通过 `ast.parse` 编译检查；服务进程已重启加载新代码；浏览器手动验证四档切换即时预览、保存后刷新仍生效、半屏下各档布局无溢出。

### 今天栏客户名快捷进入操作界面

- 在「今天」栏待跟进列表中，将客户名从纯文本 `<span>` 改为 `<button>`，点击客户名即直接打开客户操作界面（`openEditModal`），无需先选中再点右侧/下方聚焦卡片的「查看客户」。
- 点击客户名时 `event.stopPropagation()` 阻止冒泡，避免同时触发行选中；点击行内其他区域仍保持原有的选中并展示聚焦卡片行为。
- 同步更新 `style.css` 与 `visual-v2.css`，为 `button.today-task-customer` 添加按钮重置样式（去边框、去背景、继承字体）与 hover 变色，保持与原 span 视觉一致。
- 数据影响：无。
- 验证：浏览器手动验证点击客户名打开客户详情、点击行其他区域仍正常选中。

### Agent 工作接口与确认式写入

- 新增面向 Agent 的今日工作简报：`GET /api/agent/brief/today` 聚合到期待办、待判断 Inbox 与未来 7 天安排，支持 JSON 与 `?format=markdown` 两种输出。
- 新增单客户工作区：`GET /api/agent/customers/<id>/workspace` 返回客户事实、联系人、未完成待办、最近沟通、开发邮件、现有工作理解与信息缺口，明确标注写入前须先创建提议。
- 新增确认式写入流程：Agent 可通过 `POST /api/agent/proposals` 创建待办或沟通记录提议；只有调用 `POST /api/agent/proposals/<id>/confirm` 后，内容才会写入 `reminders` 或 `follow_up_logs`。每次提议和确认均进入操作日志。
- 数据影响：每个用户的业务数据库新增 `agent_proposals` 表，用于保存待确认、已确认和已取消的提议；不改变既有客户、待办或沟通记录数据。
- 验证：Python 编译通过；回归测试覆盖工作简报、客户工作区、提议确认写入，以及 Inbox 安排下一步后离开列表，共 2 项通过。

## 2026-07-28

### 沟通记录高亮（记号笔）功能

- 在"编辑跟进记录"模态框中新增高亮工具栏，支持黄/绿/粉 3 色文本高亮与清除，像记号笔一样在沟通内容、结果、下一步计划三个字段上自由标注。
- 三个字段从 `<textarea>` 改为 `contenteditable` 富文本区域，可直接渲染并编辑带 `<mark>` 标签的 HTML。
- 高亮数据以富文本 HTML 形式存入 `follow_up_logs.content / result / next_plan` 字段（字段类型 TEXT，无需迁移）。
- 后端新增 `sanitize_mark_html()` 白名单过滤函数：仅保留 `<mark class="hl-yellow|hl-green|hl-pink">`，其他 HTML 标签（`<script>`、`<img onerror>` 等）全部转义为纯文本，防止 XSS。接入 `update_follow_history`、记录客户回复、完成任务、记录沟通 4 个写入入口。
- 前端新增 `renderRichText()` 渲染函数：与后端 sanitize 逻辑对齐，仅渲染合法 `<mark>` 标签，其他标签转义显示。替换了客户详情 timeline、历史页 timeline、周报本周记录、周报历史脉络 4 处展示点的 `escapeHtml()` 调用。
- 修复前后端双重转义问题：`sanitize_mark_html` 和 `renderRichText` 均不转义 `&`（输入已是浏览器转义后的 entity），避免 `&amp;amp;` 双重转义导致用户看到 `&amp;` 而非 `&`。
- 字体优化：在 `visual-v2.css` 中新增标题层级（`--fs-title-l/m/s`）、行距（1.65）、字距（0.005em）规范，强化 h2/h3/h4 与正文的对比；为 `time`、`.timeline-date`、`.stat-value` 等数字/日期元素统一 `font-variant-numeric: tabular-nums`，让对齐更整齐。
- 新增 3 色 mark 样式（黄/绿/粉），使用 `linear-gradient` 实现记号笔效果（下半部填充），适配深色 hero 区。
- 数据/依赖影响：仅修改 `app.py`、`app/static/app.js`、`app/static/index.html`、`app/static/visual-v2.css`，不涉及数据库结构变更。旧数据（纯文本）兼容，无需迁移。
- 验证：前后端 sanitize/render 逻辑经单元测试验证——合法 mark 保留、script/img onerror 转义、非法 class 修正为 yellow、`&` 不双重转义、纯文本无影响。

### 今天列表支持多选客户与批量操作

- 「今天」页面的待跟进列表支持多选客户：桌面端悬停行时序号位置浮出复选框（序号淡出、复选框淡入）；触屏端长按行 500ms 进入多选模式，所有行的序号变为复选框，点行切换选中，底部批量栏出现。
- 列表下方新增批量操作栏（选中后出现）：「导出邮箱」复用 `doExportEmails`，将所选客户的联系人邮箱去重后复制到剪贴板；「快速编辑」打开新弹窗 `todayQuickEditModal`，可一次性给选中客户设置等级、状态或下次跟进日期，每项均可选「不修改」保留原值。
- 快速编辑复用现有 `/api/customers/batch/level`、`/api/customers/batch/status`、`/api/customers/batch/next_follow_up` 三个批量接口，不新增后端路由。
- 视觉：不新增栅格列，复选框与序号共享 32px 列（`.today-task-index-wrap` 容器，复选框 `position:absolute` 叠加在序号上）；`is-selection-mode` 类控制触屏多选模式下的全局显隐，`is-multi-selected` 类控制单行已选状态；批量栏使用 `--v2-clay-soft` 配色。
- 长按检测集成在 `initTodayTaskSorting` 的 pointerdown 中，仅 `pointerType==='touch'` 时启动 500ms 计时器；pointermove > 7px 或 pointerup 取消计时器，与已有拖拽逻辑互不干扰。
- 数据/依赖影响：仅修改 `app/static/index.html`、`app/static/app.js`、`app/static/visual-v2.css`，不涉及数据库结构或新依赖。
- 验证：前端 JS 语法检查通过。

### 液态玻璃效果扩展到核心常驻表面

- 将 SVG 液态滤镜 `tradeOsLiquidGlass`（feTurbulence + feDisplacementMap 折射）从仅 compact-workbar 一处扩展到三个常驻浮层：侧边栏、顶部 command-shelf、AI 询问面板。
- 三个表面的 `backdrop-filter` 在原有 `blur + saturate` 基础上叠加 SVG 滤镜与 `brightness(1.02~1.04)`，呈现背景轻微折射扭曲的液态质感；同时把顶边 inset 高光从 .88/.94 提到 .92/.96/1，强化玻璃边缘感。
- modal-overlay 背景虚化从 `blur(9px)` 加到 `blur(16px) saturate(108%)`，让前景模态框的玻璃层次更突出。
- 保留 `@supports not (backdrop-filter: url(...))` 降级分支并补齐三个新表面的兜底规则：不支持 SVG filter 的浏览器回退到普通 `blur + saturate` 毛玻璃，视觉差异减弱但不会失效。
- 数据/依赖影响：仅修改 `app/static/visual-v2.css`，不涉及数据库、后端逻辑或业务数据。
- 验证：浏览器实测侧边栏、command-shelf、ai-ask-panel 三个表面均可见液态折射，滚动时背景文字被轻微扭曲；未出现花屏、闪烁或卡顿；Chrome/Safari 均正常支持，未降级。

### Inbox 按待办动作分组重组

- Inbox 不再按客户个体逐条平铺，改为按待办动作/情况分组：客户有新回复、待二次开发、新客户待跟进、新客户待联系、重点客户待安排、有分析待跟进、长期沉默、其他建议。
- 每组显示标题与计数，组内每个客户默认单行摘要（客户名 + 一句话原因 + 主操作按钮），点击展开完整详情（事实依据、上次判断、完整分析、其他操作按钮）。
- `ai_suggestion` 按 `dedupe_key` 里的 `signal_version` 前缀拆成语义子类，复用现有数据，不修改后端、不修改数据库结构。
- 现有 tab 筛选（全部/客户回复/未联系跟进/AI 建议）保留不变。
- 权衡：渲染从增量改为全量重建，展开/折叠时整个列表重绘；Inbox 非高频更新，影响可接受。
- 验证：前端 JS 语法检查通过。

### Inbox 分组批量操作

- 每个分组标题右侧新增「全部今天跟进」「导出邮箱」两个批量按钮，对整组客户同时生效。
- 「全部今天跟进」调用新增的 `/api/customers/batch/next_follow_up`，批量设置 `next_follow_up` 为今天并标记 `manual_next_follow=1`，客户会出现在新池的「今日」分组。
- 「导出邮箱」复用现有 `doExportEmails`，传入该分组的全部客户 ID。
- 验证：Python 编译通过，前端 JS 语法检查通过。

### Inbox 分组内按国家细分

- 每个动作分组内再按国家聚成子组（按数量降序，无国家归入「其他」放最后），批量操作按钮从动作分组下移到国家子组。
- 「今天跟进」「导出邮箱」现在按国家隔离生效：同一动作下，中国客户与美国客户分别批量处理，避免用同一封开发信群发不同国家。
- 国家名通过 `encodeURIComponent` 传递，避免 Côte d'Ivoire 等含单引号的国家名破坏 HTML。
- 验证：前端 JS 语法检查通过。

### 工作空间页面切换动画与排版稳定性优化

- 统一今天页与其他页的 h2 字号：今天页从 3.65rem 降到 2.45rem（桌面）/ 2.15rem（620px），与 Inbox、客户页完全一致，切换时标题字号不再变化。
- 移除 sidebar 的 `view-transition-name`，让侧边栏（含左下角设置按钮）完全不参与页面切换动画，位置绝对静止。
- 统一今天页与默认页的 `page-header` / `page-body` padding：今天页 header padding-top 从 34px 调到 42px，body padding 从 42px 0 58px 调到 28px 0 64px，消除切换时容器级跳变；移动端 820px/620px 断点同步统一 today 与其他页的 padding-top（70px / 28px）。
- 保留 `trade-os-page-title` 和 `trade-os-active-nav` 的 view-transition-name，让标题文字和导航选中指示器平滑 morph；优化 main-content 进入/退出动画位移（6px → 3px）和时序（150ms → 200ms）。
- 数据/依赖影响：仅修改 `app/static/visual-v2.css`，不涉及数据库、后端逻辑或业务数据。
- 验证：浏览器实测三页 h2 字号一致（32.25px），sidebar viewTransitionName 为 none，设置按钮位置切换时不动，整体流畅度 8/10。

### 全站字号层级统一与侧边栏可读性优化

- 在 `visual-v2.css` :root 中新增字号层级 token（`--fs-display-*` / `--fs-title-*` / `--fs-body*` / `--fs-label` / `--fs-caption` / `--fs-micro`），最小可读字号统一拉到 12px 起跳，避免再散落 0.6rem/0.65rem 这类过小值。
- 侧边栏宽度从 222px 加宽到 244px，主内容区左边距同步从 252px 调整到 274px；导航项字号从 15px 调到 16.2px，分区标题从 9.75px 调到 11.7px，计数徽章、用户身份、工具菜单字号统一上调一档。
- 当前选中的导航项新增 3px 粘土色左侧竖条指示器（`inset box-shadow`），强化当前位置感知。
- 顶部工作栏、today 任务行、focus 卡片、表格表头、表单 label、模态框 tab、AI 询问面板、徽章、时间线日期、日志时间等全站小字号统一走 token 兜底，不再依赖散落的硬编码 rem 值。
- 数据/依赖影响：仅修改 `app/static/visual-v2.css` 与 `app/static/index.html` 的 CSS 版本号（motion14 → motion15），不涉及数据库、后端逻辑或业务数据。
- 验证：浏览器实测登录页、主界面侧边栏、今天页、客户列表、客户详情模态框，字号层级清晰、244px 侧边栏在 1440px 视口下无文字截断、active 指示器正常显示。

### 新池「待二次开发」分组

- 新池页面新增「待二次开发」分组，列出 `customer_type='new'` 且已发开发信但 `reply_status` 为 `pending`/`no_reply`、且发信满 14 天的客户，按发信天数从长到短排序，方便快速识别该做二次开发跟进的客户。
- 表格显示客户、公司、国家、类型（中间商/终端）、等级、发信天数，支持全选与导出邮箱；具体跟进内容仍由用户逐个决定，不引入批量写入或群发。
- 后端 `/api/customers` 新增 `view=secondary_dev`，并在返回中补充 `latest_outreach_reply_status` 与 `days_since_outreach` 字段；不修改数据库结构，不影响现有 `uncontacted` 等 view 的行为。
- 验证：Python 编译通过，前端 JS 语法检查通过。

### 沟通脉络可视化

- 时间线的沟通方向改为低饱和色彩标签：我方发出为蓝色、客户来信为绿色、双方沟通为紫色、方向不明确为灰色。
- 客户现状中的“当前等待”同步使用状态色，帮助快速区分等待客户回复、近期无需求和持续观察。
- 方向标签提供说明：它用于快速阅读沟通脉络，并为后续待办与提醒的判断提供依据。

### 沟通方向识别

- 自动识别新增对 CRM 摘要句式的本地规则，可识别“客户回复/表示/要求”“向客户提供/询问/跟进”以及双方同时出现的记录。
- 历史记录中尚未写入方向的数据会在时间线展示时按同一规则判断；含义仍不明确的内容显示为“方向不明确”，任务完成记录不再显示沟通方向。
- 无需模型、无需数据库迁移；新增回归测试覆盖客户来信、我方发信、双方沟通和无法判断四类内容。

### 产品文档与数据架构口径收敛

- 新增根目录 `README.md`，以当前运行代码为准说明核心闭环、项目结构、文档职责和验证入口。
- 产品口径、设计标准、系统设计、使用说明、上线指南与 Git 说明统一为“本地 SQLite 唯一数据源 + 可验证快照恢复”；Excel 明确为导入导出格式，Apple 日历明确为只读 ICS 订阅。
- AI 调整为可关闭、按需触发的辅助层；文档明确未配置模型时客户、记录、待办、Inbox、日历和数据安全功能完整可用。
- 删除 `db.py` 中没有调用方的旧 iCloud 数据库复制接口，当前启动、关闭和后台任务均不再包含 iCloud 拉取或推送。
- 为 2026-07-23 重构审查报告补充历史状态说明，并在协作规则中加入事实源、同步用词、AI 边界和数据单源约束。
- 数据影响：没有修改数据库结构或业务数据；后续多设备使用以访问同一部署服务为推荐路径。
- 验证：全项目文本复查未发现现行文档继续宣称 iCloud 数据库同步；Python 编译通过，9 项风险回归测试通过，前端 JavaScript 语法检查通过。

### Motion continuity, interaction, and QA instrumentation

- Customer, Inbox, and Today lists now reconcile stable row keys during refreshes. Existing rows retain their visual position, changed ordering uses a short transform-based reflow, and delayed loading feedback avoids distracting flashes on fast responses.
- Modal, AI panel, and mobile navigation interactions now keep keyboard focus predictable: opening moves focus to an actionable control, closing returns to the originating control when available, and Escape closes the active overlay or mobile navigation.
- Added opt-in `?motion_debug=1` runtime diagnostics for QA. It records frame intervals and long tasks without running in ordinary user sessions.
- Added a diagnostic-only `motion_rows` path that exercises 20, 100, and 300 real customer records. Cold list creation uses one batched parse up to 100 rows and frame-sliced insertion beyond that threshold; column visibility now switches at table level instead of touching every cell.
- Added automatic lightweight materials for constrained devices plus `motion_lite=1|0` QA overrides. Reduced-motion, slow-update, and reduced-transparency fallbacks remain available.
- Customer and Inbox lists now render an inline failure explanation with a retry action. Mobile customer filters keep horizontal touch scrolling while hiding the native scrollbar.
- Verification: JavaScript syntax and all 9 Python regression tests pass. Browser regression confirmed AI focus transfer/return, modal and mobile-nav Escape behavior, 20/100/300-row performance, customer and Inbox error states, customer empty state, responsive layouts, lightweight materials, zero page-level horizontal overflow, and no application console warnings or errors.

### 动效系统与渲染性能第一轮优化

- 新增统一动效 Token，页面切换、导航选中、弹窗、Toast、按钮按压与 Inbox 筛选使用一致的 160–280ms 节奏和低位移动画。
- 页面内部导航接入 View Transition，并保留无支持浏览器的轻量 CSS 回退；导航选中项与普通页面标题保持位置连续。
- 弹窗增加简短的进入和退出动画，快速重复打开时会取消尚未结束的关闭计时，避免弹窗被旧动画误关。
- 删除玻璃面板的鼠标光斑追随，客户表格单元格停止实时背景模糊；AI 面板、命令架、遮罩与拖动状态的模糊强度已降低，拖动过程只保留位置变化。
- 新增减少动态、低刷新设备和减少透明度模式；这些模式会关闭非必要位移、缩放和高成本玻璃材质。
- 新增 `MOTION_OPTIMIZATION_PLAN.md`，记录后续列表连续性、性能录制、视觉验收标准与完成定义。
- 数据与依赖影响：不修改业务数据、数据库结构或第三方依赖。
- 验证结果：JavaScript 语法检查和 9 项 Python 回归测试通过；已在运行页面验证导航、Inbox 筛选、弹窗开关、最终计算样式与控制台状态。

### 显示精简与动画干扰修复

- 客户表格单元格移除 `backdrop-filter: blur(15px) saturate(1.04)` 与 380ms 过渡，hover 不再拖泥带水，滚动与悬浮渲染成本下降。
- 移除 `customer-row-priority.is-settling td` 上的 `customerMaterialSweep` 动画：原先 0% `opacity:0` 会让整行 td 内容在 650ms 内消失，拖拽排序时内容不可读。
- Today 列表 `customer-light-on::after` 扫光动画时长从 650ms 缩短到 320ms，光带透明度从 .66 降到 .35，降低对内容的视觉干扰。
- 缩短多组过长动画时长，统一到 200–320ms 节奏：`customer-light-off` 340ms→200ms、`customerLightPress` 430ms→240ms（缩放幅度从 .78/1.09 收敛到 .88/1.04）、`todayRankChanged` 620ms→320ms（scale 1.22→1.1）、`todayTaskDropPulse` 520ms→260ms、`customerPriorityDropPulse` 520ms→260ms、`progress-fill` width 0.6s→0.3s。
- 批量操作条 `batch-bar` 改用 0.15s 纯 fade 动画，不再借用 `modalIn` 的 8px 位移动画。
- 数据与依赖影响：仅修改 `app/static/style.css`，不涉及数据库结构、业务数据或第三方依赖。
- 验证结果：CSS 诊断无错误；逻辑改动均落在动效与显示层，业务回归测试不受影响。

## 2026-07-27

### Customer light and Today sorting refinement

- Fixed customer-card dragging after the Grid conversion: the floating card now retains the workspace column definition while moving, so its width and fields stay aligned. Dragging begins only after a short movement threshold, matching Today and preventing a simple press from leaving a card in a stuck drag state.
- Merged the Today lamp and sorting grip into one control: press to toggle the lamp; press and move to reorder. Keyboard lamp toggling and arrow-key ordering remain available.
- Expanded the lit state into a low-saturation mist-green card glow and fixed the date rail width so date length no longer crowds customer details.
- Removed the drag placeholder outline and lamp-ring guide. Today cards can now be dragged from the card body; the lamp keeps its tap-to-toggle behavior.
- Removed the remaining grab styling from the lamp so it reads purely as a light switch; dragging begins from the card body.
- Applied the same interaction to the customer table: removed its six-dot drag control, made the lit customer row itself draggable, and removed the table drag placeholder outline.
- Reworked the customer-list visual hierarchy to match Today: unlit customers merge into a quiet, lightly desaturated list; lit customers receive a full-row mist-green glass glow.
- Corrected the customer-table implementation after visual QA: removed residual per-cell card shadows and made the drag ghost reuse the exact visible column widths.
- Rebuilt the customer drag preview as a fixed-width shell containing a full customer-table clone, preventing detached table reflow from distorting the card during drag.
- Replaced the heavy drag-table clone with a fixed-width lightweight preview containing only the lamp and customer name; pointer tracking now uses GPU transforms and reordering work is throttled to one pass per animation frame.
- Rebuilt the customer workspace from table rows into CSS Grid work cards. The card list preserves column scanning, filters, batch selection and actions while reusing the Today-style original-card drag, auto-scroll, reflow and landing motion.

### 客户灯光交互原型

- 客户灯光实验页重做为“液态玻璃提醒卡片”方向：客户资料保持纸张内容层，点亮后出现可展开的玻璃提醒卡片并进入独立的今日推进排序队列；未接入正式 CRM 功能。
- 新增极简客户关注排序原型：去除状态文字和提醒卡片，仅以嵌入式玻璃标记、置顶分组与六点拖拽手柄表达关注与排序；普通客户降低对比度以让重点客户自然前置。
- 新增从零构建的 Liquid Glass 客户排序原型：移除复古纸张、灯光与黄铜元素，使用环境染色背景、厚玻璃置顶客户层、简洁玻璃控制件和无文字拖动排序反馈；未接入正式 CRM 功能。
- 将 Liquid Glass 原型调为更温润、低科技感的中性配色：以暖灰、苔绿、陶土与雾蓝环境色取代蓝紫霓虹；新增按压、材质高光扫过以及客户置顶/排序时其他行连续让位的缓动动画。
- 数据与依赖影响：无业务数据、数据库或第三方依赖变更。
- 验证结果：已完成本地 HTML 与交互脚本检查；正式界面将在视觉方向确认后再接入。

### 客户关注标记正式接入

- 将液态玻璃客户标记接入“今天”和“客户”界面；已标记客户在客户列表自然置顶，普通客户降低饱和度与对比度，Inbox 不显示该标记。
- “手动排序”视图为已标记客户提供六点拖拽把手；拖动时保留玻璃悬浮卡、占位反馈和连续让位动画，同时支持键盘上下方向键排序。
- 修复客户列表重点态使用表格行伪元素造成的列宽异常与背景越界：玻璃材质改由各单元格承载，保持原有列宽、可见列设置与操作区布局稳定。
- 客户列表的所有条目统一采用暖色液态玻璃层；普通客户保留较低对比度，重点客户使用更厚、更亮的玻璃材质。已标记客户的拖拽把手改为在常规客户列表和“手动排序”视图都可用。
- 数据与依赖影响：复用 `customers.is_pinned`、`pinned_order` 与 `pinned_at`，无需数据迁移或新增第三方依赖。
- 验证结果：JavaScript 与 Python 静态检查、重点客户排序接口回归测试通过。

### Mac 启动器离线依赖恢复

- 修复 Mac 启动器在虚拟环境被解压工具损坏后、又遇到网络或 DNS 暂不可用时无法启动的问题：重建运行环境后会复用同项目旧环境中仍完整的 Python 依赖，并验证关键模块后再启动。
- 数据与依赖影响：不修改 CRM 业务数据、不新增第三方依赖；仅在在线安装失败且本机存在旧环境依赖时复制可再生成的运行依赖。
- 验证结果：模拟无网络安装失败，确认 Python 3.12 可加载 Flask、Waitress、APScheduler、Requests、OpenPyXL、Email Validator 与 DNS 依赖。

### 客户颜色标记与手动排序

- 客户列表使用一个颜色圆点标记需要保持在前方的客户，不增加客户等级、原因、期限或名额等额外分类。
- 颜色标记提高饱和度并整合到客户名称左侧；移除顺序编号和上下箭头，改为与“今天”栏一致的六点拖动手柄，同时保留键盘方向键排序。
- 颜色标记操作兼容 POST 与 PUT 请求，避免部分本地运行环境拦截 PUT 后出现“方法不允许”。
- 修复客户拖动排序的命中问题：拖动中的行会暂时让出指针命中，使其能准确识别下方的目标客户并完成重排。
- 修复客户拖动排序未结束的问题：将移动和松开事件改由页面级监听处理，确保鼠标、触控板和触屏松开后都会保存排序。
- 客户拖动排序改为复用“今天”栏的视觉语言：拖起时显示悬浮卡片，目标位置显示高亮占位，其他客户平滑让位，落下时回弹确认。
- “颜色标记”升级为统一的客户灯光状态：今天、Inbox、客户列表和客户详情均显示同一盏灯；已照亮客户带暖光，未照亮客户降低少量视觉对比度，各处均可直接开关。
- 客户灯光改为复古极简的嵌入式玻璃指示灯：按压灯罩后灯芯点亮，再向整张客户卡扩散低饱和墨绿灰光；任务列表固定为五列，避免灯光控件挤乱日期和客户内容。
- “手动排序”视图集中显示带颜色标记的客户；顺序保存在当前用户数据库中，不会因其他客户产生新跟进或更新时间变化而被打乱。
- 数据与依赖影响：每个用户数据库的 `customers` 表新增 `is_pinned`、`pinned_order`、`pinned_at` 三列及对应查询索引；不修改已有客户资料，无新增第三方依赖。
- 验证：Python、JavaScript 静态检查及重点客户接口回归测试。

### 数据安全与日历订阅修复

- 日常运行加固：恢复快照会等待正在处理的请求结束、暂停定时任务并在恢复后重新启动；定时任务固定使用 Asia/Shanghai，单个用户的过期背调检查异常不会阻塞其他用户。
- 日常运行加固：本地启动遇到数据库初始化失败会退出；Excel 同步只接受当前用户已上传的文件；端口占用进程只会在显式设置 `CRM_ALLOW_PORT_TAKEOVER=1` 后终止。
- 首次启动默认不再向 Hamid 工作区写入演示客户、提醒或跟进记录；仅在显式设置 `CRM_SEED_DEMO_DATA=true` 时生成演示数据。
- 备份恢复仅显示并接受带完整清单的快照；恢复前校验校验和与 SQLite 完整性，先创建 `before_restore` 安全快照，再以干净 SQLite 文件替换目标数据库并移除旧 WAL/SHM 伴随文件。
- 日历订阅将 CRM 保存的 Asia/Shanghai 时间正确转换为 UTC 标记；全天任务的日期保持不变。
- 配置中的存储和邮件验证数值环境变量会在启动时提供清晰的有效范围错误。
- 数据与访问影响：未修改现有业务数据；总览、周报和总览客户详情继续保持公开访问。
- 验证：新增隔离数据库、备份校验/恢复、日历时间和公开总览访问回归测试，并运行 Python 与 JavaScript 语法检查。

## 2026-07-26

### 暂停线上测试，恢复局域网运行

- 已将当前线上测试版本（不含 `.env` 中的密码和密钥）保存为 `archive/trade-os-online-test-20260726.tar.gz`，SHA-256 为 `8553530235de5df707fa8b23434cfbf4ef6b25aba75b8ffd7ede00947befa1dc`。
- 主程序恢复局域网入口 `0.0.0.0:8080` 与“选择账号进入”界面；不再启用六码自动登录流程。
- 数据与依赖影响：不修改客户数据库。线上部署文档、Waitress 入口和 Tunnel 配置示例保留在项目中，供后续恢复实验时使用。
- 验证结果：Python 与 JavaScript 语法检查、局域网登录接口和首页加载检查通过。

### Mac 启动器解压后无法打开修复

- 修复 `Mac启动器.command` 在解压后丢失可执行权限、Finder 提示“没有正确的访问权限”的问题；启动器现已恢复为可执行文件，并移除当前文件的解压隔离标记。
- 启动器检测到解压后残留的不完整 `.venv-mac` 时，会完整重建这个可再生成的运行环境，避免 Python 的 `--clear` 在目录结构损坏时失败。
- 数据与依赖影响：不影响 CRM 数据、配置或运行依赖。
- 验证结果：已确认启动器具备所有者可读、可写、可执行权限；已模拟不完整运行环境并完成重建。

## 2026-07-25

### 客户列表、Inbox 与总览性能优化

- 客户列表的默认视图改为由数据库先完成计数与分页，再补充当前页的联系人、提醒和最近沟通信息；页面每次显示 30 位客户时不再加载并加工全部客户。
- Inbox 导航角标新增轻量计数接口。完整 Inbox 结果会在服务端短时缓存，数据写入后立即失效；后台刷新角标不再每分钟下载和重算完整 Inbox。
- 周会总览的客户索引改为只保留搜索和列表所需字段；客户简介、备注、网站等详情内容在点击该客户时按需获取。
- Today 与未来提醒的补充信息改为按客户批量读取最新沟通、开发信和调研记录，消除每条提醒分别查询三次数据库的 N+1 路径。
- 数据与依赖影响：现有三个用户数据库启动时会新增 reminders、follow_up_logs、outreach_emails、inbox_items、web_monitor_logs 的查询索引；无数据删除、无第三方依赖变化。
- 验证结果：Python 编译检查和前端 JavaScript 语法检查通过；以当前 Hamid 数据复测，客户默认列表约从 148ms 降至 29ms，总览响应从约 664KB 降至约 199KB，Inbox 角标缓存命中时约 1.4ms、响应约 90B。

### 启动时本地存储维护

- 应用每次启动后会自动收敛超出保留策略的自动数据库快照，清理超过 45 天的非权威上传文件，并在超过 14 天后删除表格视觉审计临时目录。
- 近期每天保留最多 12 份自动快照，较早日期每天保留最多 3 份，90 天以上的自动备份目录会移除；手工命名备份、恢复前备份、数据库、源代码与 `authoritative_` 上传文件不参与自动删除。
- 新增 `maintenance.py`，可单独执行与启动时相同的保守清理。
- 数据与依赖影响：仅删除符合保留策略的可再生成文件，不修改 CRM 业务数据或新增依赖；可通过 `CRM_BACKUP_RETAIN_DAYS`、`CRM_UPLOAD_RETAIN_DAYS` 等环境变量调整保留期。
- 已将未被当前应用引用的表格审计临时目录、嵌套旧项目副本和解压元数据移入系统废纸篓，可在确认后由用户永久清空。
- 验证结果：启动维护函数与独立脚本均可执行；已收敛 196 个过量自动备份目录并释放项目目录约 1.46GB，当前应用数据目录与数据库完整保留；启动日志会记录删除数量及释放空间。

## 2026-07-23

### Inbox 客户详情与邮箱导出入口

- Inbox 卡片中的客户名称现在可直接打开对应客户详情；没有关联客户的卡片保持普通文本。详情标题区提供“导出邮箱”，可一键复制该客户全部去重联系人邮箱。
- 数据与依赖影响：仅调整前端交互与样式，不修改接口、数据库或依赖。
- 验证：已完成 JavaScript 语法检查。

### 开发邮件一键粘贴记录

- 开发邮件记录改为单一粘贴入口，自动提取主题、正文、发送时间和回复状态；需要时可展开修正字段。
- 数据与依赖影响：复用既有开发邮件接口和字段，不修改数据库或依赖。
- 验证：已完成 JavaScript 语法检查。

### 修复旧客户库缺少更新时间列

- 启动迁移现在会为旧版 `customers` 表补齐 `updated_at` 列，恢复客户列表按更新时间排序和加载。
- 数据与依赖影响：仅对缺少该列的本地用户数据库执行一次可逆的 `ALTER TABLE ADD COLUMN`；现有数据不改写，无新增依赖。
- 验证：检查全部当前用户库的 `customers.updated_at` 均可用，并完成客户列表查询验证。

### 今天页数据反馈、手机操作与日期本地化

- 今天页的“需要跟进客户”和“逾期”统计改为直接依据当天待办列表计算；空列表时两个数值均显示为 `0`，与列表和右侧工作区保持一致。
- 手机端将“询问 Trade OS”纳入底部快捷操作架，移除独立悬浮入口；AI 面板从操作架上方打开，并在窄屏时忽略历史拖拽坐标，避免遮挡日程与快捷操作。
- 今天页日期统一使用中文“7月23日 · 星期四”格式，修复登录后被英文日期覆盖的问题。
- 数据与依赖影响：仅调整前端显示与布局，不改动接口、数据库或运行依赖。
- 验证：`node --check app/static/app.js` 通过；已在桌面与 390px 窄屏复查今天页、AI 入口和日期显示。

### 列表操作、可读性与信息来源澄清

- 客户列表保持“访问网站”“查看与编辑”“归档”三个直接入口；公司名称也可作为查看客户的快捷入口。
- 提升今天、Inbox 与客户列表的辅助文字尺寸和对比度，强化边界与纸张层，使列表、表头和建议依据更容易阅读。
- Inbox 的建议动作明确标注为“AI 建议”或“系统提醒”，并将证据改为“事实依据”，帮助用户区分事实、规则与推断。
- 数据与依赖影响：仅调整前端内容呈现和交互，不改动接口、数据库或运行依赖。
- 验证：`node --check app/static/app.js` 通过；已复查客户列表、Inbox 与窄屏客户卡片布局。

### 客户详情改为事实与行动工作台

- 客户详情首屏现按“最近发生、当前等待、下一步、关键需求”展示，联系人和沟通时间线保留在同一工作区，打开客户即可恢复当前上下文。
- AI 背调与历史分析移至默认收起的“分析备注与历史 AI 内容”，不再作为客户详情的主要信息区；客户等级和阶段继续保留在资料页供历史兼容。
- 新增“当前等待”人工编辑，以及下一步的完成、延后到明天和延后 3 天快捷操作。完成会写入沟通时间线，延后只更新待办日期。
- 数据与依赖影响：复用 `customers.attention_reason`、`reminders` 和 `follow_up_logs`，未新增数据库表或第三方依赖；新增当前等待与待办延后接口。
- 验证：`venv\\Scripts\\python.exe -m py_compile app.py`、`node --check app/static/app.js` 通过；已静态核对客户详情加载、当前等待更新、待办完成和待办延后调用链。

### 外部模型协作闭环

- 客户详情页新增“导出全部沟通”，一次复制完整沟通时间线，可直接粘贴到 ChatGPT 等外部模型。
- “分析备注与历史 AI 内容”支持保存外部模型结果，并允许用户确认具体行动和日期后创建下一步；外部文本不会自动改写客户资料或任务。
- 数据与依赖影响：每个用户数据库新增 `external_analysis_notes` 表及客户时间索引，用于保存外部分析原文；无新增第三方依赖。
- 验证：`venv\\Scripts\\python.exe -m py_compile app.py db.py` 与 `node --check app/static/app.js` 通过；四个相关接口路由已注册；已执行用户数据库初始化并确认 Hamid、Amy、Kelly 的 `external_analysis_notes` 表均创建成功。

### 新客户未联系二次跟进进入 Inbox

- 新开发客户的 15/30/60 天开发提醒到期后，若仍未记录真实沟通或开发邮件，会以“未联系跟进”卡片出现在 Inbox；卡片提供安排再次联系、记录联系结果和 7 天后提醒。
- 每位客户同一开发节点只显示一张卡片；每次最多展示 8 位待跟进客户，完成沟通后自动停止后续未联系提醒。推迟会在 7 天内保持安静。
- 数据与依赖影响：复用既有 `customers`、`reminders`、`follow_up_logs`、`outreach_emails` 和 `inbox_items`，无表结构迁移、无新增依赖。
- 验证：Python 与 JavaScript 静态语法检查，以及 Inbox 接口的未联系客户筛选验证。

## 2026-07-22

### 修复 Today 查看客户失败

- 修复 Today 和 Inbox 中打开客户详情时，详情弹窗渲染 AI 摘要引用了未定义变量，导致已取得客户数据仍显示“加载客户信息失败”的问题。
- 数据与依赖影响：无数据库结构、已有客户数据或新增依赖变化。
- 验证结果：`node --check app/static/app.js` 通过；已核对客户详情使用接口返回的当前客户对象渲染 AI 摘要。

### 添加客户录入界面重整

- 添加客户弹窗按录入顺序收敛为“公司、官网识别、主要联系人”；官网与识别操作合并为一行，次要的公司资料统一收进“更多公司资料”。
- 联系人区域取消多层卡片包裹，批量邮箱导入改为默认收起的按需工具，减少首次录入时的滚动与视觉干扰；邮箱检查、联系人去重和导入逻辑保持不变。
- 修复弹窗页眉、内容区和页脚背景未随外框圆角裁切的问题，弹窗滚动仍限定在内容区。
- 数据与依赖影响：无数据库结构、已有客户数据或新增依赖变化。
- 官网粘贴后自动识别并直接回填公司、国家、类型、行业、简介和联系方式；国家字段增加官网正文与简介中的国家名称回退识别，覆盖 AI 已在简介中识别但遗漏结构化国家字段的情况。
- 保存区提供“保存并复制邮箱”，将本次客户的邮箱逐行复制到剪贴板；保留“仅保存”用于不需复制时的快速保存。
- 联系方式区以“粘贴邮箱”作为默认且高亮的主操作：粘贴单个或多个邮箱后自动检查并生成联系人；手动填写联系人改为按需展开。
- 修复待 SMTP 后台复核的邮箱被前端错误排除、无法自动添加的问题；格式或域名已明确无效的邮箱会从联系人草稿自动移除，SMTP 复核返回明确无效时也会自动移除。
- 国家字段会把 AI 返回的英文/本地语言国家名标准化为 CRM 中文名称，例如 `Spain`、`España` 统一为“西班牙”。
- 修复官网自动识别回填联系人时的前端变量引用错误，避免识别结果在联系人写入阶段中断。
- 官网直接读取遇到 HTTP 403 或 429 时会先使用更完整的浏览器访问头，再回退到公开网页阅读器提取正文，降低可在浏览器访问但被脚本读取拒绝的官网识别失败率；读取仍仅用于用户主动提交的公开网址。
- 数据与依赖影响：无数据库结构或新增依赖变化；联系人 CSV 导出接口新增可选 `customer_id` 筛选。
- 验证结果：`venv\\Scripts\\python.exe -m py_compile app.py` 与 `node --check app/static/app.js` 通过；以官网文本 `Valencia, Spain` 验证国家回退结果为“西班牙”，`Spain, España` 标准化结果为“西班牙”。

### AI 问答与客户分析证据化

- 主界面“问问 AI”会先从待办、沟通记录、开发邮件和客户资料中检索与问题最相关的内容，再生成回答；回答要求按“直接回答、事实依据、分析与下一步、信息缺口”组织，并为事实标注可打开核对的证据编号。
- “我今天还有什么值得做”及同类今日优先级问题改为确定性 CRM 查询：只展示今天到期和已逾期的未完成待办，不再基于客户画像、历史 AI 分析或“等待回复”状态生成产品、报价、物流或催跟建议；没有待办时明确显示“暂无新的行动依据”。
- 主界面 AI 回答支持安全的 Markdown 展示：标题、段落、列表、加粗、行内代码、引用和表格会按阅读版式呈现，表格可横向查看，不再直接显示 Markdown 标记。
- 客户 AI 分析新增联系人、沟通方向和已记录下一步作为上下文；报告中的客户事实需标注来源，推断需明确标识，资料不足时会列为“待核实”，避免把通用销售假设写成客户结论。
- 新增客户“当前理解”和 AI 建议历史：记录最新事实、最近变化、未闭环事项、行动判断与建议来源；新沟通、待办完成、沟通编辑或新增下一步会触发更新。没有新的行动依据时系统保存“暂无新的行动依据”，不制造泛化跟进建议；用户在 CRM 中实际执行后会把该动作回写为建议反馈，供后续去重与复盘。
- 数据与依赖影响：每个用户数据库会自动创建 `customer_understandings` 与 `ai_recommendations` 两张表，并为建议历史创建客户时间索引；不新增第三方依赖，既有研究报告和 CRM 记录保持不变。
- 验证结果：`python -m py_compile app.py app/engine.py` 与 `node --check app/static/app.js` 通过；问答检索、今日工作确定性排序、状态更新和证据返回逻辑已做静态检查。

### 官网读取兼容 Windows 系统证书

- 官网智能识别和官网监控的 HTTPS 请求现在会在保持证书校验的前提下，同时使用 Windows 的 ROOT 与 CA 证书库。浏览器已经信任的企业代理证书或本地颁发机构证书可以被 CRM 正确识别，避免出现浏览器可打开、AI 提示“unable to get local issuer certificate”的情况。
- 数据与依赖影响：仅修改 `app/engine.py` 的官网读取 TLS 上下文，无数据库结构或新增第三方依赖。
- 验证结果：Python 语法检查通过；TLS 上下文保持 `CERT_REQUIRED` 和主机名校验，未降低 HTTPS 安全校验。
- 对 Python 仍无法补全的证书链，读取器会仅在 `CERTIFICATE_VERIFY_FAILED` 时交由 Windows 自带的 Schannel `curl.exe` 重试；该回退与 Edge/Chrome 共享系统信任机制，继续验证证书与主机名。

### 邮箱可发送性检查基础层

- 将原“邮箱验证”升级为“邮箱可发送性检查”：结果新增可发送性状态、置信度、邮箱类型、风险标记和逐项证据，并在导入界面显示“可以尝试发送 / 需要人工核对 / 无法发送”。
- 修复无 MX 记录即判无效的问题：系统现在识别 Null MX（明确不收邮件），并在无 MX 但域名存在 A/AAAA 地址时使用 SMTP implicit MX 规则；DNS 超时与配置问题会显示为暂时无法确认。
- 部门公共邮箱与免费邮箱改为类型提示，不再直接降低为无效；一次性邮箱改为风险标记。检查结果会按用户保存到新建的 `email_verifications` 表。
- 新增 `email_delivery_events`、`email_verification_jobs` 和 `email_domain_probes` 表，并为开发信补充收件邮箱、联系人和 Message-ID 字段。SMTP worker 使用持久化任务队列、最多两个 MX、一次临时故障重试和域名级 Catch-all 缓存；不会执行 `DATA` 或发送邮件。
- 数据与依赖影响：每个用户数据库会自动创建四张邮箱证据/任务表，并迁移 `outreach_emails` 的三个关联字段；无新增第三方依赖。SMTP 探测需在 `.env` 显式填写 EHLO、MAIL FROM 与开关后才会运行。
- 验证结果：Python 静态语法检查通过；模拟 DNS 覆盖 MX、Null MX、implicit MX、NXDOMAIN 和临时错误分支；模拟 SMTP 覆盖接受、`550 5.1.1` 硬退信和 `451` 临时重试；接口缓存、任务入队和结果持久化测试通过。

## 2026-07-21

### 同日跟进任务合并

- 同一客户同一天只保留一条未完成跟进任务。新建或更新"下次跟进"时若已存在同日未完成任务，会把动作和原因合并到现有任务，不再插入第二条；新客户 15/30/60 天开发池待办不受影响。
- 修复 `update_customer`、`create_customer`、`complete_reminder`、`add_follow_history`、`create_customer_task` 五个入口的重复插入路径；新增辅助函数 `_merge_or_create_reminder` 统一处理。
- 一次性清理已存在的重复任务：Hamid 的 Publimpresos S.A.S 在 2026-07-21 原有两条未完成任务（id=2091 "继续跟进" 与 id=2136 "询问客户市场信息"），合并为一条保留最新动作"询问客户市场信息"；Amy、Kelly 无重复。清理前备份位于 `data/backups/dup-cleanup-20260721_142502`。
- 数据与依赖影响：仅修改 `app.py`，并删除 1 条重复 reminder 记录；无表结构变化、无新增依赖。
- 验证结果：Python 静态语法检查通过；三个数据库重复任务检查均为 0；服务已重启加载新代码。

### 智能导入国家识别修正

- 移除 `DOMAIN_COUNTRY_MAP` 中 `.com` 默认识别为"美国、全球"的条目。`.com` 是通用顶级域名，原先会优先匹配并让所有 `.com` 客户被识别成美国。
- 现在仅依靠国家代码顶级域名（如 `.com.au`、`.de`、`.com.cn`）识别国家；`.com` 域名交由 AI 根据官网内容判断或由用户手动选择。
- 数据与依赖影响：仅修改 `app.py`，不影响已导入客户的现有国家字段。
- 验证结果：Python 静态语法检查通过；`.com.au` 等带国家代码后缀仍能正确识别。

### 上次跟进日期显示修正

- 修复补录历史跟进时 `customers.last_contact` 被错误设为今天的问题。`add_follow_history` 现在以用户填写的沟通日期更新 `last_contact`，与 `follow_up_logs.follow_date` 保持一致。
- 客户详情、总览客户列表、Today 待办列表、收件箱静默判断现在统一从 `follow_up_logs` 取 `MAX(follow_date)` 作为"上次跟进"，不再依赖可能落后的 `customers.last_contact` 字段。
- 添加开发信时若 `sent_date` 比当前 `last_contact` 更新，会同步更新客户表的"上次联系"。
- 数据与依赖影响：仅修改 `app.py`，无表结构变化、无新增依赖。
- 验证结果：Python 静态语法检查通过；总览、客户详情和待办列表都会显示跟进记录中的最新沟通日期。

### 新客户联系状态修复

- 客户列表中的“已有联系”现在仅由真实跟进记录或开发信记录判定；导入来源、客户状态和旧版客户分类不再将未沟通的新客户归入已有联系。
- 已修正 Hamid 在 2026-07-20 新录入且仍未归档、无沟通记录的 29 位客户为新客户；当天已有真实沟通的客户维持已有联系。另有 2 位同日客户已归档，未改动。
- 数据与依赖影响：更新 `data/hamid.db` 中 29 条客户的旧分类字段为 `new`，无表结构变化、无新增依赖。
- 验证结果：客户分类逻辑静态检查通过；数据库核对确认 29 位未归档客户均无跟进和开发信记录，更新后均可进入新客户视图。

### 沟通记录自动完成待办

- 在客户时间线手动保存沟通后，系统会自动完成该客户截至沟通日期的最近一条未完成跟进待办，并将待办与沟通记录相互关联；无需再重复点击“完成任务”并填写同一份内容。
- 已完成 Brand Waves 于 2026-07-20 到期的重复待办，关联其 2026-07-21 的 WhatsApp 跟进记录。
- 数据与依赖影响：更新 1 条待办及其关联跟进记录；无表结构变化、无新增依赖。
- 验证结果：Python 静态检查通过；待办匹配限定为同一客户、未完成、普通跟进类型且到期日不晚于沟通日期，未来待办不受影响。

### 7 月 9 日伪沟通记录清理

- 查明 Kinda Plastics、ASAP Plastics 等客户被误判为“已有联系”的原因：旧批量测试在 2026-07-09 连续调用添加跟进接口，把“七月价格更新/跟进”和测试文本写成了真实沟通。
- 软删除该连续测试批次的 82 条伪沟通记录，涉及 13 位客户；恢复这些客户的“新客户”身份并清空错误的最近联系日期，保留 7 月 30 日等真实待办和完整审计日志。
- 客户列表计算最近联系日期时新增软删除过滤，删除或纠错后的沟通记录不再继续影响联系状态。
- 数据与依赖影响：更新 `data/hamid.db` 中 82 条跟进记录和 13 条客户状态；无表结构变化、无新增依赖。修改前完整备份位于 `data/backups/2026-07-21/094713_978898`。
- 验证结果：13 位客户均无批次外跟进和开发邮件；清理后 Kinda Plastics、ASAP Plastics 等全部归入新客户，82 条伪记录不再显示，数据库完整性为 `ok`。

## 2026-07-20

### 最新客户资料合并

- 从 `CRM reminder(4).rar` 中仅提取 Amy、Hamid、Kelly 的客户主表与联系人资料，当前新版程序代码、界面、配置、沟通记录、提醒和系统设置均未被压缩包覆盖。
- Amy 更新 2 位客户；Hamid 新增 32 位客户、更新 5 位客户并新增 70 位联系人；Kelly 更新 2 位客户。
- 合并按客户 `updated_at` 判断资料新旧；Hamid 的 Bentleigh Group、Acryl Innovations GmbH、PLASTI-D、Petron Thermoplast FZ LLC 共 4 条当前资料时间更新，因此保留当前值，避免新数据覆盖较新的本地调整。
- 数据与依赖影响：三位用户合计新增 32 位客户、70 位联系人并更新 9 位客户；未修改数据库结构、代码或运行依赖。合并前完整备份位于 `data/backups/2026-07-20/183939_100979`。
- 验证结果：Amy 为 290 位客户、127 位联系人，Hamid 为 302 位客户、234 位联系人，Kelly 为 153 位客户、0 位联系人；三库 `PRAGMA integrity_check` 均为 `ok`，外键检查无异常。除上述 4 条刻意保留的较新资料外，客户与联系人数据已和压缩包完全一致。

### 最新跟进记录补充合并

- 补充合并 `CRM reminder(4).rar` 中的最新跟进记录：Amy 新增 2 条并更新 2 条，Hamid 新增 2 条并更新 7 条，Kelly 新增 3 条。
- 新增记录包括 Glowforge、Eplexi、Allanson International Inc.、高级滑板各类产品等客户的最新进展；SolidRock 与 KarmaPlus International FZC 的记录日期和内容同步为 7 月 20 日的最新版本。
- 数据与依赖影响：仅新增或更新 `follow_up_logs` 数据，不修改程序代码、数据库结构或运行依赖；合并前完整备份位于 `data/backups/2026-07-20/195025_424181`。
- 验证结果：三位用户的跟进记录与压缩包数据逐条一致，三库完整性检查均为 `ok`，外键检查无异常；“全部记录”按跟进日期与创建时间可正常显示 7 月 20 日记录。

### 客户沟通入口与列表区域化

- 客户详情将“记录新的沟通”和“记录开发信或邮件”合并为一个“记录沟通”入口；展开后通过纯文字的“沟通记录 / 开发邮件”切换两套原有表单，保存接口与数据类型保持不变。
- 移除统一入口上的浏览器展开符号和加号，避免符号与文字重复表达；每次打开客户详情默认回到“沟通记录”，保存任一类型后自动收起入口。
- 客户列表由断开的表格下划线改为每位客户一块完整浅色区域；所有单元格共用连续的上下边界与同心圆角，“访问网站、查看与编辑、归档”使用独立操作容器并统一顶部基线。
- 数据与依赖影响：仅调整客户详情和客户列表的 HTML、CSS 与前端切换状态，不修改客户数据、接口、数据库结构或运行依赖。
- 验证结果：Python 与 JavaScript 静态检查通过；真实浏览器确认桌面端客户区域边界连续，网站与操作按钮顶部坐标一致；统一入口无加号且两种记录表单可正常切换。768×1024 下页面无横向溢出、表格区域保留横向查看，390×844 下客户自动转为完整卡片且无横向溢出；控制台无错误或警告。

### 跟进记录支持修改沟通方式

- “编辑跟进记录”新增沟通方式字段，可在 WhatsApp、邮件、电话、会议、报价、寄样、其他跟进、客户回复和完成任务之间修改；保存接口同步校验并更新记录类型。
- 将 Hamid 的 Acryl Innovations GmbH 记录 `#34243` 从 WhatsApp 更正为邮件，保留原日期、内容和“我发给客户”的方向，并写入操作日志。
- 数据与依赖影响：修改 1 条跟进记录的 `activity_type` 与 `communication_channel`，未修改数据库结构或新增依赖；修改前完整备份位于 `data/backups/2026-07-20/204100_838593`。
- 验证结果：Python 与 JavaScript 静态检查通过；隔离数据库接口测试确认类型可修改、可恢复为邮件且无效类型返回 400；真实页面确认记录显示“邮件”，编辑弹窗默认选中“邮件”，新版服务已在 8080 端口加载，数据库完整性为 `ok`。

## 2026-07-19

### Liquid Glass 小范围交互样板

- 为顶部快捷操作架、“询问 Trade OS”浮动入口与面板、“记录客户回复”弹窗建立首组 Liquid Glass 交互样板；业务入口、表单字段、保存逻辑与 AI 功能保持不变。
- 快捷操作架会在页面内容滚动到其下方时增强材质厚度、边缘高光和投影；AI 入口及面板会响应指针位置、按压、打开来源和拖动状态，面板快捷问题继续支持左右滚动。
- “记录客户回复”弹窗改用同心圆角体系，外壳、字段、按钮按父级间距形成连续轮廓；表单正文采用高可读性的稳定材质，标题区在内容滚动时增强玻璃分界。
- 增加背景滤镜不受支持、系统减少动态效果及手机底部弹层三种降级方案。
- 数据与依赖影响：仅修改 HTML/CSS 视觉状态与前端交互反馈，不修改数据库结构、客户数据、接口或运行依赖。
- 验证结果：Python 与 JavaScript 静态检查通过；真实浏览器确认桌面端快捷操作架 25px、AI 入口 26px、AI 面板与快速记录弹窗 32px 的轮廓连续，AI 面板从入口方向展开且页面无横向溢出；快速记录正文滚动后标题玻璃会增强；390×844 下弹窗使用 28px 圆角并保持 8px 安全边距，AI 入口与面板不重叠；控制台无错误。iPad 将在设计方向确认后与后续全局应用一起验收。

### 持续操作层与客户表格对齐

- 重新明确 Liquid Glass 的职责：左侧工作空间取消独立玻璃面板边界，导航以轻量玻璃选中态直接融入页面背景；今天、Inbox 和客户抬头在桌面滚动后收缩为持续可见的玻璃操作条。
- Inbox 的“刷新”和“记录客户回复”、今天的快捷操作、客户页的导出与新增入口会随各自抬头保持在右上方，减少滚动后返回页面顶部的操作。
- 客户列表为公司、国家、最近发生、下一步、网站和操作等列建立统一宽度；表头、客户内容和操作按钮统一从单元格顶部基线开始，避免操作按钮比客户名称上浮。
- 数据与依赖影响：仅调整页面布局、滚动视觉状态与表格列宽，不修改功能逻辑、接口、数据库或运行依赖。
- 验证结果：Python 与 JavaScript 静态检查通过；真实浏览器确认左侧导航无独立面板边框和阴影，今天、Inbox、客户在桌面滚动后收缩为 64px 高的玻璃操作条，Inbox 滚动 511px 后“刷新”和“记录客户回复”仍完整固定在右上；1280px 窗口下常用客户列无需横向滚动，表头与数据列横坐标一致，客户名称和首个操作按钮顶部基线差为 0；390×844 下不启用桌面收缩效果、页面无横向溢出，控制台无错误。

### 顶部操作层流畅度与真实折射

- 顶部常驻操作层改为独立浮层：原页面抬头自然随内容滚走，64px 操作条从页面最顶端出现，不再保留 12px 空隙。
- 取消抬头宽度、高度和内边距动画，显示过程只使用 GPU 友好的位移与透明度，避免滚动时反复触发布局计算。
- Chromium/Edge 使用 SVG displacement map 对操作条后方真实页面内容进行轻微折射，并叠加动态边缘高光；Safari 和不支持 SVG 背景滤镜的浏览器自动使用稳定的模糊玻璃降级。
- 参考开源实现的 `displacementScale`、边缘折射与高光原理，直接适配现有原生 HTML/CSS 架构，不引入 React、Astro 或新增运行依赖。
- 数据与依赖影响：只修改今天、Inbox、客户顶部操作层的呈现与滚动反馈，不修改其他页面、表格、业务功能、接口或数据库。
- 验证结果：Python 与 JavaScript 静态检查通过；真实 Chromium 浏览器确认操作条出现前后页面总高度保持不变，不再产生布局重排，滚动后的顶部坐标为 0、页面无空隙；计算样式已采用 `url(#tradeOsLiquidGlass)` SVG 折射滤镜，Inbox 的“刷新”和“记录客户回复”保持启用；390×844 下桌面浮层关闭且无横向溢出，控制台无错误。Safari 使用模糊玻璃降级，无法呈现 Chromium 的位移折射。

### 参考样板方向的全局视觉与信息架构优化

- 新增独立的第二版视觉层，将参考样板的暖纸背景、编辑式标题、液态玻璃侧栏与快捷操作架、陶土主操作、克制分隔和低饱和建议区应用到正式页面；所有原有页面与操作入口继续保留。
- “今天”调整为桌面双栏工作区：左侧保留可拖动排序的待跟进列表，右侧显示当前客户、原因、最近动态、AI 建议和原有操作；未来 14 天日程与完整日历、Apple 日历同步入口继续保留。
- 修复“今天”拖动排序在指针离开卡片、切换页面、窗口失焦或触控取消后仍遗留悬浮卡片的问题；排序监听改到文档级并在导航、失焦、页面隐藏时统一清理，同时保留键盘上下移动和原有保存顺序能力。
- 顶部新增紧凑快捷操作架，复用现有搜索客户、记录沟通和添加客户逻辑；移动端改为底部悬浮，并调整 AI 助手位置，避免与快捷操作互相遮挡。
- 侧栏一级入口聚焦“今天、Inbox、客户”，新客户、完整日历、全部记录和操作日志收纳进可展开的“更多工具”；设置、切换账号和所有低频页面仍可访问，没有删减功能。
- 将“未建立真实沟通”和独立“开发客户”统一为“新客户”：没有任何沟通记录或开发邮件时属于新客户，出现任意有效沟通或开发邮件后自动进入“已有联系”；客户列表和“更多工具”中的新客户工作区使用同一动态结果。记录开发邮件时同步将客户转为已有联系，编辑客户不会再意外改回新客户。
- 客户列表操作改为带文字与统一矢量图标的“访问网站、查看与编辑、归档”，替代含义不清的箭头和省略号；AI 入口改为深色液态玻璃胶囊，AI 面板、侧栏、快捷操作架增强高光、折射感和背景模糊。
- 修正客户列表、操作按钮和底部快捷操作架的垂直基线、按钮尺寸与图标居中；拖动排序离开“今天”时会立即清理浮层，避免任务卡片覆盖客户表格。
- Trade OS AI 按钮和展开面板支持鼠标或触控自由拖动，位置保存在当前浏览器并在窗口尺寸变化时自动限制在可视区域；面板快捷问题改为可左右滑动，新增“报价待回复”和“本周到期”快捷入口。
- 全界面按钮、状态、搜索、拖动、日历和导航符号统一为代码内 SVG/CSS Mask 图标；历史备注中的旗帜和星级 Emoji 在显示层自动移除，数据库原文保持不变。
- 客户详情改为大尺寸快速工作区，联系人、资料、AI 分析标签升级为玻璃分段控件；Inbox、完整日历、新客户、全部记录和操作日志补充各自的纸张层、分隔、悬停和信息层级，原有处理动作继续保留。
- 修复仅打开客户详情、等待 AI 资料载入后关闭也会误报“有未保存修改”的问题；弹框现在等待异步资料填充完成后再记录初始状态。
- Inbox 的“记录到时间线”完成后提供 12 秒撤销入口；撤销会同时移除刚生成的沟通记录、恢复 Inbox 卡片，并精确恢复处理前的客户联系日期、客户类型、状态和更新时间。客户在撤销窗口内又被修改时会拒绝覆盖新状态。
- 字体最终采用本地托管 `Noto Serif SC Variable` 与 `Noto Sans SC Variable`（思源宋体/思源黑体同源字族），移除 Google 网络字体依赖；字体文件与 OFL 授权材料约 11 MB。
- 数据与依赖影响：客户分类由现有沟通记录与开发邮件动态计算，不修改数据库结构、无需数据迁移；Inbox 撤销令牌只在服务内存中短暂保存，不新增数据表；显示层过滤 Emoji 不修改历史备注；新增约 11 MB 本地字体静态文件，无新增 Python 或前端运行依赖。
- 验证结果：Python 编译与 JavaScript 静态检查通过；真实浏览器拖动并恢复顺序后无悬浮卡片、占位符和排序状态残留；Hamid 的 113 个原始新类型客户中有 12 个存在历史活动并已自动排除，动态“新客户”返回 101 个，首屏全部为未联系客户；本地字体已由浏览器正确采用；仅查看客户详情后可直接关闭且不再出现未保存误报。1440×900 Windows 常用桌面布局、768×1024 iPad 和 390×844 iPhone 均无横向溢出，iPad 客户操作按钮保持完整，iPhone AI 图标与底部快捷操作之间保留 11px 间距。

## 2026-07-18

### Mac 启动器权限与解压环境自修复

- 修复 `Mac启动器.command` 缺少执行权限导致双击时提示“没有正确的访问权限”的问题，并清除 FastZip 添加的下载隔离标记。
- 启动器检测到 ZIP 解压导致 `.venv-mac` 中 Python 符号链接失效时，会自动清理并重建该运行环境，避免权限修复后继续因损坏环境启动失败。
- 数据与依赖影响：不修改客户数据库；`.venv-mac` 仅包含可重新生成的 Python 运行依赖，重建后会根据现有 `requirements.txt` 安装依赖；无新增依赖。
- 验证结果：启动器 Bash 语法检查通过，文件已具备当前用户执行权限，FastZip 隔离标记已移除；完整首次启动需要在用户双击后按当前网络环境安装依赖。

### Trade OS 视觉系统第一阶段落地

- 正式采用“暖质编辑式工作台”作为界面基线，统一暖纸色、墨色、陶土主色、低对比边界、玻璃浮层、圆角、阴影和交互焦点等基础视觉 Token。
- 桌面侧栏改为暖色悬浮玻璃导航，现有“今天、Inbox、客户、本周工作、设置、切换账号”等入口及个人显示设置全部保留；导航图形改为同一风格的线性 SVG 图标。
- “今天”页面按已确认的第二版样板重新排版：强化日期和标题层级，将待办改为宽松连续列表，选中项使用纸张浮层，客户上下文改为完整宽度工作区，未来安排和折叠内容继续保留。
- 手动拖动排序、批量记录、添加客户、记录沟通、调整日期、完整日历、Apple 日历同步和 AI 建议等现有操作均保留原事件与数据逻辑。
- 补充 Windows 桌面、iPad 和 iPhone 查看场景的响应式基础；iPad 保留核心操作，iPhone 优先保证导航、待办和客户上下文的阅读体验。
- 设计规范从候选状态升级为分阶段实施的 v1.0，并记录字体美观优先、客户详情响应式工作区和当前不做深色模式等已确认决定。
- 数据与依赖影响：纯 HTML/CSS 外观调整，无数据库结构变化、无数据迁移、无新增运行依赖；本阶段仍保留现有网络字体，正式中文字体文件将在 Windows 实页对比后本地化。
- 验证结果：Python 语法检查和 JavaScript 静态检查通过；真实数据页面在桌面默认窗口、768×1024 和 390×844 三种尺寸完成浏览器验收，无横向溢出和控制台错误；待办选择可正确切换客户上下文，拖动手柄数量与待办一致，手机导航可正常打开和收起。现有功能元素的 ID、事件处理和九个页面容器均未删除。

## 2026-07-17

### 个人日历隔离与及时同步

- Apple 日历订阅由三人共用的公开地址改为每位登录用户独立的私有令牌链接；旧公共地址停止输出业务数据，避免 Hamid、Amy、Kelly 的日程相互混入。
- 个人订阅只读取与系统内完整日历一致的有效客户待办，排除新客户开发 15/30/60 天批量提醒；现有开发提醒继续留在数据库和开发客户流程中。
- 日程标题直接显示“客户公司 · 具体动作”，移除员工姓名前缀；日历名称简化为“客户跟进”。
- 事件 UID 改为“用户 + 来源 + 任务 ID”的稳定标识，同客户同日多项任务可以独立更新；重复拉取不再修改全部事件版本，未变化时支持 ETag/304。
- 个人订阅只输出当前有效待办；完成、删除或改期待办后，旧事件直接从订阅源移除，不显示 Apple 日历的灰色“已取消”条目；新增待办立即进入个人订阅源。
- 今日判断、完成时间和订阅日期统一使用 Asia/Shanghai；全天事件结束日期修正为次日，避免午夜及 RFC 5545 日期边界错误。
- 日历页将“推送更新”改为“检查订阅内容”，显示订阅源最近变化时间和有效待办数量，并说明 Apple 日历按客户端获取计划同步；旧订阅需要取消后重新添加个人链接。
- 数据与依赖影响：复用 `system.db.app_settings` 保存每位用户的随机订阅令牌，无表结构变化、无新增依赖；删除待办改为完成态留档，不再物理删除；修改前完整备份位于 `data/backups/2026-07-17/144725_827833`。
- 验证结果：Python、JavaScript 静态检查通过；隔离测试确认 Hamid 58 项、Amy 30 项、Kelly 0 项有效待办且 UID 集合互不相交，个人订阅中开发提醒为 0；临时数据库生命周期测试确认新建立即出现、完成后事件完全移除、相同 ETag 再次请求返回 304。

### 精准客户搜索与个人化设置

- 客户页快捷视图调整为“全部 / 等待回复 / 长期未联系 / 尚无下一步 / 已归档”，资料质量筛选收进“更多筛选”。
- 新增国家、客户类型、行业、等级、当前状态、下一步、未联系天数、联系日期和标签组合筛选；搜索扩展到联系人联系方式、沟通记录、待办、开发邮件、备注和客户画像。
- 搜索可识别“等待回复”“中间商”“30天未联系”“2025年联系过”等自然条件，并在结果中说明匹配原因。
- 保存的客户视图改为按登录用户保存在系统数据库，支持保存完整组合条件，并自动迁移当前浏览器中的旧视图。
- 现有设置页新增个人使用设置：可选功能显示、主导航顺序、默认页面、客户列表字段和 Inbox 长期未联系阈值；Inbox、今天和客户等核心功能始终启用。
- AI、客户分析、官网监控、邮箱验证、日历同步、本周工作、开发邮件和 Excel 导入可以隐藏；官网监控与客户分析关闭后，相应后台检查暂停，历史数据继续保留。
- 保存后的全局同步改为局部刷新并保留客户页位置；个人设置保存不会刷新或跳离当前页面。
- 产品边界明确排除首次引导、话术模板、评分式优先级、模块数据删除、第三方模块市场和复杂权限系统。
- 数据与依赖影响：复用 `system.db.app_settings` 保存每位用户的 JSON 配置，无数据库结构变化、无新增依赖；修改前备份位于 `data/backups/2026-07-17/101419_477053`。
- 验证结果：关键词搜索精确命中并显示匹配原因；“30天未联系”识别为自然条件并返回 8 位客户；等待回复视图返回 9 位、个人阈值下长期未联系返回 7 位，Inbox 保持 5 项；个人设置写入、读取和恢复通过；真实浏览器确认 8 个可选模块均位于现有设置页，核心导航固定、组合筛选和列显示正常，保存设置后页面保持原位；浏览器无控制台错误，四个数据库完整性为 `ok`，服务为 `healthy`。

## 2026-07-16

### Inbox 长期未联系恢复与 AI 稳定降级

- Inbox 恢复少量真正长期未联系客户：A/B/C+ 客户 45 天、其他客户 75 天无沟通时可进入重新判断队列，每次最多显示 5 位。
- 今天已经跟进、已有待办或处于“等待回复 / 近期无需求 / 暂时观察”等复查周期内的客户不会因旧记录重新进入 Inbox。
- “问问 AI”在云端模型不可用时自动使用当前 CRM 的到期待办、长期未联系客户和未回复开发邮件生成本地建议，不再直接展示“所有 LLM 后端均不可用”。
- 本地降级结果显示明确提示，相关原始记录仍可打开核对；云端模型恢复后自动继续使用模型回答。
- 数据与依赖影响：无数据库结构变化，无新增依赖。
- 验证结果：DeepSeek 独立连通测试成功；Hamid Inbox 恢复 5 位长期未联系客户，最近沟通距今 228—242 天，今天已跟进客户未重新出现；本地降级分析规则、Python 与 JavaScript 静态检查通过；服务为 `healthy`。

### 客户自动分类、静默刷新与低打扰状态判断

- 官网智能识别新增客户类型判断：分销、进口、贸易类归为“中间商”，生产、项目实施和材料使用方归为“终端”；识别结果仍可手动修改。
- 行业领域扩充为 20 个常见建议，并改成可自由填写的建议输入框，支持更细的实际行业名称。
- 程序版本更新不再显示“现在刷新”提示；更新会等待当前弹框完成或关闭后静默刷新，避免打断填写。
- 源码自动重启仅在 `TRADE_OS_DEV_WATCH=1` 的开发模式启用，日常使用不会再因文件监控触发服务重启和页面刷新。
- 完成每日跟进、记录沟通或发送开发信后，系统自动判断“等待客户回复 / 日常跟进后仍未回复 / 近期无需求 / 暂时观察”，并设置合理复查日期。
- 旧记录中信息方向尚未识别时，系统会结合“我方、询问、发送、报价、开发信、客户回复”等语义判断等待回复或双方沟通。
- AI 整理在后台运行期间，按钮和结果区持续显示“AI 正在整理”，用户可以继续填写其他内容，完成后恢复操作按钮。
- Inbox 在复查日期前保持安静；客户出现新回复、官网变化或复查日期到达时才重新生成建议。
- “已有分析但没下一步”“高等级但没任务”“长期未联系”“历史开发信未回复”等常规筛选不再进入 Inbox，继续保留在客户筛选视图中。
- 客户工作台的“关系记忆”改为“客户当前状态”，集中展示最近发生、当前判断、客户画像和下一步，减少长篇 AI 内容抢占主视线。
- 数据影响：`customers` 新增 `attention_state`、`attention_reason`、`attention_updated_at`、`attention_review_date`；旧数据自动迁移，无新增依赖。
- 历史补齐：修复上线前当天已跟进但状态为空的记录；修改前已建立完整备份 `data/backups/2026-07-16/160824_711641`，补齐 Hamid 11 位、Kelly 1 位客户的当前状态。
- 验证结果：四个状态字段已迁移到 Hamid、Amy、Kelly 数据库且完整性均为 `ok`；状态规则测试确认发出信息→等待回复、明确无需求→近期无需求、有下一步→已安排；智能分类测试确认 Trading→中间商、Sign Factory→终端；本地浏览器确认行业可自由填写、客户当前状态四块布局和 AI 后台运行提示正常；Hamid 刷新后的 Inbox 为 0，今天已跟进客户残留数为 0；服务为 `healthy`。

### 联系人邮箱去重与自动合并

- 官网智能识别、邮箱批量导入、客户创建和手动添加联系人统一按 `去除首尾空格 + 忽略大小写` 后的邮箱识别同一联系人。
- 同一客户出现相同邮箱时只保留一个联系人，并合并姓名、职位、电话、WhatsApp、LinkedIn 和首选联系方式等补充字段。
- 联系人草稿编辑时输入重复邮箱会立即合并并给出成功提示；后端保存接口和数据库唯一索引提供双重兜底。
- 数据影响：迁移时合并历史同客户重复邮箱联系人；执行前已创建 `pre-contact-email-dedupe` 完整数据库备份，无新增依赖。
- 历史清理结果：Hamid 合并 15 条、Amy 合并 188 条、Kelly 0 条；三个用户库的同客户重复邮箱组均为 0。
- 验证结果：Python 与 JavaScript 静态检查通过；Sparklight LLC 重复邮箱接口测试保持 1 个联系人并返回 `merged=true`；本地浏览器确认大小写不同的相同邮箱会即时合并；唯一索引存在，服务为 `healthy`，三个用户数据库完整性均为 `ok`。

### Inbox 建议改为业务状态判断

- AI 建议卡片移除“7 天后再看”和“忽略本次”，改为“记录最新进展”和“暂不安排下一步”。
- “暂不安排下一步”表示最近尚未形成下一步计划，可以直接确认，也可采用“等待客户回复 / 近期无需求 / 当前不投入”快捷建议或自由填写实际情况。
- 当前建议关闭后，新的客户回复、官网变化或分析信号仍会触发 AI 结合具体情况重新判断。
- 用户填写的实际情况会作为“上次判断”呈现在后续新建议中，帮助判断新的提醒是否仍适用。
- 客户回复卡片的退出动作改名为“无需记录”，避免与 AI 建议状态混淆。
- 数据影响：`inbox_items` 新增 `resolution_reason` 和 `resolution_note`，保留业务判断原因及说明；旧数据自动迁移，无新增依赖。
- 刷新机制：Inbox 每分钟仅更新角标，不再重绘当前列表；程序版本变化时显示可控刷新提示，不再自动刷新整页；新增手动“刷新”按钮。
- 验证结果：Python 与 JavaScript 静态检查通过；新状态接口校验通过；本地浏览器确认自由填写、手动刷新和提示文案正常；服务为 `healthy`，system、Hamid、Amy、Kelly 数据库完整性均为 `ok`。

### 沟通发言人自动识别与保存退出

- 记录沟通和完成跟进时，系统默认根据登录业务员、客户/联系人以及 WhatsApp 时间戳后的发言人自动判断“我方发送 / 客户发送 / 双方沟通”。
- 取消每次必选信息方向的操作，仅在识别有误时通过“识别有误？”手动纠正。
- AI 提示词明确传入当前业务员姓名和客户上下文，并按 `[time] 发言人: 正文` 规则分段归属。
- 未保存提醒在客户工作台中显示“保存并退出”，会依次保存待导入邮箱、联系人草稿、沟通草稿和客户资料。
- 数据影响：沟通方向字段继续保留，新记录由自动识别结果写入；无法确定时保存为 `unknown`，不阻断日常记录。
- 验证结果：截图示例中两段 `Hamid:` 消息稳定识别为“我发给客户”（high confidence）；`Hamid:` 与客户发言人并存时识别为“双方沟通”。本地界面验证确认必选方向按钮已移除、自动识别提示可见、“保存并退出”可见。

### 补全 2025 年历史沟通记录

- 识别 Excel 中的纯年度汇总表头，包括“（2025）”这类列名。
- 将年度汇总单元格按 `[10/13-10/19]` 等周期标记拆分为独立沟通活动。
- 每段记录按周期结束日生成日期，跨年周期自动进入下一年。
- 年度汇总列始终优先采用方括号周期日期，防止“2.8/4/6mm”等产品规格被误识别为日期。
- 每段记录使用独立来源标识，支持重复执行时精确去重。
- 客户名称在 Excel 单元格中意外连续重复两次时，自动折叠后匹配已有客户，避免产生重复客户。
- 完整性审计：“（2025）”列有 45 个非空客户单元格，包含 92 段周沟通记录。
- 补导结果：新增 92 条、覆盖 44 个已有客户、未匹配 0 条，活动日期范围为 2025-08-31 至 2025-12-28。
- 验证结果：再次执行时新增 0、更新 0、跳过 259；重复来源标识 0；system、Hamid、Amy、Kelly 数据库完整性均为 `ok`。

### 恢复邮箱验证与导入

- 将旧嵌套项目中的邮箱验证功能迁回当前主版本。
- 恢复新建客户、添加客户和客户联系人页的“邮箱验证与导入”入口。
- 支持粘贴后自动验证，并提供手动验证按钮。
- 验证内容包含：格式、域名 A 记录、MX 记录、一次性邮箱、角色账户和系统内重复联系人。
- 界面分类显示有效、存疑、无效和重复邮箱。
- DNS 或 MX 临时超时时标记为“存疑”，避免将可用邮箱误判为无效；系统内已存在的邮箱直接返回重复客户。
- 添加 `email-validator` 和 `dnspython` 运行依赖。

### 数据完整性与数据源治理

- 补全 Hamid Excel 中 16 条漏失沟通记录，新建 6 个缺失客户。
- 建立 Excel 单元格级去重标识，重复导入不再生成重复记录。
- 统一活动数据库目录为 `data/`，为每个用户保存独立 Excel 数据源清单。
- 登记历史数据库为非活动留档，嵌套启动器统一转向主程序。
- 扩展自动备份保留和数据库标识检查。

### 交互与 AI 整理

- 所有可编辑弹框统一接入未保存退出提醒。
- 沟通记录增加“我发给客户 / 客户发给我 / 双方沟通”方向。
- AI 整理根据方向分离双方的陈述、需求和行动。

## 记录规则

以后每次功能修改至少记录：

- 日期和功能名称。
- 用户可见变化。
- 数据结构或迁移影响。
- 新增依赖与配置。
- 验证方式和已知限制。
# 2026-07-25

- 登录体验：登录页改为单一 6 位个人访问码输入；系统自动匹配用户工作区，不再要求先选择账号。失败限流保持有效。
- 登录体验：受信任设备的登录会话默认有效 30 天，并在每次访问时续期；本机使用固定会话密钥，服务重启后不会让已登录设备失效。
- 访问性能：带版本号的 CSS 与 JavaScript 改为长期浏览器缓存；HTML 与 API 响应仍不缓存，避免界面与业务数据过期。
- 数据/依赖：新增环境变量 `CRM_PIN_HAMID`、`CRM_PIN_AMY`、`CRM_PIN_KELLY`，并兼容既有 `CRM_PASSWORD_*`。
- 验证：Hamid 访问码 `313743` 自动匹配及错误访问码拒绝测试通过。

- 上线安全：登录改为每位用户的环境变量密码，生产环境必须配置随机会话密钥；会话 Cookie 启用 HttpOnly、SameSite 和 HTTPS 安全标记。
- 上线安全：团队总览、健康检查及备份/恢复/完整性接口增加登录保护；移除启动脚本中的明文 API Key。
- 运行方式：新增 Waitress 生产入口 `serve.py`、systemd 与 Cloudflare Tunnel 示例配置、家庭服务器上线指南。
- 数据/依赖：新增 `waitress==3.0.2`；`data/` 全目录不再允许提交，避免上传表格、SQLite 数据和备份进入 Git；不迁移现有 SQLite 数据。
- 验证：Python 编译检查、Flask 鉴权接口检查及生产环境配置检查。
# 2026-07-29

## 客户联系分类

- “已有联系”现在只在客户已回复开发信，或沟通记录标记为客户来信/双向沟通时成立；相对视图显示为“未获回复”。
- 已发送开发信、我方单向跟进和导入来源会保留为活动事实，但不会把客户归入“已有联系”。
- 数据影响：无需迁移；现有客户列表会按已保存的回复状态与沟通方向重新计算。
- 验证：新增客户视图回归测试，覆盖“仅发送开发信”与“客户已回复”两种情形。

# 2026-08-02

## 网页连接稳定性提示

- 用户可见变化：网络真正中断时显示底部轻量“正在重连”提示；只读请求自动重试，恢复后显示短暂“连接已恢复”，正常操作不再频繁弹出错误提示。
- 数据/依赖：无数据结构、数据库迁移或新增依赖；写入请求不自动重试，避免重复保存。
- 验证：JavaScript 语法检查、生产发布后本机鉴权接口与公开站点检查；已知限制是 Mac 到 Cloudflare 的外网通道断开时仍需网络恢复。

## 手机端工作区重排

- 用户可见变化：手机端保留页面右上液态玻璃快捷按钮，隐藏旧版底部快捷栏；客户卡片改为紧凑的公司、关系、最近发生和下一步信息，移除手机上难以阅读的空白字段堆叠。
- 数据/依赖：无数据结构、数据库迁移或新增依赖。
- 验证：移动端 CSS 级联检查、JavaScript 语法检查；需在手机浏览器完全刷新后查看。

## 首屏与移动快捷栏优化

- 用户可见变化：登录后先显示“今天”页面，偏好设置在后台加载，不再等待全站配置完成；断线提示延迟 1.2 秒，避免短暂请求抖动造成长时间“正在连接”错觉。手机端快捷栏恢复为右上单按钮，点击后展开完整操作组。
- 数据/依赖：无数据结构、数据库迁移或新增依赖。
- 验证：桌面与 iPhone User-Agent 公网首屏请求均完成；正式站点发布后再次测量。

## 首页首屏缓存

- 用户可见变化：首页应用外壳允许 Cloudflare 缓存 30 秒，并在后台刷新；登录状态、客户数据和所有 API 仍不缓存，重复打开手机页面可减少等待。
- 数据/依赖：无数据库变化；仅调整首页 HTTP 缓存头。
- 验证：服务端代码检查、公开首页响应头检查；首次冷启动仍受 Mac 到 Cloudflare 通道延迟影响。

## iPad 快捷栏交互

- 用户可见变化：iPad 与手机统一为点击右上主按钮后展开完整快捷操作，收起时只保留主按钮；不再永久占据页面空间。
- 数据/依赖：无数据结构、数据库迁移或新增依赖。
- 验证：触摸设备 CSS 级联检查；移动端需完全刷新后查看。

## 移动端首屏资源减负

- 用户可见变化：移除依赖外部网络的 Google 字体请求；手机端不再加载桌面专用大背景纹理，降低首屏阻塞和弱网下长时间等待。
- 数据/依赖：改用项目内置字体；无数据库变化。
- 验证：手机视口资源配置检查、JavaScript/Python 语法检查；桌面端视觉纹理保持不变。

## 首屏字体请求优化

- 用户可见变化：首屏不再请求两套包含数百个分片的 CJK 字体 CSS，直接使用设备系统字体，减少手机首次打开时的串行资源请求。
- 数据/依赖：无数据库变化；字体回退到 iOS/macOS/Android 系统字体。
- 验证：首页资源清单检查和正式发布后手机请求测速；文字内容保持可读，字体细节随设备系统略有差异。

## 连接提示自动收起

- 用户可见变化：持续网络异常时提示最多显示约 6 秒，之后自动收起；当前页面仍可使用，后台接口恢复后继续自动更新，避免“正在连接”永久遮挡内容。
- 数据/依赖：无数据结构、数据库迁移或新增依赖。
- 验证：JavaScript 语法检查；API 重试和页面局部可用逻辑保持不变。

## API 首屏并行化

- 用户可见变化：版本检查改为后台并行，页面数据请求不再排队等待版本接口；版本接口异常或超时不会拖住当前页面。
- 数据/依赖：无数据结构、数据库迁移或新增依赖。
- 验证：JavaScript 语法检查；版本检查设置 800ms 超时，业务 API 保持原有重试和鉴权逻辑。

## 连接提示误报抑制

- 用户可见变化：单个后台接口偶发失败不再弹出全局“正在连接”；只有连续网络失败或设备离线时才提示，避免页面已经可用却被连接状态打扰。
- 数据/依赖：无数据结构、数据库迁移或新增依赖。
- 验证：JavaScript 语法检查；网络重试、恢复提示和离线事件保持有效。

## 页面内可用状态

- 用户可见变化：移除覆盖页面的全局“正在连接”浮层；各页面保留自己的局部加载状态，已经显示出的内容不再被后台请求遮挡。客户页增加明确的“添加客户”入口；桌面今日跟进右侧详情卡恢复为随页面滚动的粘性卡片。
- 数据/依赖：无数据结构、数据库迁移或新增依赖。
- 验证：JavaScript/Python 语法检查、移动端资源测速；后台请求重试仍在运行。

## 客户快速添加按钮

- 用户可见变化：客户搜索栏右侧新增小型液态玻璃“＋”按钮，可持续快速打开添加客户表单；原有右上可展开快捷栏保留。
- 数据/依赖：无数据结构、数据库迁移或新增依赖。
- 验证：前端语法检查、移动端 CSS 级联检查；按钮沿用现有添加客户流程。

## Tunnel 传输稳定性

- 网络变化：针对当前网络中 QUIC/UDP 连接反复超时的实测情况，Tunnel 启动模板改为使用 Cloudflare 支持的 HTTP/2/TCP 传输，减少 UDP 被 VPN、路由器或运营商丢弃造成的断线。
- 数据/依赖：不涉及业务数据；仍使用现有 Cloudflare Tunnel 和免费方案。
- 验证：`cloudflared` 版本 2026.7.3；Cloudflare 官方文档确认 `http2` 为 QUIC 不稳定或 UDP 受限时的可用传输方式。已知限制是若 TCP 7844 同样被阻断，需要恢复网络或更换出口。
## 2026-08-02 — 恢复右上快捷栏并移除重复入口

- 用户可见变化：恢复网页、iPad 与手机端右上角液态玻璃快捷栏及展开交互；移除客户页标题下文字“添加客户”和搜索框旁重复加号，客户新增统一从快捷栏进入。
- 数据/依赖影响：无数据库结构、业务数据或第三方依赖变化。
- 验证：完成前端模板与样式检查，发布前执行语法检查。
## 2026-08-02 — 移动端导航收敛与客户工作区恢复

- 用户可见变化：手机端移除无实际操作空间的左侧工作栏和菜单触发器；今日页“记录沟通”快捷栏移到右侧空白区域，并统一为液态玻璃材质；恢复客户重点高亮与灯光开关；iPad 宽度重新显示客户网站列；修正“下一步”标题与日期的排版字体；重做客户筛选条的玻璃层和选中切换，消除边角残色。
- 数据/依赖影响：无数据库结构、业务数据或第三方依赖变更；重点标记继续使用既有 `is_pinned` 数据。
- 验证：执行前端脚本语法与后端编译检查；发布后验证生产入口。
## 2026-08-02 — 统一液态玻璃组件与搜索入口收敛

- 用户可见变化：全局搜索、页面右侧快捷栏、客户筛选与 Inbox 筛选统一使用同一套高折射液态玻璃；右侧快捷栏随长页面滚动保持可用；客户页移除重复搜索框，统一使用顶部全局搜索；筛选器取消陶土色选中轨道，改为玻璃选中态。
- 数据/依赖影响：无数据库、业务数据或第三方依赖变化；保存的客户视图改为读取顶部搜索词。
- 验证：执行前端脚本语法与后端编译检查，发布后验证本机和公网入口。
## 2026-08-02 — 客户下一步操作反馈

- 用户可见变化：客户详情中的“完成”“明天”“3 天后”和创建下一步在请求期间显示轻量处理中状态；成功后由已更新的下一步卡片、关闭的编辑器或日期选中态确认结果，不再叠加成功弹窗；失败才明确提示。
- 数据/依赖影响：无数据结构和依赖变化，仍使用既有提醒改期与创建接口。
- 验证：执行前端脚本语法与后端编译检查，发布后验证入口可访问。

## 2026-08-03 — 首次进入自行创建访问码

- 用户可见变化：Hamid、Amy 与 Kelly 的既有访问码全部作废；每个人第一次选择自己的账号时，直接创建一组 6 位数字访问码并进入，之后仍使用该访问码登录。更新后，已登录设备会要求重新登录一次。
- 数据/依赖影响：访问码只以密码哈希形式保存于 `system.db` 的应用设置中；初始化会清除旧的访问码设置，不影响客户、沟通、提醒或附件数据；生产环境不再读取 `CRM_PIN_*` 配置。
- 验证：已执行前端脚本语法与后端编译；正式地址和本机 `/api/auth/users` 均确认三位成员均为 `pin_setup_required: true`。
## 2026-08-02 — 沟通方式支持自由填写

- 用户可见变化：新建和编辑沟通记录时，“沟通方式”从固定下拉框改为可输入字段，并提供 WhatsApp、邮件、展会现场、微信、视频会议、客户来访等建议；自定义方式会在时间线和本周工作中原样显示。
- 数据/依赖影响：`follow_up_logs.activity_type` 继续使用现有字段；编辑接口允许非空且不超过 80 字符的自定义方式。
- 验证：执行前端脚本语法与后端编译检查，发布后验证入口可访问。
## 2026-08-02 — 高透明液态玻璃回调

- 用户可见变化：全局搜索与页面快捷栏恢复高透明、低厚度的玻璃材质；移除叠加内边线和多层高光，保留单层折射与细边缘。
- 数据/依赖影响：无。
- 验证：执行前端脚本语法检查，发布后验证入口可访问。
## 2026-08-02 — 今日焦点直接记录

- 用户可见变化：宽屏“今日焦点”中的记录输入框现在可直接完成当前提醒并写入时间线，支持 Cmd/Ctrl + Enter；记录沟通图标改为定位并聚焦该输入框，不再强制跳转完成弹窗；四项客户事实改为小尺寸圆角内容块。
- 数据/依赖影响：直接调用既有提醒完成接口，写入方式为 `follow_up`；无数据库迁移。
- 验证：执行前端脚本语法与后端编译检查，发布后验证入口可访问。
## 2026-08-04 — 发送开发信后完成新客户开发待办

- 用户可见变化：在客户详情保存开发信后，相关的新客户开发待办会自动标记完成，不再继续显示为待办；其他独立的后续待办保持不变。
- 数据/依赖影响：仅更新当前客户的 `outreach_*` 提醒状态及 `next_follow_up`，不新增依赖，不改变开发信和沟通记录的数据源。
- 验证：已通过代码检查确认开发信写入、待办关闭和下一次待办重算在同一事务中执行。
## 2026-08-09 — 本周工作与跨成员客户阅读区

- 用户可见变化：本周工作页保留 Hamid、Amy、Kelley 三人并列展示，客户条目默认突出最新一条进展并折叠同周其他记录；新增上一周、回到本周、下一周阅读控制。点击客户进入宽屏只读客户工作区，按客户身份、本周进展、历史脉络、当前等待、已确认下一步和未完成待办组织信息；联系人、客户等级、AI 判断和绩效比较不再出现在周报阅读区，手机端详情改为全屏阅读。
- 数据/依赖影响：跨成员详情接口改为明确字段白名单，新增 `week_start`、`timeline_page`、`timeline_per_page` 分页参数（每页最多 50 条），仅返回周会所需客户事实、上报活动、分页时间线和未完成待办；排除联系人、研究报告、客户理解、AI 推荐、邮箱验证和操作日志。接口继续只读并在 `finally` 中恢复原用户数据库上下文；无数据库迁移和新增依赖。
- 验证：`node --check app/static/app.js`、`python3 -m py_compile app.py db.py scheduler.py` 通过；使用项目 `.venv-mac` 执行风险回归测试，新增周报白名单/分页测试通过，整套测试另有 1 项既有附件本体缺失失败，与本次改动无关。
## 2026-08-09 — 修复周报客户卡片容器错位

- 用户可见变化：修复周报客户卡片 HTML 容器提前闭合导致的错位；同一成员的所有客户现在保持在对应成员栏内，Hamid、Amy、Kelley 三组不会互相串栏或掉出三栏网格。
- 数据/依赖影响：仅修正前端 DOM 拼接，无数据库、接口或依赖变化。
- 验证：JavaScript 语法检查，并重新检查周报生成结构。
## 2026-08-09 — 对齐本周工作的主页面背景与透明度

- 用户可见变化：本周工作改用与“今天”一致的主画布、树纹背景叠加透明度、纸张层级和卡片透光度，取消独立页面过厚的白色遮罩，让周报回到同一套 Trade OS 工作台视觉。
- 数据/依赖影响：仅调整前端样式，无数据库、接口或依赖变化。
- 验证：完成 CSS 级联检查并准备进行浏览器视觉验收。

## 2026-08-09 — 修复轻量材质模式下的周报背景降级

- 用户可见变化：修复设备进入轻量材质模式后周报背景被清空、成员区变成近乎不透明白色的问题；轻量模式现在只关闭模糊和动效，保留与“今天”一致的背景图和透明纸张层。
- 数据/依赖影响：仅调整前端样式，无数据库、接口或依赖变化。
- 验证：浏览器检查 `html.motion-lite` 下的周报背景与卡片透明度规则。
# 2026-08-13 · Prospecting Lab 自动集成接口

- 新增 Hamid 专用、可轮换/撤销的 Prospecting Lab Bearer 令牌，服务端只保存 SHA-256 摘要。
- 令牌仅允许读取 Hamid 客户索引、创建客户，以及补充联系人、外部分析备注和已确认开发信；不允许删除、备份恢复、待办或跨用户访问。
- 数据影响：`system.db/app_settings` 新增一条令牌摘要记录；不新增表。
- 依赖影响：无。验证：令牌生成、摘要存储与最小权限回归测试通过。
## 2026-08-17 — 发布并验收公司网络只读周报入口

- 用户可见变化：将 `/share/weekly` 共享周报入口同步到正式运行副本 `TradeOS/runtime`；公司真实 IPv4 `122.233.82.41` 和 IPv6 出口前缀 `240e:390:9ab:3e00::/64` 均可直接进入本周工作，只读页面不要求三位成员访问码。
- 访问边界：从 VPN 代理出口 `103.172.81.135` 访问仍返回 403；共享会话可读取周报、客户索引和周报客户详情，但客户写入接口返回 401，离开白名单网络后会话失效。
- 数据/依赖影响：发布脚本仅同步代码和静态资源，排除 `data/`、私密环境文件、虚拟环境和日志；另在正式私有环境文件显式写入公司 IPv4/IPv6 白名单。正式服务健康检查返回 HTTP 200，未修改业务数据库或会话密钥。
- 验证：正式公网 `https://app.trosa.space/share/weekly` 通过 IPv4 和 IPv6 均返回 302 并完成 `/api/auth/me`、`/api/overview/stats`、`/api/weekly-summary` 只读流程；非白名单 VPN 出口返回 403。全量 51 项回归测试通过，`node --check`、`py_compile`、发布脚本语法检查通过；当前机器没有 Chrome 可执行文件，因此以公网 HTTP 流程和后端测试完成验收。

## 2026-08-17 — 修正公司 IPv6 网络下的周报共享入口

- 用户可见变化：公司网络优先使用 IPv6 时，`/share/weekly` 不再因为只匹配 IPv4 而误显示“只允许从公司网络访问”；当前公司 IPv4 `122.233.82.41` 与 IPv6 出口前缀 `240e:390:9ab:3e00::/64` 均可进入只读周报。
- 安全边界：仍不加入 VPN 出口 `103.172.81.135`；IPv6 按当前公司出口 `/64` 前缀匹配，配置变化后需要更新环境变量。共享会话继续只允许周报读取接口，离开白名单网络后失效。
- 数据/依赖影响：不修改 SQLite、客户数据或会话密钥；仅更新 IP 白名单默认值、生产环境模板和部署说明，不新增依赖。
- 验证：隔离服务和正式本机服务均验证 IPv4/IPv6 允许、非白名单拒绝、周报接口可读、客户写入返回 401；全量 51 项回归测试通过，`node --check app/static/app.js`、`py_compile`、发布脚本健康检查通过。
