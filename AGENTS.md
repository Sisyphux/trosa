# 项目协作规则

## 1. 工作方式

- 不迂回立论：直接说明事实、判断、未知和建议，避免滥用先设定反方再陈述己方的句式。
- 事实源优先：先核对当前代码、数据库迁移、正式运行目录和现行界面，再修改产品说明。历史报告只用于追溯，不能覆盖现行实现。
- 保护用户修改：当前工作区可能包含用户尚未提交的修改。不得覆盖、回退、删除或格式化无关内容。
- 功能变更日志：每次修复、新增或删除用户可感知功能时，同步更新根目录 `CHANGELOG.md`，记录日期、功能变化、数据/依赖影响和验证结果。
- 文档分层：`README.md` 负责总览；产品口径、设计标准、系统设计和使用说明分别维护各自职责；阶段性报告明确标注历史状态。
- 修改范围保持克制：优先解决一个完整问题，避免在同一任务中同时重构无关模块。

## 2. 产品核心

- Trade OS 的核心闭环是：恢复客户上下文 → 执行已确认动作 → 记录实际事实 → 按需确认新的动作与日期。
- 客户、联系人、沟通记录、待办、Inbox、日历、搜索、导入导出和备份恢复必须在未配置模型时完整运行。
- 沟通记录可以没有下一步。
- 待办必须同时包含明确动作和日期。
- “没有下一步”“等待回复”“很久未联系”和“资料不完整”属于状态或信息缺口，不能自动转成逾期承诺。
- 低置信度信号进入 Inbox、辅助分析或待审阅区域，用户确认后才能成为待办。

## 3. 性能优先

- 加载速度、操作响应和滚动流畅度优先于装饰效果。
- 正式性能基线包含旧款 Win10、8GB 内存、集成显卡和较慢网络环境。
- 核心页面不得依赖 AI、官网读取、邮箱验证或其他可选后台能力才能完成加载。
- 大列表必须分页、窗口化或按需加载；不得一次渲染完整客户历史。
- 客户详情优先加载身份摘要和最近记录；联系人、文件、完整时间线和辅助分析按需加载。
- 保存沟通、联系人、文件或待办后优先局部更新，不重新下载和渲染整个客户对象。
- 旧设备性能模式关闭 `backdrop-filter`、复杂滤镜、多层阴影、列表进入动画和非必要页面过渡。
- 新增视觉效果前需验证普通模式和性能模式。
- 性能修改必须记录实际测量结果，不能只以主观感受作为验收。
- 目标基线：点击后 100ms 内出现反馈；客户身份区尽快呈现；核心滚动在目标旧设备上接近 60fps；不得出现长时间主线程冻结。

## 4. 状态与错误

- 加载中、成功但为空、未登录、权限不足、网络失败、服务错误和数据解析失败必须使用不同状态。
- HTTP 或业务请求失败不得显示为“没有客户”“没有待办”或“本周没有内容”。
- 所有核心列表和详情页面提供清楚的失败说明和重试入口。
- 多成员页面中，单个成员加载失败不得阻塞或清空其他成员内容。
- 界面不得用历史缓存冒充最新数据；使用旧缓存时明确标注并在后台刷新。

## 5. 数据单源与安全

- `data/` 或 `CRM_DB_PATH` 指向的 SQLite 目录是唯一业务数据源。
- Excel 只承担导入、导出和历史恢复。
- Apple 日历只订阅 ICS 事件，不能回写 CRM。
- 文档中的“同步”必须写明对象与方向，例如“Apple 日历订阅”或“Excel 导入”。
- 当前架构不宣称 iCloud 数据库同步、离线副本合并或跨设备冲突解决。
- 数据库写入、导入、附件保存、备份和恢复必须保留来源、校验和审计链路。
- 不得以文件时间戳覆盖正在使用的数据库。
- 删除旧版本、运行目录、虚拟环境或归档前，必须先确认正式服务实际使用路径，并确认目标不包含数据库、附件、配置和唯一可运行代码。
- 本地数据库快照不能替代异机或异地备份。

## 6. 多用户隔离

- Hamid、Amy、Kelley 的业务数据和个人偏好分别保存。
- 所有写入接口必须绑定已认证的当前用户。
- Agent、浏览器插件和后台任务不得通过请求参数任意切换写入其他用户数据库。
- 本周工作允许跨成员只读查看明确共享的客户进展。
- 跨成员只读接口必须使用字段白名单，不返回联系人隐私、内部令牌、AI 推断和无关后台字段。
- 跨成员读取完成后必须恢复原数据库用户上下文。

## 7. 联系人与身份匹配

- 联系人是邮箱、电话、WhatsApp 和外部沟通进入客户关系的身份基础层。
- 邮箱匹配前统一去除空格并转换为小写。
- 电话和 WhatsApp 匹配前统一规范格式，并保留用户输入的原始值。
- 只有唯一、精确、标准化后的邮箱或手机号匹配可以自动选择联系人。
- 名称、昵称、尾号、公司简称和缺少国家代码的手机号只能作为候选，不能自动写入。
- 不得根据联系人昵称、邮箱前缀或 WhatsApp 显示名推断公司名称。
- 同一邮箱或手机号疑似属于多个客户时进入待审阅流程。
- 新建、合并和重新归属联系人必须保留来源。
- 合并联系人前展示双方身份、所属客户和被引用历史；用户确认后执行，并保留撤销快照。
- 删除联系人前检查沟通记录、开发信、邮件事件和其他历史引用。

## 8. AI 使用边界

- AI 是可关闭、由用户主动触发的辅助阅读和整理层。
- 模型输出必须区分系统事实、用户输入、推断和未知。
- AI 可以总结、提取事实、指出信息缺口和准备候选内容。
- AI 不根据缺失资料推断客户价值、采购意向、合作概率或商业承诺。
- 模型输出不能直接改写客户、联系人、沟通记录或待办。
- 结构化写入前展示目标和字段，由用户确认。
- 消息发送、报价、价格承诺、交期承诺和其他商业承诺始终由用户完成。
- AI 建议不能自动进入 Today、日历或逾期统计。
- AI 模块关闭后，停止对应后台任务和模型调用，不参与核心页面查询、排序、Inbox 或提醒计算。
- 关闭 AI 后保留历史分析数据，但默认只读和折叠。
- 如果辅助分析明显影响核心页面性能、事实准确性或用户判断，应从核心路径移除；确认无有效用途后再删除相关活跃代码。

## 9. Pi Agent 操作助手边界

- Pi Agent 是 Trade OS 的快速自然语言操作入口，主要承担查询、定位、整理和确认式业务操作。
- Pi Agent 不负责修改代码、界面、配置文件、数据库结构或部署环境。
- Pi Agent 只能通过 Trade OS 后端提供的原子业务接口访问客户、联系人、沟通记录和待办。
- Pi Agent 不得直接使用文件、Shell、SQLite 或绕过业务接口。
- 读取、搜索、时间线整理和信息缺口检查可以直接执行。
- 新增、修改、完成待办和记录沟通前，展示目标客户、字段、来源和影响，用户确认后写入。
- 普通写入确认强度低于删除，但仍需提供清楚的预览。
- 删除联系人、删除沟通记录、删除待办和归档客户必须强制二次确认具体目标，并保留可恢复撤销快照。
- 删除确认不得与创建、修改或完成操作合并为同一张普通确认卡。
- 复合指令拆成原子步骤；每一步说明读取结果、待确认写入和未执行内容。
- 多项普通写入可以汇总预览后确认；涉及删除的步骤必须单独确认。
- Pi Agent 可以指出“缺少联系人”“没有沟通记录”“没有明确下一步”等信息缺口，但不能因此自动创建客户、联系人或待办。
- Pi Agent 只有在用户给出明确动作和日期后才能准备待办提议。
- Pi Agent 返回内容区分：系统事实、用户输入、Agent 整理和未知信息。
- Pi Agent 的查询结果必须有数量上限和分页，不得因读取完整历史拖慢核心服务。
- Pi Agent 不在核心页面加载过程中自动运行。

## 10. 浏览器沟通采集

- 网易邮箱和 WhatsApp Web 采集由用户主动打开扩展后执行。
- 扩展只读取当前打开的邮件或聊天页面，不后台扫描全部邮箱或全部 WhatsApp 会话。
- 扩展不得拦截私有网络请求、访问 WhatsApp 内部数据库或自动发送消息。
- 扩展自动提取邮箱、手机号、方向、时间、正文和来源，写入前展示客户匹配与新增消息范围。
- 无唯一匹配时允许选择已有客户、新建联系人候选或暂存待归属沟通。
- 扩展不得根据昵称自动创建公司。
- 导入必须保存来源、原始内容、消息指纹和适配器版本。
- 重复执行必须保持幂等，只导入尚未保存的新消息。
- 自动摘要与原始证据分开保存。
- 扩展通过受限业务接口写入，不直接访问 SQLite。
- 扩展令牌绑定单一用户，支持撤销，不能访问备份恢复、系统设置和其他用户数据。

## 11. 前端设计与验收

- 修改前端或 UI 前，完整阅读：
  - `design/TRADE_OS_UI_SYSTEM.md`
  - `design/inspiration/DESIGN_PROFILE.md`
  - `design/inspiration/CATALOG.md` 中与目标页面相关的条目
- UI System 是现行设计事实源，灵感库只用于约束视觉选择。
- 不为单独页面引入另一套视觉语言。
- 文字层级、信息归属和操作效率优先于卡片、玻璃和装饰。
- 宽屏、iPad、iPhone 和旧 Win10 网页端均属于正式验收范围。
- 修改后检查加载、空状态、错误状态、键盘操作、焦点、200% 缩放、减少动态效果和性能模式。
- 视觉验收必须使用当前真实界面截图，不以历史原型代替现行实现。

## 12. 修改验证

- 后端修改至少运行相关 Python 回归测试和语法检查。
- 前端修改至少运行 JavaScript 语法检查，并在真实浏览器中验证目标流程。
- 数据迁移必须可重复执行、可回滚，并在副本或隔离数据库上验证。
- 数据修复前创建一致性快照，修复后运行 SQLite `integrity_check` 和业务校验查询。
- 性能修改必须同时比较修改前后结果，并记录设备、页面、数据量和测量方式。
- 功能完成后说明修改文件、数据影响、依赖影响、测试结果、浏览器验收结果和已知限制。
