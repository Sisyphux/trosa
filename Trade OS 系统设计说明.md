# Trade OS 系统设计说明

> 本文解释当前信息模型、运行结构、数据边界和自动化职责。操作步骤见《使用说明》，产品规则见《产品与设计标准》。

## 1. 系统目标

Trade OS 保存一段客户关系的长期上下文，让业务员随时回答：

- 这家公司和哪些人有关？
- 最近实际发生了什么？
- 当前在等待什么？
- 我已经承诺何时做什么？
- 历史信息来自哪里，能否核对和恢复？

系统通过可靠记录和明确待办降低遗忘与重复劳动。业务判断、沟通内容和商业承诺由用户负责。

## 2. 信息模型

```text
客户
├─ 联系人：和谁沟通
├─ 记录：已经发生的事实
├─ 待办：明确的未来动作与日期
├─ 文件：查阅客户时的原始资料（报价单、合同、样品、展会资料等）
├─ Inbox：尚待判断的新信号
└─ 分析备注：人工或可选模型形成的辅助理解
```

| 概念 | 当前存储 | 约束 |
|---|---|---|
| 客户 | `customers` | 长期关系容器，可归档恢复 |
| 联系人 | `contacts` | 归属于客户，同邮箱在客户内去重 |
| 记录 | `follow_up_logs`、`outreach_emails` | 保存日期、内容、方向和来源 |
| 待办 | `reminders` | 包含具体动作和日期 |
| 文件 | `customer_files` + `uploads/customer_files/` | 元数据入 PostgreSQL 并记录 SHA256，二进制落盘；逻辑数据库 dump 不含文件本体，备份 bundle 必须同时包含附件 |
| Inbox | `inbox_items` 及动态查询 | 处理后离开，不代替时间线 |
| 分析备注 | `agent_prospect_profiles.research_json` 与沟通辅助结果 | 与事实分开展示，保留来源；旧研究/推荐表只作为迁移与历史数据兼容，不再是当前运行入口 |

旧等级、阶段和注意状态字段继续兼容历史数据。日常视图优先读取真实记录、待办和动态条件。

## 3. 工作循环

```text
Inbox / 到期待办 / 主动打开客户
              ↓
       用户判断并执行动作
              ↓
       记录实际发生的事实
              ↓
   如有明确下一步，确认动作与日期
              ↓
          生成新待办
```

- 记录允许没有下一步。
- 待办需要具体动作和日期。
- 低置信度建议先保持为信号或分析备注。
- 已到期事项全部保留在今天页，手动排序只表达当天关注顺序。

## 4. 页面分工

- **Inbox 收**：放置需要人判断的客户回复、待归属沟通和 Sela 请求。
- **时间线记**：保留客户关系中已经发生的事实。
- **今天做**：呈现用户已经确认且到期的承诺。
- **客户承载关系**：集中联系人、最近发生、当前等待、下一步和来源。
- **本周工作分享**：从真实记录选择团队需要看到的进展。

沟通整理、截图识别和用户主动触发的官网智能导入在需要时读取这些上下文，并把结果放回辅助区域。它不参与核心闭环的成立条件。

## 5. 运行结构

```text
单页前端（app/static）
        ↓ HTTP
Flask 应用与接口（app.py）
        ├─ 数据访问与迁移（db.py）
        ├─ 后台任务（scheduler.py）
        ├─ ICS 订阅（ical_gen.py）
        ├─ 邮箱复核（email_verifier.py）
        └─ 可选模型与网站读取（app/engine.py）
                ↓
PostgreSQL（ECS 正式唯一业务源）
        ├─ identity / core / trosa / sela / audit
        └─ 兼容层：旧 SQLite 形状的 API
文件存储（/var/lib/trade-os/uploads/）
```

当前三位用户通过统一 PostgreSQL 中的 `identity.memberships` 和兼容引用隔离数据；不再把用户数据库作为正式运行时的物理分片。应用采用单进程运行，符合当前低并发规模。

## 6. 数据单源与交换

正式业务事实源是 PostgreSQL：

- `identity` 保存组织、用户和成员关系。
- `core` 保存公司、联系人和规范化身份。
- `trosa` 保存客户工作区、时间线、待办、Inbox 与兼容引用。
- `sela` / `audit` 保存外联同步和来源审计。
- `/var/lib/trade-os/uploads/` 保存客户附件与导入来源；`CRM_DB_PATH` 只属于 SQLite 隔离/回滚边界。
- SQLite 隔离/回滚目录中的 `active_store.json` 只标识该副本，不代表 ECS 正式数据源。

Excel 承担导入、导出和历史恢复。导入记录保存原文件、工作表、单元格和内容指纹；无法可靠匹配的内容进入审阅流程。

Apple 日历通过个人 ICS 地址读取当前待办。它是只读订阅通道，不复制数据库，也不把日历端修改回写到 CRM。

当前启动、关闭和后台任务均不调用 iCloud 数据库复制。跨设备共享依赖集中部署访问；离线副本之间没有自动冲突合并。

## 7. 数据保护

系统使用以下保护链路：

1. PostgreSQL 使用外键、唯一约束、索引和事务；兼容层将旧用户作用域映射到统一 schema。
2. 数据修改后由正式备份链路生成一致性 logical dump，并打包附件。
3. 备份记录文件清单、SHA-256，并在 PostgreSQL 容器内执行 `pg_restore --list` 验证。
4. 恢复前验证 bundle 清单、哈希、数据库 restore-check 和附件路径。
5. 恢复动作先创建当前 PostgreSQL dump 和附件 bundle 的安全副本。
6. 近期 bundle 按保留策略清理，手工恢复点单独保留。
7. 客户日常删除采用可恢复归档。

本地快照用于快速恢复。正式部署还需要把备份复制到加密的异地介质并定期演练恢复。

## 8. 可选辅助层

沟通整理、截图识别、官网智能导入和邮箱 SMTP 复核按个人设置与环境配置启用。

辅助层遵循：

- 读取范围显式可控。
- 输出区分事实、推断和未知。
- 失败时返回清楚状态，不阻断客户、记录、待办和备份操作。
- 结构化写入前由用户确认。
- 发送、报价和承诺由用户执行。
- 关闭模块后停止对应后台处理并保留历史内容。

上层 Agent 通过 `/api/agent/*` 和 `/api/gateway/*` 使用 Trosa 的原子工具。Agent 可以读取今日工作、客户工作区、客户沟通时间线和跨客户沟通搜索结果，再根据用户目标组合多个原子工具；结构化写入要么生成登录会话下的提议，要么遵守 Gateway scope、幂等和撤销边界。Trosa 不包含独立 Agent/Web runtime，核心 CRM 业务独立运行。

## 9. 自动化边界

核心自动化负责保存、校验、生成快照、计算到期、更新动态视图和生成 ICS。可选辅助可以读取用户主动提交的官网内容、整理上下文或复核邮箱。

```text
系统：保存事实、计算日期、保护数据、呈现信号
辅助模块：读取和整理已授权信息
用户：判断价值、执行沟通、确认承诺
系统：记录决定，并在约定时间再次呈现
```

这条边界让核心流程可预测，也让所有关键业务动作保留明确责任人。
