# Trade OS 产品与设计标准

> 本文件是产品、界面、文案和交互修改的默认标准。现行代码与本文件出现冲突时，先核对真实业务行为，再同步修正实现或标准。

## 1. 产品承诺

Trade OS 帮助外贸业务员保存客户关系上下文，在约定时间看见明确待办，并把实际发生的沟通沉淀为可靠记录。

用户负责事实、判断和行动；系统负责保存、组织、提醒和保护数据；可选辅助模块负责读取与整理。

## 2. 六条原则

1. 客户关系是长期上下文。
2. 已发生的事情形成记录，具体动作与日期形成待办。
3. 核心工作流在离线、无模型的环境中完整可用。
4. 系统按信息压力分层，保留全部到期承诺。
5. 每次写入都有清楚结果，重要数据可追溯、可验证、可恢复。
6. 界面安静、直觉，并让第一次使用的人知道下一步。

## 3. 核心对象

- **客户**：一家公司及其长期关系信息。
- **联系人**：公司中实际沟通的人。
- **记录**：已经发生的事实，包括邮件、通话、会议、报价、寄样和客户回复。
- **待办**：未来明确承诺的动作，包含具体内容和日期，可关联联系人。
- **Inbox 信号**：需要用户判断的新信息，处理后离开。
- **分析备注**：基于事实形成的人工作业或可选模型输出，保留来源与时间。

除归档外，界面尽量少要求用户维护固定状态。可由记录推导的信息使用动态视图呈现。

## 4. 信息归属

1. 已经发生 → 客户时间线。
2. 尚待判断 → Inbox。
3. 已确认动作和日期 → 待办、今天和日历。
4. 可能值得关注 → 动态视图或低压力建议。
5. 日常无需再看 → 归档，保留恢复能力。

事实、承诺和建议使用清楚的视觉与文案边界。

## 5. 页面结构

日常主导航包括：

- **今天**：到期承诺、当前关注和未来短期安排。
- **Inbox**：待判断的新信息。
- **客户**：搜索、查看和维护全部关系。
- **本周工作**：团队选择分享的真实进展。

新客户、完整日历、全部记录、操作日志和设置位于场景入口。新增一级页面需证明现有页面、面板或动作无法清楚承载。

## 6. 客户组织

系统使用事实生成动态视图，例如：

- 等待回复
- 很久未联系
- 没有下一步
- 已归档

用户可以使用自由标签、保存视图和手动关注颜色。等级、阶段与旧状态字段保留历史兼容时，应降低其默认视觉权重。

## 7. 今天与待办

- 待办必须有具体动作和日期。
- 今天展示全部到期与逾期事项，不由模型隐藏。
- 手动排序表示个人当天关注顺序，不改写业务事实。
- 完成待办时记录实际结果；下一步由用户按需安排。
- 规则或辅助模块发现的关注点先进入低压力信号，用户确认后才形成待办。

## 8. Inbox

Inbox 只接收需要人判断、且尚未被现有任务覆盖的信息，例如客户回复、未联系复查、导入异常和用户已启用的监控信号。

- 普通成功提示、备份完成和系统日志留在对应页面或操作日志。
- 处理信号时可以记录事实、安排下一步或归档。
- 保存沟通、完成任务或安排下一步后，相关信号及时离开。
- 可选分析内容默认归入客户详情；只有确实需要用户判断的新变化才进入 Inbox。

## 9. 可选辅助模块

AI 问答、沟通整理、客户分析、官网监控和邮箱深度复核均属于可选辅助。

- 入口允许个人关闭。
- 关闭后保留历史内容，并停止对应的可选后台任务。
- 模型不可用时显示清楚状态，固定核心继续运行。
- 模型回答列出可核对的事实依据，并把推断单独标识。
- 信息不足时列为未知或待确认。
- 模型输出不直接写入业务事实。
- 创建待办需由用户确认客户、动作和日期。
- 发送消息、报价和承诺需由用户执行。
- 外部服务的数据范围、地址和密钥必须显式配置。

## 10. 数据与同步语言

- SQLite 业务目录是唯一事实源。
- Excel 入口使用“导入”“导出”或“恢复”，避免笼统称为双向同步。
- Apple 日历使用“订阅”或“检查订阅内容”，说明它只读取 ICS 事件。
- “同步”文案必须写清数据对象、方向和是否可回写。
- 当前产品说明不得宣称 iCloud 数据库同步、实时多端合并或冲突解决。

## 11. 数据安全

- 客户删除默认为归档，并可恢复。
- 永久删除放在高风险维护入口，并清楚说明影响范围。
- 导入保留原始来源和去重指纹。
- 数据修改后生成一致性快照。
- 恢复前先保存当前版本，并验证清单、哈希与数据库完整性。
- 多人共享部署保持单个应用进程；高并发需求触发数据库架构复审。

## 12. 本周工作

本周工作用于分享真实进展：

- 每个人从自己的记录中选择内容。
- 展示客户、日期、实际工作、结果和下一步。
- 支持历史周切换和窄屏单列。
- 页面不展示排名、KPI 或强制绩效摘要。

## 13. 交互与语言

- 每个页面提供一个清楚的主要动作。
- 保存后立即说明结果。
- 能在原地完成的操作保持上下文。
- 空状态说明页面用途和可采取的动作。
- 批量操作说明对象数量和影响范围。
- 使用业务语言：记录沟通、安排下一步、归档、导入、订阅日历。
- 技术名词仅在设置、维护和诊断场景出现。

## 14. 视觉语言

目标是安静、可信、清楚、有秩序。

- 文字层级优先于复杂卡片。
- 色彩用于识别、层级和个人关注。
- 数字和警示只表达真实到期或错误状态。
- 宽屏充分利用空间，移动端按任务顺序单列。
- 动效帮助理解状态变化，并尊重减少动态效果设置。

## 15. 修改验收

每次修改至少检查：

- 核心流程在无模型配置时能否完成？
- 信息属于事实、承诺、待判断信号还是建议？
- 是否新增了可由记录推导的维护字段？
- 数据源、导入方向和日历订阅范围是否表达准确？
- 写入、导入、归档和恢复是否可追溯？
- 保存结果和错误状态是否清楚？
- 桌面与窄屏能否完成核心操作？
- 语法、接口、回归测试和真实页面是否通过？
- 用户可感知变化是否写入 `CHANGELOG.md`？
