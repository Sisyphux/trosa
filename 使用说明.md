# Trade OS 使用说明

Trade OS 是外贸客户关系工作台。日常使用围绕客户、联系人、沟通记录、Inbox 和明确待办展开。核心功能使用本地 SQLite 保存数据，无需配置 AI，也不依赖 iCloud。

## 快速开始

### 公司周报入口

公司 Mac 在公司网络内开机并取得固定地址 `192.168.0.58` 后，公司内任何设备都可以打开 `http://192.168.0.58:8080`，直接进入只读“本周工作”。这个入口长期自动运行，不需要手动开启；Mac 离开公司、关机或休眠时暂时不可用，恢复公司网络后自动回来。

周报内容实时读取云服务器，Mac 不保存第二份客户数据库。该入口只能查看周报和周报客户详情，不能新增、修改或删除数据。

### Windows 一键启动

双击 `start.bat`。首次运行会创建虚拟环境并安装依赖，完成后自动打开 `http://localhost:8080`。

### 桌面窗口

```bash
python desktop.py
```

### 手动启动

```bash
python app.py
```

停止服务时在启动窗口按 `Ctrl + C`。应用会在关闭前创建数据库快照。

## 第一次使用

1. 选择 Hamid、Amy 或 Kelley 的个人工作区。
2. 在“客户”中手动添加、批量添加，或到“设置”上传 Excel。
3. 打开客户并补充联系人、最近沟通与明确下一步。
4. 在“今天”处理到期事项，在 Inbox 判断新回复和复查信号。
5. 到“设置”按需隐藏可选模块。

正常首次启动保持个人数据库为空。只有显式设置 `CRM_SEED_DEMO_DATA=true` 时才写入演示数据。

## 核心页面

| 页面 | 用途 |
|---|---|
| 今天 | 全部到期与逾期待办、手动关注顺序、未来 14 天安排 |
| Inbox | 需要判断且尚未被现有待办覆盖的新回复和复查信号 |
| 客户 | 搜索、添加、批量录入、联系人、时间线、当前等待和下一步 |
| 本周工作 | 从真实记录中选择团队共享内容 |
| 新客户 | 查看尚未形成真实联系的潜在客户 |
| 完整日历 | 按月查看待办并进入 Apple 日历订阅 |
| 全部记录 | 集中浏览和维护沟通历史 |
| 操作日志 | 核对新增、编辑、归档、导入和恢复操作 |
| 设置 | 个人界面、可选模块、Excel 导入和数据维护 |

## 日常工作方式

### 记录沟通

在“今天”、客户详情或快捷操作中选择客户，填写实际沟通内容、方向、结果和联系人。若已有明确下一步，同时填写具体动作与日期；系统会把事实写入时间线，并创建对应待办。

### 管理待办

- 待办包含具体动作和日期。
- 今天页显示全部到期与逾期事项。
- 拖动只调整当天个人关注顺序。
- 完成时记录真实结果，可选择继续安排下一步。

### 处理 Inbox

Inbox 用于判断新回复、未联系复查和已启用模块产生的少量信号。处理方式包括记录进展、安排下一步、确认无需记录或归档。

### 管理客户与联系人

- 一个客户可保存多个联系人。
- 联系人支持姓名、职位、邮箱、电话、WhatsApp 和 LinkedIn。
- 客户内相同邮箱会规范化并合并。
- 归档客户可恢复；永久删除位于高风险维护入口。
- 新增客户时输入官网并选择“从网站识别/自动识别”，系统会先读取官网事实和公开联系方式，再展示来源预览；确认后才填入表单，保存客户仍需再次确认。
- Exa MCP 和动态网页读取属于可选外部能力。没有模型时，官网标题、结构化数据、邮箱、电话和国家线索仍可提取；AI 整理必须由用户主动勾选，结果会标为待确认。

### 客户文件

- 客户详情工作区的“文件”页签可保存报价单、合同、样品图片、展会资料等原始文件，查阅客户时可直接查看或下载。
- 支持 Excel、Word、PDF、图片、文本、压缩包等基本类型，单个文件不超过 25MB，可多选或拖拽上传，并可选填分类。
- 点击文件条目即可打开：PDF 与图片在浏览器直接预览；Excel 与 CSV 会以表格形式预览前 50 行；文本类直接预览内容；其他类型点击即下载。列表右侧也保留“预览/下载/删除”按钮。
- 文件随客户详情一起加载；PDF 与图片可在浏览器直接查看，其他类型点击下载。删除需确认，删除记录保留在操作日志中。
- 文件元数据保存在本地数据库，文件本体存放在 `uploads/customer_files/`；从早期备份恢复后若文件本体缺失，列表会标注“文件本体缺失”，不影响其他数据。

## Excel 导入与导出

Excel 是数据交换和历史恢复格式：

- 上传 `.xlsx` 或 `.xls` 导入客户和历史沟通。
- 系统匹配常见列名，并保留原始文件、工作表和单元格来源。
- 重复历史记录通过内容指纹去重。
- 无法可靠匹配到客户的记录进入审阅，不会自动创建错误客户。
- 导出用于备份、交接或外部处理。

界面中的 Excel 操作不构成持续双向同步。CRM 数据以 SQLite 为准。

## Apple 日历订阅

完整日历页面提供个人 ICS 订阅地址。Apple 日历会按自己的获取频率读取未完成待办。

- 每位用户使用独立订阅令牌。
- 日历订阅只读取事件。
- 在 Apple 日历中修改事件不会回写 CRM。
- 需要立即核对时，可在系统中使用“检查订阅内容”。

Apple 日历订阅与 CRM 数据库跨设备同步是两条不同链路。当前系统不执行 iCloud 数据库拉取或推送。

## 数据存储与恢复

默认数据目录为项目下的 `data/`，可通过 `CRM_DB_PATH` 指向专用目录。

```text
data/
├─ system.db
├─ hamid.db
├─ amy.db
├─ kelley.db
├─ active_store.json
├─ backups/
└─ uploads/
```

- 每位用户的业务数据相互隔离。
- 数据修改后自动生成延迟安全快照。
- 恢复前会先保存当前版本。
- 快照带清单、哈希和 SQLite 完整性校验。
- 正式部署仍需把 `data/backups/` 加密复制到异地介质。

不要把 `data/` 放进 iCloud、Dropbox 等文件级实时同步目录。SQLite 正在运行时发生文件替换可能造成版本覆盖或损坏。

## 可选 AI 辅助

AI 问答、沟通整理、截图识别和客户总结都属于按需辅助，并共用当前生效的一套 AI 接口配置。未来新增 AI 功能也应复用这套共享连接。未配置模型时，客户、记录、待办、Inbox、日历、导入导出和备份恢复照常使用。

### 快速接入

管理员登录后打开「设置 → AI API 快速接入」：

1. 选择 DeepSeek、通义千问、智谱 GLM、OpenAI/兼容接口，或本地的 LM Studio/Ollama。
2. 填写 API Key、Base URL 和模型名；本地服务不需要 Key。
3. 点击「测试连接」。测试只发送固定的连接探针，不会携带客户资料。
4. 点击「保存并立即生效」。API Key 不回显、不写入 SQLite 客户数据库；设置页保存的独立文件默认位于 `CRM_DB_PATH/ai-config.env`，权限为 `600`。

也可以继续使用环境变量方式：本地开发复制 `.env.example` 为 `.env`；ECS 写入 `/etc/trade-os/trade-os.env`；Mac 正式运行写入 `~/Library/Application Support/TradeOS/env.production`。环境文件修改后需要重启应用。支持的 `LLM_BACKEND` 值为 `deepseek`、`qwen`、`glm`、`openai`、`lmstudio`、`ollama` 或 `auto`；建议选择一个具体后端，`auto` 仅用于兼容旧配置并选择一套共享连接。

截图识别会复用当前接口和模型，因此该模型还需要支持图片输入；未配置共享连接的旧环境仍可兼容使用单独设置的 `VISION_API_KEY`、`VISION_BASE_URL`、`VISION_MODEL`。模型结果属于辅助分析，形成待办前仍需确认客户、动作和日期。

内置 Pi Web 聊天入口（含 Pi SDK Runtime、原生 Pi 页面和嵌入面板）已移除。需要 AI 辅助时使用上方配置的“AI 助手”或“AI 客户分析”；AI 属于可关闭的辅助层，不影响客户、记录、待办、Inbox、日历和备份恢复等核心 CRM 功能。

## 其他可选模块

- **官网监控**：按个人设置启用，读取网站变化并产生待判断信号。
- **邮箱验证**：基础格式和 DNS 检查可用；SMTP 收件人复核默认关闭，需要专用网络与邮箱配置。
- **开发邮件**：记录已发送邮件和回复状态。
- **本周工作**：按个人需要显示或隐藏。

## 项目文件

| 文件或目录 | 职责 |
|---|---|
| `app.py` | Web 服务、接口和业务编排 |
| `db.py` | SQLite、迁移、快照和恢复 |
| `app/engine.py` | 可选模型调用与网站读取 |
| `scheduler.py` | 到期提醒和可选后台任务 |
| `ical_gen.py` | ICS 日历订阅 |
| `maintenance.py` | 数据目录维护 |
| `app/static/` | 前端界面与交互 |
| `tests/` | 风险回归测试 |
| `deploy/` | Linux 上线示例 |

## 常见问题

**数据库在哪里？**

默认位于 `data/`，每位用户一个数据库。当前实际路径也会写入 `data/active_store.json`。

**每次打开要重新上传 Excel 吗？**

不需要。导入后数据保存在 SQLite 中。

**需要联网吗？**

本地客户、记录、待办、备份和日历页面可离线运行。外部模型、网站读取、公网访问和远程日历订阅需要网络。

**如何在多台设备使用？**

推荐把应用部署在一台长期运行的主机上，各设备通过浏览器访问同一个服务。这样所有设备使用同一 SQLite 数据仓库。

**AI 无法使用会影响提醒吗？**

不会。到期计算、Today、Inbox 核心信号和备份均由确定性规则运行。

**Apple 日历为什么没有立刻更新？**

日历客户端按自己的计划抓取订阅。先在 Trade OS 中检查订阅内容，确认事件已进入 ICS，再等待客户端刷新或重新订阅个人链接。
