# Trosa 使用说明

Trosa 是外贸客户关系工作台。正式业务由 ECS 上的 PostgreSQL 唯一承载；日常使用围绕客户、联系人、沟通记录、Inbox 和明确待办展开。未配置 AI 不影响核心功能，也不依赖 iCloud。

## 快速开始

### 正式工作台

打开 `https://app.trosa.space`。Windows 可双击 `start.bat`，Mac 可双击 `Mac启动器.command`；两个启动器只打开正式工作台，不启动本地业务服务。

公司局域网的只读周报入口仍由 `com.tradeos.weekly-lan` 提供；它只转发 ECS 上的内容，不保存第二份客户数据库。

### 本地开发

正式服务唯一入口是 `serve.py`，只接受 PostgreSQL。PostgreSQL 演练使用
`python3 tools/postgres_rehearsal.py test`；只有需要旧 SQLite 形状回归时，才显式使用
`CRM_ENV=development TRADE_OS_DEV_SQLITE=1 python3 desktop.py`。不要直接运行 `app.py`。

## 第一次使用

1. 选择 Hamid、Amy 或 Kelley 的个人工作区。
2. 在“客户”中手动添加、批量添加，或到“设置”上传 Excel。
3. 打开客户并补充联系人、最近沟通与明确下一步。
4. 在“今天”处理到期事项，在 Inbox 判断新回复和复查信号。
5. 到“设置”按需隐藏可选模块。

新建隔离开发环境默认不写入演示数据；正式数据以 PostgreSQL 为准，不通过本地数据库初始化。

## 核心页面

| 页面 | 用途 |
|---|---|
| 今天 | 全部到期与逾期待办、手动关注顺序、未来 14 天安排 |
| Inbox | 需要判断且尚未被现有待办覆盖的新回复和复查信号 |
| 客户 | 搜索、添加、批量录入、联系人、时间线、当前等待和下一步 |
| 本周工作 | 从真实记录中选择团队共享内容 |
| 新客户 | 查看尚未形成真实联系的潜在客户 |
| 完整日历 | 按月查看待办并进入 Apple 日历订阅 |
| 全部记录 | 集中浏览和维护沟通历史 |
| 操作日志 | 核对新增、编辑、归档、导入和恢复操作 |
| 设置 | 个人界面、可选模块、Excel 导入和数据维护 |

## 日常工作方式

### 记录沟通

在“今天”、客户详情或快捷操作中选择客户，填写实际沟通内容、方向、结果和联系人。若已有明确下一步，同时填写具体动作与日期；系统会把事实写入时间线，并创建对应待办。

### 管理待办

- 待办包含具体动作和日期。
- 今天页显示全部到期与逾期事项。
- 拖动只调整当天个人关注顺序。
- 完成时记录真实结果，可选择继续安排下一步。

### 处理 Inbox

Inbox 用于判断新回复、未联系复查和已启用模块产生的少量信号。处理方式包括记录进展、安排下一步、确认无需记录或归档。

### 管理客户与联系人

- 一个客户可保存多个联系人。
- 联系人支持姓名、职位、邮箱、电话、WhatsApp 和 LinkedIn。
- 客户内相同邮箱会规范化并合并。
- 归档客户可恢复；永久删除位于高风险维护入口。
- 新增客户时输入官网并选择“从网站识别/自动识别”，系统会先读取官网事实和公开联系方式，再展示来源预览；确认后才填入表单，保存客户仍需再次确认。
- Exa MCP 和动态网页读取属于可选外部能力。没有模型时，官网标题、结构化数据、邮箱、电话和国家线索仍可提取；AI 整理必须由用户主动勾选，结果会标为待确认。

### 客户文件

- 客户详情工作区的“文件”页签可保存报价单、合同、样品图片、展会资料等原始文件，查阅客户时可直接查看或下载。
- 支持 Excel、Word、PDF、图片、文本、压缩包等基本类型，单个文件不超过 25MB，可多选或拖拽上传，并可选填分类。
- 点击文件条目即可打开：PDF 与图片在浏览器直接预览；Excel 与 CSV 会以表格形式预览前 50 行；文本类直接预览内容；其他类型点击即下载。列表右侧也保留“预览/下载/删除”按钮。
- 文件随客户详情一起加载；PDF 与图片可在浏览器直接查看，其他类型点击下载。删除需确认，删除记录保留在操作日志中。
- 文件元数据保存在 PostgreSQL，文件本体存放在正式服务的 `uploads/customer_files/`；从早期备份恢复后若文件本体缺失，列表会标注“文件本体缺失”，不影响其他数据。

## Excel 导入与导出

Excel 是数据交换和历史恢复格式：

- 上传 `.xlsx` 或 `.xls` 导入客户和历史沟通。
- 系统匹配常见列名，并保留原始文件、工作表和单元格来源。
- 重复历史记录通过内容指纹去重。
- 无法可靠匹配到客户的记录进入审阅，不会自动创建错误客户。
- 导出用于备份、交接或外部处理。

界面中的 Excel 操作不构成持续双向同步。正式 CRM 数据以 PostgreSQL 为准。

## Apple 日历订阅

完整日历页面提供个人 ICS 订阅地址。Apple 日历会按自己的获取频率读取未完成待办。

- 每位用户使用独立订阅令牌。
- 日历订阅只读取事件。
- 在 Apple 日历中修改事件不会回写 CRM。
- 需要立即核对时，可在系统中使用“检查订阅内容”。

Apple 日历订阅与 CRM 数据库跨设备同步是两条不同链路。当前系统不执行 iCloud 数据库拉取或推送。

## 数据存储与恢复

- 正式业务表、事务、身份和操作日志都在 ECS PostgreSQL；每位成员通过组织/成员关系隔离。
- 正式服务的 `CRM_DB_PATH` 只保存附件、导入来源和回滚材料，不是业务数据库目录。
- 正式备份是 PostgreSQL logical dump + 客户附件 bundle + manifest/哈希，并在独立位置做 restore-check。
- SQLite 只用于隔离开发、历史导入演练和明确批准的恢复边界；不得把本地 SQLite 当作正式数据源。
- 不要把正式服务的数据目录放进 iCloud、Dropbox 或其他文件级实时同步目录。

## 可选 AI 辅助

沟通整理、截图识别和官网智能导入都属于按需辅助，并共用当前生效的一套 AI 接口配置。未来新增 AI 功能也应复用这套共享连接。未配置模型时，客户、记录、待办、Inbox、日历、导入导出和备份恢复照常使用。

### 快速接入

管理员登录后打开「设置 → AI API 快速接入」：

1. 选择 DeepSeek、通义千问、智谱 GLM、OpenAI/兼容接口，或本地的 LM Studio/Ollama。
2. 填写 API Key、Base URL 和模型名；本地服务不需要 Key。
3. 点击「测试连接」。测试只发送固定的连接探针，不会携带客户资料。
4. 点击「保存并立即生效」。API Key 不回显、不写入 PostgreSQL 客户表；设置页保存的独立文件默认位于 `CRM_DB_PATH/ai-config.env`，权限为 `600`。

也可以继续使用环境变量方式：本地开发复制 `.env.example` 为 `.env`；ECS 写入 `/etc/trade-os/trade-os.env`；Mac 正式运行写入 `~/Library/Application Support/TradeOS/env.production`。环境文件修改后需要重启应用。支持的 `LLM_BACKEND` 值为 `deepseek`、`qwen`、`glm`、`openai`、`lmstudio`、`ollama` 或 `auto`；`auto` 会自动选择一套共享连接。

截图识别会复用当前接口和模型，因此该模型还需要支持图片输入。模型结果属于辅助分析，形成待办前仍需确认客户、动作和日期。

独立 Agent/Web runtime 不属于当前 Trosa。需要 AI 辅助时使用上方配置的共享连接，通过沟通整理、截图识别或官网智能导入触发；AI 属于可关闭的辅助层，不影响客户、记录、待办、Inbox、日历和备份恢复等核心 CRM 功能。

## 其他可选模块

- **官网智能导入**：用户主动提供官网地址后读取并整理为客户资料候选，结果仍需人工确认。
- **邮箱验证**：基础格式和 DNS 检查可用；SMTP 收件人复核默认关闭，需要专用网络与邮箱配置。
- **开发邮件**：记录已发送邮件和回复状态。
- **本周工作**：按个人需要显示或隐藏。

## 项目文件

| 文件或目录 | 职责 |
|---|---|
| `serve.py` | 唯一正式 Waitress 入口，强制 PostgreSQL |
| `app.py` | Web 路由、接口和业务编排（不作为正式启动命令） |
| `db.py` | PostgreSQL 迁移/契约、隔离 SQLite 和恢复边界 |
| `app/engine.py` | 可选模型调用与网站读取 |
| `scheduler.py` | 到期提醒和可选后台任务 |
| `ical_gen.py` | ICS 日历订阅 |
| `maintenance.py` | 数据目录维护 |
| `app/static/` | 前端界面与交互 |
| `tests/` | 风险回归测试 |
| `deploy/` | Linux 上线示例 |

## 常见问题

**数据库在哪里？**

正式业务数据库是 ECS 本机 PostgreSQL；`CRM_DB_PATH` 只用于附件、导入来源和回滚材料。

**每次打开要重新上传 Excel 吗？**

不需要。导入后数据由 Trosa 写入 PostgreSQL。

**需要联网吗？**

正式工作台需要访问 Trosa 服务；外部模型、网站读取、公网访问和远程日历订阅按功能需要网络。

**如何在多台设备使用？**

所有设备通过浏览器访问 ECS 上的同一个 Trosa 服务，共用同一个 PostgreSQL 事实源。

**AI 无法使用会影响提醒吗？**

不会。到期计算、Today、Inbox 核心信号和备份均由确定性规则运行。

**Apple 日历为什么没有立刻更新？**

日历客户端按自己的计划抓取订阅。先在 Trade OS 中检查订阅内容，确认事件已进入 ICS，再等待客户端刷新或重新订阅个人链接。
