# Trade OS 重构审查报告

> 历史文档：本报告记录 2026-07-23 的重构判断，部分建议已落地，部分实现已继续演进。当前产品与架构规则以 `README.md`、`PRODUCT_DIRECTION.md`、`PRODUCT_DESIGN_STANDARD.md` 和 `Trade OS 系统设计说明.md` 为准。

审查日期：2026-07-23  
审查范围：`app.py`、`db.py`、`scheduler.py`、`app/engine.py` 与前端静态资源。  
审查结论：现有系统已拥有成为外贸工作操作系统所需的大部分记录、提醒和数据安全基础。下一阶段应收敛 AI 前台能力，统一客户工作流的数据模型，并先完成“客户详情首屏 + 外部模型协作”的闭环。

## 1. 现有功能盘点

| 模块 | 当前能力 | 建议 | 依据 |
|---|---|---|---|
| Today | 今日待办、未来 14 天安排、批量完成、近期活动 | 加强 | 已由提醒与沟通记录驱动，符合行动优先原则。 |
| Inbox | 客户回复、AI 建议与人工处理项 | 调整 | 改为未归档信息入口；保留回复处理，移除 AI 建议作为主要类型。 |
| 客户列表 | 搜索、视图、筛选、批量新增、联系人导出 | 保留并加强 | 已支持公司、联系人、邮箱、国家及业务领域检索。 |
| 客户详情 | 沟通、联系人、开发信、提醒、AI 背调 | 调整 | 首屏需固定为最近发生、当前等待、下一步、最近沟通与关键事实。 |
| 沟通记录 | 跟进记录、方向、关联联系人、下一步、提醒 | 加强 | 可演进为统一 Interaction 时间线。 |
| 开发信 | 手工保存开发信、回复状态 | 调整 | 改名为“已发送邮件/沟通”，补充收件人、发件人、附件、消息 ID 与来源。 |
| 客户背调报告 | 单客户与批量生成、官网抓取、存储报告 | 暂停前台生成 | 保留已保存的可追溯事实；停止将报告作为客户页面核心内容。 |
| 全局 AI 问答 | 读取 CRM 后由模型回答并可创建任务 | 弱化 | 保留为用户主动触发的只读助手，禁止使用推断替代事实。 |
| 官网监测 | 定时抓取官网变化、生成提醒 | 保留并加强 | 适合“公开动态 + 来源链接 + 人工决定”的定位。 |
| Excel 同步 | 上传、解析、恢复历史、回写 Excel | 调整 | 保留一次性迁移和备份用途；停止作为客户数据的并行日常维护源。 |
| 联系人和邮箱校验 | 联系方式、SMTP/MX 校验、投递事件 | 保留 | 属于可验证事实，符合新边界。 |
| 周报与统计 | 本周工作、概览、操作日志 | 调整 | 指标切换为新增、首次联系、跟进、回复、逾期与重复拦截。 |
| 日历与提醒 | 任务、iCal 订阅、系统通知 | 保留 | 是防遗漏的核心能力。 |

## 2. AI 功能盘点

| 能力 | 输入与输出 | 当前展示/写入 | 风险 | 决策 |
|---|---|---|---|---|
| 客户背调报告 | 官网、客户资料、沟通记录 → 摘要、需求、合作价值、建议 | 客户详情 AI 背调；`research_reports` | 信息密度高，推断内容占据核心页面 | 暂停自动与批量生成；保留历史报告和来源。 |
| 沟通整理 | 沟通原文 → 摘要、需求、事实、意向、下一步 | Inbox 录入与沟通记录弹窗 | `intent` 和建议下一步可能越过证据边界 | 保留“事实提取”和摘要；默认隐藏意向与建议，须用户确认后写入。 |
| AI 建议 Inbox | 近期理解/活动 → 建议动作 | Inbox、Today、客户详情 | 自动建议成为待办来源，容易制造无依据行动 | 移除主流程；现有数据迁移为普通备注或已处理记录。 |
| 全局 CRM 问答 | CRM 数据 + 问题 → 只读回答和证据 | 浮动 AI 面板 | 模型可替代；已有证据标记机制 | 保留为按需功能，优先使用本地事实检索，输出明确分隔事实/推断。 |
| 官网资料提取 | 官网正文 → 公司资料 JSON | 智能导入 | 资料来源目前未成为独立可追溯实体 | 保留，新增来源 URL、抓取时间、用户确认状态。 |
| 官网变化监测 | 官网快照 → 内容变化/提醒 | 任务与 `web_monitor_logs` | 按客户等级触发，耦合旧等级模型 | 保留监测；改用用户订阅、最近联系或重要标记决定频率。 |

## 3. 数据流审查

### 已具备的链路

1. 客户可由手工、新建批量录入、智能官网导入和 Excel 导入进入 `customers`。
2. 跟进记录写入 `follow_up_logs`，下一步同步为 `reminders`，Today 读取提醒。
3. 联系人、开发信、背调、官网监测分别关联客户。
4. 数据库按用户拆分，并具备完整性检查、备份、恢复和 iCloud 同步支持。

### 主要断点与重复

1. 客户状态分散于 `customers.status`、`level`、`attention_state`、`customer_understandings.action_state` 与 `ai_recommendations.review_status`。这些字段表达的语义重叠，且部分由推断产生。
2. 沟通事实分散于 `follow_up_logs`、`outreach_emails` 和 `inbox_items`；时间线无法天然覆盖收发邮件、电话、WhatsApp、LinkedIn 和文件。
3. `research_reports`、`customer_understandings` 和 `ai_recommendations` 都保存面向客户的总结/判断，形成多套“客户理解”。
4. Excel 支持导入与回写，数据库同时也承担主数据职责；应明确 SQLite 为唯一主数据源，Excel 仅作为迁移、导入/导出和备份格式。
5. 现有公司去重主要依赖 Excel 导入中的名称匹配；常规客户新建与外部结果批量导入缺少统一的标准化公司名、域名和联系人去重服务。

## 4. 页面审查

| 页面 | 用户首先看到的内容 | 结论 | 首要调整 |
|---|---|---|---|
| Today | 今日待办、未来安排和周记录 | 基础合理 | 加入新回复、未完成记录、长期未推进、今日完成量。 |
| Inbox | 客户回复与 AI 建议 | 定位偏窄 | 支持粘贴线索、邮件、网址、文件和外部模型结果；一键归入客户、联系人、沟通或行动。 |
| 客户列表 | 客户视图、筛选、等级/状态过滤 | 旧模型仍显著 | 默认使用“待联系、等回复、已报价、长期未推进、已归档”等事实视图；隐藏等级和阶段。 |
| 客户详情 | 资料、AI 背调、记录等分区 | 无法保证 3 秒恢复上下文 | 重排首屏，AI 报告移入折叠的“分析备注”。 |
| 周报/概览 | 记录与客户等级相关统计 | 可用但指标偏旧 | 改为真实工作量和遗漏风险指标。 |

## 5. 技术债务与数据结构建议

### 优先清理

1. 前台隐藏 `level`、`status`、`attention_state`、AI 意向判断和 AI 推荐；旧字段先保留用于迁移和历史兼容。
2. 停止写入 `customer_understandings` 与 `ai_recommendations` 作为自动动作依据；移除其对 Inbox、Today 排序和提醒的影响。
3. 以一次可回滚的迁移将 `outreach_emails` 并入统一沟通时间线；在兼容期保留旧表读取。
4. 将客户去重提炼为单一服务：标准化域名优先，其次标准化公司名+国家，再以联系人邮箱辅助确认。

### 目标数据模型

- `customers`：保留公司资料、来源、`last_contact_at`、`next_action_at`、`current_waiting`、`next_action_text`、无效与归档信息。
- `contacts`：补齐来源、验证状态、更新时间。
- `interactions`：统一邮件、WhatsApp、LinkedIn、电话、会议、备注与已发送内容；保存方向、主题、发生时间、外部消息 ID 和来源。
- `actions`：将提醒升级为行动对象，包含内容、到期、完成、结果和来源沟通记录。
- `customer_facts`：只保存可追溯的产品、规格、数量、价格、认证、交期、承诺和异议；每项关联原始记录与用户确认状态。
- `file_records`：关联客户、沟通、文件路径、类型、来源和上传时间。
- `public_updates`：保存公开动态的来源 URL、发布时间、抓取时间、摘要与用户处理状态。

## 6. 分阶段实施建议

### 阶段一：清理与重新聚焦

1. 客户详情首屏重排为最近发生、当前等待、下一步、最近沟通、关键事实和快速操作。
2. 隐藏客户等级、阶段、意向和 AI 推荐的前台入口；保留旧数据以避免历史丢失。
3. 将 Today 的提醒来源限制为用户创建、邮件/沟通明确生成以及可验证的异常规则。
4. 将 Inbox 的 AI 建议类型迁移为普通备注，新增未归档信息的快速录入和转换。

验收：打开客户后可在 3 秒内得知最近发生、当前等待和下一步；无模型调用时核心流程完整可用。

### 阶段二：外部模型协作闭环

1. 提供简洁、完整、仅沟通、仅报价与需求的 Markdown 上下文复制。
2. 提供排除名单 CSV/JSON 导出，字段包括公司、标准化域名、国家、联系人、历史联系/推荐/无效状态和时间。
3. 支持 CSV、JSON、Excel、Markdown 表格粘贴导入，展示成功、重复、缺失和待确认数量。
4. 支持外部模型结果回填为分析备注、待办、跟进日期、邮件草稿和事实；所有结构化写入均须确认。

验收：导入 20 个客户无需逐个创建；用户能一键复制完整客户上下文；外部结果不会自动覆盖已确认资料。

### 阶段三：减少手工记录

1. 先实现邮件导入/关联，再实现已发送邮件与回复的时间线写入。
2. 支持附件关联、未识别邮件进入 Inbox、回复后缺失下一步提醒。
3. 实现报价后未跟进、客户回复后无行动、文件未关联客户、新客户长期未联系等异常提醒。

### 阶段四：公开动态

1. 基于现有官网监测扩展来源、发布时间和历史快照展示。
2. 将 LinkedIn、Instagram、Facebook 等开放平台接入作为独立能力评估，确保合规、来源可追溯和稳定性后再上线。

## 7. 建议的首个开发包

首个开发包建议限定为“客户详情事实工作台”，避免同时修改导入、邮件和数据迁移：

1. 重排详情页首屏和快速操作。
2. 建立事实卡片与手工维护入口。
3. 新增 Markdown 上下文复制。
4. 将 AI 背调、AI 推荐和意向从默认视图移至折叠的“分析备注”。
5. 将 Today/Inbox 中的 AI 建议从自动待办来源移除。

该开发包直接改善上下文恢复速度，同时保持现有客户、联系人、跟进、提醒和备份数据可用。
